# B7 · 프로젝트 · 분석 서비스 — 안 그려진 상태 24판 (2026-09-20)

생성기 `node tools/design/gen-b7-project-states.mjs`(repo root · 멱등 확인 — 2회 실행 md5 동일) · 렌더 `node tools/design/render-b7.mjs <이름들>`(= `render-b6-account.mjs` 와 같은 코드, 임시 폴더만 `_tmp-b7-states`) · 이미지 `design-canvas/v2/img/b7-states-*.jpg` 18장(각 ≤ 57 KB · `landxi/assets/proto/crops/**/n-clean.jpg` 640×420 무오버레이 원본).

## 0. 발주 원문

> YOUR SCOPE = the undrawn states that the audit found in the 프로젝트 and 분석 서비스 masters (P4m · P5m · P6m · P7m · P10m · A1m · A4m · §0 15–25행). No 선택 1·2·3 — these are missing STATES of already-designed screens, so you must match the existing masters exactly. … FIX the craft defects the audit lists for Labeling toolbar (P6m) and Train (P5m) by drawing corrected versions.

## 1. 어휘를 맞춘 방법

- 부품은 **B5 생성기의 코드를 그대로 불러 쓴다**: `gen-b5-project.mjs` · `gen-b5-analysis.mjs` 의 소스를 읽어 `import` 줄과 `wr('B5-…')` 줄만 떼고 함수 범위에서 평가 → `div · txt · disp · lab · st · tb · cta · fld · sel · search · chk · img · pager · poly · brkIn · ico · ibtn · mapTools · mapPlate · tealPolys · mapTag · tabs · headCollapsed · drawer · node · nchip · head · segs · steps …` 가 B5 와 바이트 단위로 같은 마크업을 낸다. B5 `.dc.html` 은 다시 쓰지 않는다(mtime 확인).
- 모달 밑그림이 B5 판 그대로인 곳(진행 오버레이 = `B5-Analysis-Run-Review`)은 B5 함수의 출력을 통째로 깔고 스크림을 얹었다.
- 레일은 `tools/design/b5-rail.html` 에서 직접 조립: 활성 막대 **1개**(분석 판의 이중 활성 P2m 없음) · 마크 `LAND XI` 14px / 자간 .06em(B6 account 값 — 법전 바닥 14 · P1m 두 줄 넘침 없음).
- 모달 그릇 = `B5-Project-Delete`(스크림 `rgba(1,1,2,.54)` x 72–1440 · 흰 판 · 제목 Paperlogy 20 · 바닥 헤어라인 · `취소` 텍스트 + 잉크 CTA).
- 새 CSS 클래스 0 — 헬멧은 B5 의 것 그대로(`@import` 가 `@font-face` 앞).

## 2. 파리티 표 (원본 → 아트보드 · 자리)

원본 소스 확인: `ai-project.html` · `ai-project-view.html` · `ai-project-dataset.html` · `ai-project-models.html` · `ai-project-deploy.html` · `ai-project-work.html` · `analysis-ai.html` + `page-ai-project6-{list,view,work}.js` · `page-ai-project7-dataset.js` · `page-analysis-{ai,done}.js` · `admin-info-data.js` · `ai-project-data.js`.

| # | 원본 상태 · 컨트롤 | 아트보드 | 자리 · 비고 |
|---|---|---|---|
| 1 | 목록 0건 — `프로젝트가 없어요`(list.js:64) | **B7-Projects-Empty** | 툴바(검색 · 초기화 · 검색 · 페이지 크기 · 페이저 · 검정 CTA `프로젝트 만들기`) 그대로 · 좌 = 점선 + 브래킷 판(원본 문구 + `프로젝트 만들기 ›`) → 탭 5 실크롭 띠(데이터 · 라벨링 · 학습 · 분석 · 배포) → 아카이브 8 썸네일(파일 추가 모달과 같은 목록) · 우 `프로젝트 조회 0 / 0` = Create 폼 7필드 목차 + 탐지 유형 실크롭 2 |
| 2 | 검색 0건 | **B7-Projects-NoResult** | 검색 필드 `태양광` 잉크 테두리 + × · `총 8건 중 0행` · 점선 판 `검색 조건에 맞는 프로젝트가 없어요` + `초기화 ›` · 아래 `전체 프로젝트 8` 무채 썸네일 · 우 패널 = 선택 없음(액션 3 비활성) — 좌·우·건수가 같이 0 을 말한다(감사 S3 교훈) |
| 3 | 구성원 초대 모달 `#ap-modal-invite`: 아이디(이메일)* + `확인` → 이름 자동(`id.split('@')[0]`) · 취소 / 구성원 초대 · 토스트 | **B7-Project-Invite** | 480×392 · 아이디 `sylee@namwon.go.kr`(원본 placeholder 예시 → `시연`) · `확인됨` · 이름 `sylee` `자동` 틴트 · **역할 라디오 `편집자 / 뷰어`**(인벤토리 §4-2 · 현 원본 JS 는 편집자 고정 → 유보 ②) · `초대 후 구성원 4` |
| 4 | 초대 오류 — `아이디를 확인해 주세요.`(has-error + NotifyUI.warn) | **B7-Project-Invite-Error** | 확인 없이 `구성원 초대` → 필드 잉크 테두리 + 빨강 글자 1줄(채움·테두리 빨강 0) · 이름 = 점선 `아이디 확인 후 자동 입력`(원본 placeholder) · 역할 비활성 |
| 5 | 개요 수정 `openEditForm()`: 프로젝트명*(100) · 대표 이미지(안내문) · 탐지유형 카드 2(라벨 · 설명) · 취소 / 저장 | **B7-Project-Overview-Edit** | 상태어 `수정 중` · 헤더 액션 숨김 · 본문 = 인라인 폼 · 탐지유형 카드 380×214 실크롭(OD = pj-hero + 박스 · Seg = 농지 + 폴리곤) · `저장` 검정 CTA 1 · 원본 문구 `바운딩 박스로 객체 위치 탐지` `폴리곤으로 정밀 윤곽 탐지` `… 대표 이미지 · jpg · jpeg · png · gif` |
| 6 | 개요 구성원 카드(P4m `—` 3행) | **B7-Project-Overview-Members** | A12 선택 2: `내 계정 · 소유자 · 본인`(잉크 아바타 · 체크 없음 = 소유자는 삭제 불가) + `편집자 A` `편집자 B` `시연` · 초대 인라인 행 → 점선 `+ 구성원 초대`(모달 #3 으로 1경로) · 최근 활동 날짜 열 108px(날짜·본문 붙음 해소) · 등록일시 `—` → 목록 판과 같은 `2025-06-12 14:30 시연` |
| 7 | 파일 추가 모달: 유형 ▾ · 검색어 · 초기화 / 검색 · 아카이브 8건 표 · 페이지네이션 · 취소 / 추가 | **B7-Project-File-Add** | 920×724 · 표 7열(☐ · 미리보기 78×44 · 데이터명 · 유형 · 촬영 · GSD·규모 · 용량 · 상태) · 선택 2 = 틴트 + 액센트 2px · `추가됨` 3 = 무채 + 점선 체크 · SHP = 점선 칸 · `선택 2건 · 106.4 GB` · 밑그림 = 데이터 탭 + 우 `파일 조회` |
| 8 | 파일 추가 진행 (P10m) | **B7-Project-File-Progress** | 새 타일 2 가 그리드 앞: `추가 중 63 %`(좌→우 현상 + 액센트 선 + 하단 3px) · `대기`(무채) — 상태어는 이미지 좌하 **흰 판 위**(S8) · 우 `추가 현황` = 큰 판(현상 중) + 표(썸네일 · 상태 · 진행률 · 잔여 — 데이터 관리 `진행 현황` 열) + kv 4 · `일시정지 · 취소` · 검정 CTA 0 |
| 9 | 파일 추가 실패 (P10m) | **B7-Project-File-Fail** | 타일 = 무채 + 점선 + `추가 실패 · 좌표계 없음`(빨강 글자) · 우 = 사유 · 조치 · 크기 · `11 / 12` · `선택 제외` `좌표계 지정 ›` + 검정 CTA `다시 시도` · 탭·세그먼트 건수 11 |
| 10 | 데이터셋 만들기: 데이터셋명* · 버전* · 라벨링 데이터 선택(체크 목록) · **데이터셋 설정 자동 제안**(기준 해상도 · 타일 크기 · 겹침 비율 · 근거 1 선택 영상 해상도 · 설정 수정 · 제안값 복원) · 취소 / 만들기 | **B7-Project-Dataset-Create** | 좌 = 폼 2필드 + 표 3행(실크롭 160×90 · 체크 3) + 원본 안내 `공간정보(shp)는 … 제외` + 합계 큰 수 4 · 우 드로어 `데이터셋 설정 자동 제안` = 타일 격자를 얹은 실크롭 + 큰 수 3(`1.54 cm` 액센트 · `1024 px` · `20 %`) + 근거 막대 3(중앙값 = 기준) + 원본 문장 1 · `만들기` 검정 CTA |
| 11 | 데이터셋 목록/상세(원본: 목록 행 · 선택 삭제 · 만들기 · 페이지네이션 — 별도 상세 페이지 없음) | **B7-Project-Dataset-Detail** | NOTES §15.2 #6 "선택 행 상세 = 데이터셋 세그먼트 판의 드로어" 를 그린 것 · 좌 = 데이터셋 카드 2(샘플 4장 띠) + `라벨링 데이터 3`(어느 셋에 포함) · 우 = 큰 판 + kv 5 + 클래스 막대(results.js 1,469 / 205) + 샘플 3 |
| 12 | 새로 학습하기 드로어: 학습명* · 기반 모델(백본 · 잠금 · 안내문) · 이전 학습 이어가기 ▾ · 데이터셋* ▾(+ 승계) · 탐지 형태(고정) · 고급 옵션{학습:검증 · 배치 · 에폭 · IoU · Conf} · 취소 / 학습 시작 | **B7-Project-Train-New** | 우 드로어 480 · 필드 11(발주 목록의 `이미지 크기` 포함) · 원본 힌트 3줄 · 좌 캔버스 = `학습 #6 설정 중`(액센트 브래킷 · 검증/결과 = 점선 대기) · 툴바 CTA → `새로 학습하기` 틴트 활성(검정 CTA 는 드로어 `학습 시작` 1) |
| 13 | 학습 진행 중(원본 = 상태칩 `진행중` + 소요 `-`) | **B7-Project-Train-Running** | 시드 `학습 #4 · 12 / 100`(`시연`) 선택 · 노드 진행선 12 % · 곡선 = 12 에폭까지 실선 + 이후 점선 고스트 + 테이프 `12` · 드로어 = 큰 수 `12 / 100` + IoU 점선 칸(검증 후) + kv 10 · 취소 버튼 없음(원본에 없음) |
| 14 | AI 모델 등록 — 원본은 빈 상태 + `pt 파일 등록 기능은 추후 개발 협의`(딤) | **B7-Project-Model-Register** `추정` | 드로어 720 · 등록 방식 라디오(`학습 결과에서 등록` / `외부 모델(pt)` **준비 중** 비활성) · 모델명* · 학습 결과 표 3행(실크롭) · 자동 4(기반 모델 · 탐지 형태 · 클래스 · 데이터 유형) · 설명 — **발행 폼이 이미 가진 필드만** 빌렸다 → 유보 ① |
| 15 | 〃 등록 후 | **B7-Project-Model-Registered** `추정` | 틴트 띠 `등록 완료` · 모델 카드(실크롭 372×224 + kv 4) · `등록하지 않은 학습 결과 2`(`등록 ›`) · 점선 `외부 모델 … 준비 중` · 우 `모델 정보` + 검정 CTA `카드 발행 요청`(다음 단계) |
| 16 | 발행 폼 `학습 결과 선택` 모달 픽커 3 | **B7-Project-Deploy-Picker** | 680×452 · 표 5열(학습 결과 · 학습일 · 라벨 · IoU · F1) + 실크롭 112×64 · 사각 라디오 · `선택 1 · 비닐하우스 v2.1` · 밑그림 = 발행 폼 13필드(B5 의 작은 대화상자 제거) |
| 17 | 서비스 목록의 준비 중 10 (A1m · A7) | **B7-Analysis-List** | 준비 중 카드 = 무채 + 점선 테두리 + 이름 회색 + `준비 중` 칩 · 선택 = `태양광 설비 현황`: 우 패널 무채 큰 판 + 상태어 + 이유 1줄 + `제공 중인 서비스 5`(선택 ›) + 버튼 2 비활성 + **CTA `준비 중` 점선 비활성** · 숫자 0(발주 2차 유지) · 썸네일 15장 전부 다른 크롭(A5m) |
| 18 | 분석 진행 오버레이 `#ai-progress-overlay`: 제목 · 단계(전처리 · 추론중 · 후처리 · 완료) · % · 막대 · 필지 진행 · 시작 시각 · 새로 분석하기 · 분석 결과 보기 | **B7-Analysis-Progress-Overlay** | 640×560 · 스캔 현상 실크롭(청록만 — 앰버 상시 칩 없음, A2m) · 단계 눈금 2 %/95 %(원본 JS 임계) · 큰 수 `1,205 / 1,674` `13:42` `시연` |
| 19 | 결과 편집 모드: 이동 · 삭제 · 저장 · 취소 + 하단 행정정보 목록(분석값만 수정) | **B7-Analysis-Result-Edit** | 지도 상단 툴 4(`이동` 활성 · `삭제` · `취소` · 검정 CTA `저장`) · 선택 필지 = 액센트 + 꼭짓점 핸들 + 원위치 점선 · 삭제 필지 = 흰 점선 · 하단 표 펼침(열 8 · `admin-info-data.js` FL_ROWS 5행 `시연` · 면적 셀만 입력) · 우 = `저장 전 변경 2` 띠 + 큰 수 −1 + 버튼 3 비활성 + 도구 설명 2줄 |
| 20 | 공유 설정 모달(기관·역할 9 · 취소 / 저장) | **B7-Analysis-Share** | 520×640 · 원본 문구 · 기관 3 묶음(틴트 머리띠 `n / m`) · 역할 9 전부 보임(스크롤 0) · `선택 2 / 9` |
| 21–22 | 로딩 / 오류 (원본에 전용 화면 없음 → 공용 패턴) | **B7-State-Loading** · **B7-State-Error** | 개요에 적용 · 아래 §3 |
| 23 | 라벨링 툴바 8 (P6m) | **B7-Project-Labeling-Fix** | 툴바 6(폭 = 글자 수 계산 · 겹침 0) + `저장` CTA · `닫기` 는 헤더 48 우측(§15.7 ⑬ 닫기 중복 해소) · 번호표는 도형 왼쪽 위(지도 도구와 겹침 0) · 라벨 행 15 · `×` 회색 #686868 |
| 24 | 학습 (P5m) | **B7-Project-Train-Fix** | 상태 줄 폭 320 우정렬(열 밖 0) · `시연` 칩 → 제목 옆 · `v2.0 · 0.80` 라벨 = 점 아래 + 흰 헤일로 · 오분류 행렬 14px / 셀 48×22 |

**추가 기능 없음 확인** — 빈 상태의 탭 띠 · 아카이브 썸네일은 이미 있는 메뉴·목록의 표시. File-Progress/Fail 의 `일시정지 · 취소 · 다시 시도 · 좌표계 지정` 은 데이터 관리 업로드(구현본)에 있는 동작을 같은 말로 옮긴 것. Result-Edit 의 `저장 전 변경` 은 편집 상태의 표시(서버 기능 0). 정렬 · 즐겨찾기 · 일괄 · 비교 없음.

## 3. 로딩 / 오류 공용 패턴 (구현 규칙)

1. 셸(레일 · `‹ 목록` · 탭)은 즉시 그린다. 탭 배지는 값이 올 때까지 없음.
2. 값 자리 = 무채 막대 `#EFEFEF`(그라디언트 · 반짝임 0 — 법전 §2). 라벨은 실제 라벨.
3. 직전 화면이 가진 대표 이미지는 **무채 .38** 로 유지(목록 카드에서 이미 받은 썸네일) + 점선 외곽.
4. 진행 = 판 상단 2px 액센트 선 1개(화면당 움직이는 요소 1).
5. 오류 = 같은 판 무채 .22 + 흰 판 위 `불러오기 실패`(빨강은 이 상태어 글자에만) + 제목 + 이유 1줄 + 검정 CTA `다시 시도` + `‹ 프로젝트 목록`. 값 자리는 점선 칸 / `—`(결손 = 점선 무채, §5).

## 4. 조판 실측 (1440×900)

- 프로젝트 셸: 레일 72 · 헤더 헤어라인 y 64 · 본문 x 128–904(776) · 드로어 x 960–1440(안쪽 984–1416) · 푸터 y 866 — B5 값.
- 분석 셸: 마스트헤드 64 · 타이틀 34 + 파랑 룰 4(y 140) · 본문 x 128–1384 · List 좌 772 / 우 444 · Result 드로어 360 / 400 — B5 값.
- 모달: Invite 480×392 · File-Add 920×724 · Deploy-Picker 680×452 · Progress 640×560 · Share 520×640.
- 글자: 최소 14px(자동 검사 0건 — 아래) · 큰 수 Paperlogy 800 58/50/36 · 표 숫자 Inter.

## 5. Craft 재비평 기록 (렌더 PNG 를 열어 보고)

- **1차(24판 전부)** — Empty 고스트 카드 4 = 딱딱한 빈 판 → 메시지 판 + 실크롭 띠 + 아카이브 썸네일로 재구성 · Edit `탐지유형` 라벨이 틴트 띠에 가림 · Dataset-Create 안내문/페이저가 큰 수와 겹침 · GSD 큰 수 914px 까지 넘침 · Model-Register `준비 중` 칩이 모델명 필드 위 · 드로어 720 을 푸터 헤어라인이 가로지름 · Overlay `새로 분석하기` 가 CTA 밑 · Result-Edit 선택 필지가 좌 드로어에 잘림 + 칩이 표에 반쯤 가림 · Labeling `#1` 번호표가 좌 드로어에 잘림 · Analysis-List 부처 줄이 다음 행 이미지에 잘림 · Train-Running IoU `—` 가 막대로 보임 · Loading 제목이 영상 위에서 안 읽힘.
- **2차** — 위 전부 수정. 추가: 타일 상태어가 이름을 말줄임시킴(S5 재발) → 상태어를 이미지 좌하 흰 판으로, 이름 전폭 · 우 패널 빈 띠(118–196px) 5곳 → 표 행에 썸네일 · kv 1행 · 원본 힌트 · 도구 설명으로 채움 · `나` 표식이 잘려 보임 → `본인`.
- **3차** — 무채 필터가 잘못된 타일에 걸림(정규식) → 명시 마크업으로 교체 · Empty `학습`·`분석` 같은 장면 → 다른 크롭 · 지도 위 채운 파랑 칩 `#3 · 이동 중` → 흰 바탕 + 액센트 테두리(X4m) · Model-Register 좌 빈 판 470 → 340 + 점선 `외부 모델 … 준비 중`.
- 자동 검사(Playwright 계산 스타일): 14px 미만 **0** · 캔버스 밖 글자 **0** · 말줄임 = Overlay 밑그림(B5-Run-Review 의 영상 이름 5 · 과제 줄 1)뿐, B7 자체 0. B5 밑그림의 소지도 13px 지명 4(A6m)는 밑그림에서 14 로 올렸다.

## 6. 자체 검수 grep (B7 24판)

`border-radius|box-shadow|gradient|backdrop` = **0** · 서체 = Paperlogy / Pretendard / Inter 뿐 · 채운 파랑 **버튼 0**(`background:#006DF7` 27곳은 전부 진행 막대 · 2px 선택 막대 · 1px 현상선 · 파랑 룰 4px) · `#D1352B` = File-Fail 3 · Invite-Error 1 · State-Error 2(전부 상태어·오류 글자색) + 분석 헬멧 토큰 선언 · 앰버 사용 0(토큰 선언만) · 검정 CTA 판당 1(모달 판은 스크림 밑 1 + 모달 1 = B5-Project-Delete 와 같은 성격).

## 7. 유보

① **모델 등록 폼은 원본에 없다.** 원본 `ai-project-models.html` = 빈 상태 + "pt 파일 등록 기능은 추후 개발 협의". 발주 목록에 있어 `추정` 칩을 달고 발행 폼의 기존 필드만으로 그렸다 — 구현 전 클라이언트 확인 필요(허브 D번호 후보).
② **초대 모달의 `역할`** — 인벤토리 §4-2 는 편집자/뷰어 선택, 현 원본 JS 는 `role: '편집자'` 고정(모달에 역할 필드 없음). 인벤토리를 따랐다.
③ **개요 수정 필드 수** — NOTES §15.2 #3 은 "인라인 4필드", 현 원본 소스는 3(프로젝트명 · 대표 이미지 · 탐지유형). 소스를 따랐다.
④ **`이미지 크기`** — 발주 목록 · B5 Train 칩에는 있으나 현 원본 폼은 `데이터셋 설정 승계`(기준 해상도 · 타일 · 겹침)로 바뀌었다. 둘 다 그렸다(이미지 크기 ▾ + 승계 1줄).
⑤ **썸네일이 B5 와 다르다.** 파일·영상 이름은 B5 와 같지만 흐린 업스케일(P8m · A5m)을 피하려 `b7-states-*` 선명 크롭을 썼다. 승인되면 B5 생성기의 `files`/`EV` 도 같은 크롭으로 바꾸는 것이 맞다.
⑥ **라벨 수 240 · 88**(남원 농경지 2025.04 · 08)은 원본 pid 6 시드를 옮긴 `시연`. 합계 2,002 도 그 합. Train-Running 의 loss 0.64 · precision 0.52 는 곡선식의 **형태 견본**(`견본` 칩).
⑦ File-Progress/Fail 의 용량(55.4 / 51.0 GB) · 실패 사유 `좌표계 없음` 은 데이터 관리 업로드 시드(`시연`). 원본 프로젝트 파일 추가는 즉시 완료(mock)라 진행·실패 화면 자체가 원본에 없다 — 감사 P10m 요구로 그린 상태.
⑧ Dataset-Detail · Train-New 의 데이터셋 이름은 B5 Data 판의 `비닐하우스 단동/다동 라벨셋 v1.0 추정` 을 그대로 썼다. Train 노드의 `데이터셋 v2` 표기와는 B5 안에서 이미 어긋나 있다(손대지 않음).
⑨ B7 생성기는 실행 시 B5 생성기 소스를 읽는다 — B5 생성기가 고쳐지면(P1m 레일 등) B7 밑그림도 따라 바뀐다. 의도한 결합이지만 B5 함수 이름이 바뀌면 `load()` 의 이름 목록을 같이 고쳐야 한다.
⑩ 작업 중 1회, 렌더 명령의 이름 글롭이 넓어 다른 에이전트의 `B7-Map-* · B7-Report-* · B7-Stats-*` PNG 를 그들 소스 그대로 다시 찍었다(소스 · 생성기는 건드리지 않음). 해당 에이전트가 마지막에 다시 렌더하면 무해.
