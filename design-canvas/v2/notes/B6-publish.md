# B6 · 카드 발행 관리 (`publish`) — 2026-09-20

생성기 `tools/design/gen-b6-publish.mjs`(멱등 · 아트보드 20) · 렌더 `renders/B6-Publish-*.png` · 추가 이미지 `img/b6-publish-{road,trash,gh2}.jpg`(각 ≤ 22 KB, `landxi/assets/proto/crops/**-clean.jpg` 복사).

## 0. 발주 원문
"우린 Geo-AI 플랫폼 구축이 목표이고 현재는 UI/UX 관점에서 모든 걸 만드는 게 목표다. 안의 컨텐츠들이 부분구현·미구현 등 부족한 부분이 상당하다. 이 관점에서 모든 걸 자동으로 검토하고, 디자인 안도 자동으로 내놓는다. **허접한 건 안 된다. 기존 톤앤매너와 그 이상이어야 한다.**" — 이 화면군 = 모델 카드 **검토·승인 데스크**. 대시보드 `승인 대기` EVIDENCE-PAIR(요청 지역 실크롭 + 빨강 글자 `검토 ›`)가 입구.

## 1. 원본 확인(소스 대조)
`landxi7/admin-publish.html`(인라인 스크립트 `REQUESTS` 6건 · 세부 5탭 · 발행 처리 · `batch-class-modal`) · `assets/js/ai-project-data.js`(`AI_PROJECTS` · `AI_PERMISSIONS` 13 · `AI_TRAIN_RESULTS` · `AI_LABELING_DATA` · `AI_PROJECT_MEMBERS` · `AI_MODEL_CARDS`) · `assets/js/page-ai-labeling-core.js`(일괄 변경 막대 · 모달) · `ai-card.html`(카드 8) · `ai-card-edit.html`(3단계) · `ai-publish-create.html` + `assets/js/page-ai-publish6.js`.
- 인벤토리와 다른 점(소스가 우선): ① `ai-card-edit.html` 은 **3열 단계**(1 프로젝트 선택 · 2 학습 결과 선택 · 3 정보 입력)이고 `모델 설명 화면` 은 이미지 업로드가 아니라 **경로 텍스트**(`예: /jn/aidetect/sea_trash_drone.html`). ② 목록 검색 = `요청자` + `요청 일시`(기간) + 초기화/검색, 상태는 칩 5. ③ 개요에는 `수정`(발행 정보 수정 폼)이 따로 있다. ④ 클래스 일괄 변경 모달은 **라벨링 모드의 라벨 탭**(체크 → `클래스 일괄 변경 (n건)`)에서 열린다.

## 2. 파리티 표 (원본 기능 → 아트보드 · 위치)
| 원본 | 아트보드 · 위치 |
|---|---|
| 상태 칩 전체/대기/검토중/승인/반려 · `?status=` | 전 판 H1 우측 **큰 숫자 탭 5**(대기 = 빨강 글자) · 딥링크 진입 = `List-Pending` 마스트헤드 `?status=대기` |
| 발행 요청 목록 카드(과제명 · 상태 · 프로젝트 · 학습 결과 · 요청자 · 일시) · 새로 고침 · 검색 토글 | `Opt3` 등 좌 큐 344(썸네일 + 3줄) · `List` 3×2 이미지 카드 · 머리 `↻` `검색` |
| 검색 패널(요청자 · 요청 일시 from~to · 초기화 · 검색) · 검색 닫기 | `List` · `List-Empty` 틴트 띠 |
| 빈 상태 `요청이 없습니다` | `List-Empty` |
| `?open=<id>` 세부 · 뒤로/닫기 · 탭 5 | `Opt3` 마스트헤드 `?open=pa-6` · 세부 머리 `×` · 탭 5(개수) |
| 개요: 반려 사유 · 발행 정보 11행 · 소개 · 개발 목적 · 클래스 · 썸네일 2종 | `Opt3`(증거 열 = 카드 썸네일 520×347 + 대시보드 썸네일 + 소개/목적, 우 열 = kv 11 + 클래스) · 반려 사유 = `Review-Rejected` 맨 위 |
| 수정(과제명* · 모델명* · 자동 4 · 소개 · 개발 목적 · 클래스 자동 · 썸네일 2* 이미지 선택 · 취소/저장) | `Review-Edit` |
| 구성원(읽기 전용 · 역할 · 라벨 n · 마지막 작업) | `Review-Members`(이름 비움 §5 · 라벨 기여 막대 1) |
| 라벨링 목록(데이터명 · 라벨링됨/미작업 · 라벨 n · 일시) → 라벨링 모드(사각형/원형/폴리곤/도형 복사 · 실행 취소 · 저장 · 닫기 · 줌 · GSD · 클래스/라벨 탭 · 전체 선택 · 클래스 일괄 변경 (n건)) | `Review-Labeling` |
| 클래스 일괄 변경 모달(클래스 라디오 · 취소 · 저장) | `Review-ClassModal` |
| 학습 결과: 학습 정보 6 · 학습 설정 9 · KPI 4 · 클래스별 성능 · 오분류 행렬 | `Review-Train` |
| 분석 결과 목록(완료 · 탐지 n · 일자) → 지도 탐지결과 오버레이 + 세부(기본/처리/정사 영상/분석 범위) | `Review-Analysis`(지도 552×564 · 청록 폴리곤 · 범례 · 지도 도구) |
| 발행 처리: 상태 변경* 라디오 4 · 권한 선택*(전체 선택 · 기관명 · 권한명 13) · 취소/확인 | `Review-Process` |
| 반려 사유*(500자 · 필수 오류 문구) | `Review-Reject` |
| `ai-card.html`: 검색어(전체/카드 이름/프로젝트) · 공개 여부 · 초기화/검색 · 카드 발행 · 카드 8(이름 · 공개/비공개 · 발행일) · 페이지네이터(15/30/90 · 총 n건 중 1~n행) | `Cards` |
| 빈 상태 `발행된 카드가 없습니다` + 안내문 | `Cards-Empty` |
| `ai-card-edit.html` 발행: 1 · 2(발행됨 비활성 · 안내문) · 3(모델 명 · 모델 유형 · 알고리즘 · 사용 여부(발행 모드 `사용` 고정) · 모델 설명 · 도커 이미지 명/태그 · 탐지 형태 · 타일링 크기 · 모델 설명 화면 · 글자 수) · 취소/발행 | `Card-Edit` |
| 수정 모드(`?cid=&mc=` · 1·2 잠금 `수정 시 변경할 수 없습니다.` · 저장) | `Card-Edit-Locked` |
| `ai-publish-create.html` 전역 진입: 선택 카드 3(내 프로젝트 · 학습 결과 · 분석 결과) · 과제 유형 라디오 · 고도화(과제명 select · 안내문 · `과제 정보 수정` 아코디언) · 분석 결과 다중 선택 모달 · 목록/취소/발행 요청 | `Request` — 프로젝트 스코프(신규 과제 · 학습 결과 선택)는 `B5-Project-Deploy` 와 동일하므로 **다른 점만** 그림 |

누락 0. 추가 위젯 0(상태 분포 막대 · 라벨 기여 막대 · 처리 단계 레일은 같은 값의 표시 방식).

## 3. 선택 1 · 2 · 3 (대표 = 발행 승인 목록 + 상세 검토)
| | 구조(축) | 동기 | 대가 |
|---|---|---|---|
| 선택 1 `Opt1` | **원장(표) + 우 드로어 480** | 6건을 한눈에 정렬·비교, B5 프로젝트 드로어와 같은 문법 | 증거(실크롭)가 400px 로 줄고 탭이 드로어 안에 갇혀 학습·분석 결과를 크게 못 본다 |
| 선택 2 `Opt2` | **상태 열 보드 4 + 선택 요약 띠** | 대기→검토중→승인/반려 흐름이 공간으로 읽히고 이미지가 1급 | 세부는 다른 화면으로 넘어가야 한다 · 열 길이가 달라 빈 칸이 생긴다 |
| **선택 3 `Opt3` 권장** | **분할 검토 데스크: 큐 344 · 증거 520 · 결정 328** | 검토자가 목록을 잃지 않고 증거(실크롭 + 청록 폴리곤)를 크게 보며 같은 자리에서 `발행 처리` — 대시보드 EVIDENCE-PAIR 의 연장 | 한 화면 밀도가 가장 높다(1440 미만에서는 큐를 접어야 함) |
나머지 판은 전부 선택 3 구조.

## 4. 조판 실측 (1440×900)
레일 72 · 마스트헤드 64 · H1 34(y 88, 파랑 4px 룰) · 상태 탭 104×66(숫자 Paperlogy 30) · 룰 y 158 · 본문 y 174–834 · 푸터 룰 850.
큐 x 128–472(행 100 = 썸네일 100×67 + 3줄) · 세로 룰 x 488 · 세부 x 504–1384(머리 26 · 탭 y 218–256) · 증거 열 x 504–1024 · 결정 열 x 1056–1384(kv 30 × 11 · 처리 단계 레일 y 738 · CTA 44 y 790).
글자 바닥 14 · 본문 15–16.5 · 표 숫자 Inter tabular.

## 5. 콘티 · 데이터
- `시연`: 원본 REQUESTS 6 · 학습 결과(IoU 0.83 · F1 0.88 · Recall/Precision 은 원본 식 F1−0.02 / F1+0.01) · 클래스별 성능(원본 식) · 권한 13 · 카드 8 · 모델 카드(v1–v5 · 도커 · 타일 1024).
- `실측`(`results.js`): 남원 농지 2,098 필지(경작지 1,291 · 비경작지 807 · 평균 신뢰도 0.45 · 2026-06-08) · 비닐하우스 1,674 필지(단동 1,469 · 다동 205 · 0.79 · 2026-06-06). 정사영상 위 폴리곤 **배치**는 시연(범례에 명기).
- 사람 이름 0: 요청자 · 구성원 · 발행자 = `—`. 오분류 행렬 = 원본이 `Math.random()` 이라 점선 결손 + 이유. 도로안전 카메라(이미지셋) = 실사 없음 → 점선 결손 타일.

## 6. Craft 재비평
1. 1차: Pretendard 미적용(@import 가 @font-face 뒤) → 전 글자 굵음 · 세부 머리 상태어 위치 추정 오차 · 폴리곤 채움 과다 → @import 선두 · flex 머리/탭 · 채움 .13.
2. 2차: Opt1 드로어가 상태 탭을 가림 → 탭을 표 폭 안으로 · 판 안 설계 주석 제거 · 빨강 테두리/막대 제거(빨강 = 글자만) · 발행 처리 표가 버튼과 겹침 → 10행/5행 + 스크롤 표기 · 분석 결과 우 열 넘침 → 행 25 · 기본 정보 1행 · 클래스 칩 넘침 → flex · 라벨링 캔버스 하단 검은 띠 → cover · 카드 편집 폼 하단 빈 띠 110 → 행 간격 92.
3. 3차: 빈 목록의 `필터 밖 요청` 띠는 기능 발명 → 같은 3×2 점선 골격으로 교체 · 처리 단계 레일의 빨강 채움 → 잉크 · 요청 모달을 중앙으로(선택 카드 3 이 보이게) · 모달 시드를 원본 `analysisResults()` 값으로.
잔여: 넘침·겹침·잘림 0 · 14px 미만 0 · 빈 띠 최대 ≈ 60(`Cards-Empty` 단계 띠 아래).

## 7. 자체 검수 grep (B6-Publish-*.dc.html 20판)
`border-radius|box-shadow|gradient|backdrop|text-shadow` = 0 · font-family = Paperlogy/Pretendard/Inter 만 · `font-size < 14` = 0 · `#006DF7` 채움 버튼 0(채움은 선택 행 2px 표시 · 막대 그래프뿐) · `#D1352B` = `color` 만(대기 · 승인 대기 · 검토 › · 필수 오류).

## 8. 유보
- 정사영상 위 결과 폴리곤은 손 배치(시연). 구현 시 `geo/results/namwon-farmland-2025.geojson` 을 MapLibre 로 올린다.
- 라벨링 모드의 `클래스` 탭 · 이미지셋(도로안전 카메라) 라벨링/카메라 탐지 점 스타일 · 도로파손 클러스터 스타일은 판 없음(같은 셸의 데이터 차이).
- `ai-card-view.html`(카드 열람)은 인벤토리 §8 범위 밖 — `Cards` 의 `열람 ›` 으로만 표시.
- 토스트(`발행 정보를 수정했습니다` · `카드를 승인(발행)했습니다` · `n건의 클래스를 변경했습니다`)는 판 없음.
