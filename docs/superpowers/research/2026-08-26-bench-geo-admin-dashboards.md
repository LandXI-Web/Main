# 지오·AI 플랫폼 관리자/현황 대시보드 벤치마킹 — 실측 조사

- 작성일: 2026-08-26 · 브랜치 `plan1-foundation` · 캡처 `shots/bench/dash/`(gitignore)
- 발주 지시(원문): **"제발 임의 구현보다는 잘 만든 거 벤치마킹해서 따왔으면 좋겠다."**
- 추가 지시(원문): **"대시보드는 축소하는데, 첫 화면이니 잘 보이게 하자."**
- 제약: 기능은 원본과 **1:1**(`docs/superpowers/specs/2026-08-26-map-dashboard-options.md` §1 의 A1–A11 / B1–B16),
  타이포·색은 흰 B안(`docs/superpowers/specs/2026-08-25-client-taste-profile.md` §4).
- 이 문서는 **조사서**다. 제품 코드는 한 줄도 건드리지 않았다.

> 상태: 작성 중 (캡처 진행 중). 최종본은 §1 대상별 해부 → §2 후보 3안 → §3 이식 권고 → §4 안티패턴 순.

## 0. 수집 방법과 접근 한계 (정직 기록)

캡처는 전부 이 저장소에서 **Playwright(Chromium, headed)** 로 직접 찍었다. 두 해상도(1440×900 / 1920×1080)를
쓰고, 쿠키 배너는 자동 제거했다. 각 캡처마다 `page.evaluate()` 로 **DOM 실측 JSON**(`shots/bench/dash/_measure-*.json`)
을 함께 남겼다 — 타입 스케일(사이즈/굵기/행간/자간/폰트패밀리 + 등장 횟수), 레일 폭, 콘텐츠 컬럼 x·폭,
그리드 `grid-template-columns`·`gap`, 행 높이 중앙값(→ 900px당 행 수), border-radius/box-shadow/border 사용 분포,
색 사용 빈도, 그리고 **첫 900px(above the fold)** 의 노드 수·최대 폰트·이미지 점유율.

| 접근 등급 | 의미 | 대상 |
|---|---|---|
| **L** (Live DOM) | 실제 제품 UI를 브라우저에서 열고 DOM까지 실측 | kepler.gl, Copernicus Browser(EO Browser 후속), ArcGIS Online 콘텐츠 브라우저, ArcGIS Dashboards(공개 인스턴스), NASA Earthdata Search, V-World 포털 |
| **D** (Docs/official screenshot) | 로그인 벽 → 공식 문서·매뉴얼의 제품 스크린샷을 원본 해상도로 재렌더해 캡처 | UP42 콘솔, Mapbox Studio/Account, Roboflow, Google Earth Engine 코드에디터, Foursquare Studio, Cesium ion, Vercel, Cloudflare |
| **M** (Marketing) | 마케팅 페이지의 DOM 목업/스크린샷 | Linear, Stripe, Planet, 토스 비즈니스, 네이버 클라우드 |

**중요한 사실 정정 하나**: 지시서의 *Sentinel Hub EO Browser* 는 **폐기(deprecated)** 됐다.
`apps.sentinel-hub.com/eo-browser/` 를 열면 "EO Browser has been deprecated. Please move your workflows to the new
and improved Planet Insights Platform." 모달만 뜬다(캡처 `eobrowser-1.jpg`). 공개 후속은 **Copernicus Browser**
(`browser.dataspace.copernicus.eu`)이고, 유료 후속은 Planet Insights Platform이다. 그래서 이 조사는
**Copernicus Browser를 EO Browser 자리에 놓고** 실측했다.

---

# §1. 대상별 해부 (실측)

각 항목은 **① 캡처 · ② 그리드 실측 · ③ 타입 스케일 실측 · ④ 밀도 · ⑤ 상태 표기 문법 · ⑥ 수치/KPI 조판 ·
⑦ 지도의 위치와 크기 · ⑧ 목록·큐 조판 · ⑨ 빈 상태/대기 상태 · ⑩ 색 규율 · ⑪ 첫 900px(첫인상) ·
⑫ 훌륭한 점 / 우리가 가져올 것** 순서다. 숫자는 별도 표기가 없으면 **1440×900 기준 실측 px**이다.

## 1.1 NASA Earthdata Search — `search.earthdata.nasa.gov` 〔L〕

**캡처** `earthdata-1.jpg` `earthdata-2.jpg` `earthdata-3.jpg` `earthdata-hd-4.jpg`

지오 도메인에서 **"좌측 필터 레일 + 가운데 결과 목록 + 우측 지도"** 3단을 가장 성숙하게 조판한 실물이다.
로그인 없이 전 기능이 열리고, 데이터가 진짜다(11,131개 컬렉션).

**② 그리드.** 좌 필터 레일 `0–327`(327px, 안쪽 패딩 16). 결과 목록 컬럼 `328–928`(**600px**).
지도 `928–1440`(**512px = 화면폭의 35.6%**, 높이 100%). 세로로는 상단 브랜드바 32px → 검색바 44px →
패싯 툴바 50px → 본문. 하단에 28px **프로비넌스 바**가 상주한다(`v26.1.9-2 · Search Time: 2.9s ·
NASA Official: Doug Newman · FOIA · NASA Privacy Policy`). 즉 화면의 위아래를 얇은 시스템 밴드가 물고,
가운데를 3단이 채운다. 컬럼 사이 거터는 **0**이고 대신 **1px 헤어라인**이 경계다 — 카드 사이 여백으로
구획하지 않는다.

**③ 타입.** 결과 제목 15px/600, 메타 라인 13px/400 회색, 설명 13px/400, 프로비넌스 라인 11px/400,
목록 헤더 `11,131 Matching Collections` 17px/600, 툴바 12px/400. **네 단계뿐**이고 굵기는 400·600 둘뿐이다.

**④ 밀도.** 결과 행 1개 높이 ≈ **135px**(제목 1–2줄 + 메타 1줄 + 설명 2줄 + 프로비넌스 1줄 + 썸네일 68×64).
900px 안에 약 **5.5행**. 좌 레일 아코디언 행은 40px → 900px에 약 **20행**. 즉 같은 화면에서
**"20행짜리 얇은 목록"과 "5.5행짜리 두꺼운 목록"을 동시에** 쓴다. 밀도를 한 값으로 통일하지 않는 것이 요령이다.

**⑤ 상태 표기.** 세 층으로 나뉜다.
(a) **무채 아웃라인 아이콘 줄** — 구름 사용 가능/불가, 지도 이미지 유무 등. 색을 쓰지 않는다.
(b) **알약 칩 1개** — `1 to 3 hours`(지연 시간). 회색 채움, 높이 20px, radius 10px, 12px/400.
(c) **밑줄 링크** — `GEOSS`(출처 계보). 칩을 남발하지 않고 **행당 컬러 칩은 0–1개**로 묶는다.

**⑥ 수치.** KPI 카드가 **없다.** 큰 수(11,131)는 목록 헤더의 한 줄로 존재하고, 행마다의 수
(`4,058 Granules`)는 메타 라인의 첫 토큰이다. **숫자를 위한 별도 박스를 만들지 않는다**는 것이 이 화면의 결론이다.

**⑦ 지도.** 우측 512px. 지도는 목록의 **결과가 아니라 짝**이다 — 목록 행에 마우스를 올리면 지도에 풋프린트가 뜬다.
지도가 화면을 지배하지 않고, 폭의 1/3만 갖는다. 우리 요구("대시보드는 축소하되 첫 화면이니 잘 보이게")와
직접 맞물리는 비율이다.

**⑧ 목록.** 행 = `제목 / 메타 / 설명 / 프로비넌스 / 썸네일 / (i)정보 + (+)담기` 6요소. 우측 하단에 액션 2개
(원형 `i`, 원형 `+`)가 **항상 보인다**(호버에서 나타나지 않는다). 우리 B13의 `검토` 버튼 자리와 같은 문법이다.

**⑨ 빈 상태.** 썸네일이 없으면 68×64 무채 박스에 9px `No image available`을 중앙 정렬한다. **자리를 비우지 않고,
없다고 말한다.** 우리 §5-10(결손은 무채 + 이유 한 줄)과 같은 원리를 실제로 구현한 사례다.

**⑩ 색.** 배경 흰색, 텍스트 `#2C2C2C`/`#5A5A5A`, 상단·하단 밴드 `#111`, **액센트는 빨강 하나**(검색 버튼)
+ 파랑(링크). 지도는 컬러지만 UI는 사실상 무채다.

**⑪ 첫 900px.** 이미지(지도 타일)가 첫 화면의 **약 36%**, 나머지는 목록. 최대 폰트 17px. 즉 **"큰 글자로 놀래키지
않고, 데이터 개수와 실제 목록으로 첫인상을 만든다."** 첫 화면에서 사용자가 얻는 정보량이 가장 큰 축에 든다.

**⑫ 훌륭한 점.** 밀도를 두 종류로 쓴 것, 컬러 칩을 행당 1개로 제한한 것, 없는 것을 말하는 썸네일,
하단 프로비넌스 밴드(버전·검색 소요시간·담당자 실명). **우리가 가져올 것**: 3단 그리드 비율(327 / 600 / 512),
행의 6요소 구성, 결손 썸네일 문구, 하단 프로비넌스 밴드.

## 1.2 Esri ArcGIS Online — 콘텐츠 브라우저 / 검색 〔L, DOM 실측〕

**캡처** `arcgis-gallery-1.jpg` `-2` `-3` `arcgis-gallery-hd-4.jpg` · 실측 `_measure-arcgis-gallery.json`

이번 조사에서 **우리 취향 프로필 §4와 가장 정확히 겹치는 실물**이다. 이유는 하나 —
**border-radius가 페이지 전체에 0개, box-shadow는 헤어라인 대용 2개뿐**이기 때문이다.

**② 그리드(DOM 실측).** 좌 필터 레일 `arcgis-item-browser-filters` **w=261**(내부 콘텐츠 237, 좌우 12씩).
레일 시작 x=24. 콘텐츠 컬럼 `arcgis-item-browser-content` **x=316, w=1104**. 따라서
**거터 = 316 − (24+261) = 31px**, 우측 마진 20px. 상단 검색 인풋은 `x=24, w=1416, h=48`로
**두 컬럼을 가로질러 폭 전체를 먹는다** — 검색이 이 화면의 주인공임을 조판으로 선언한다.

**③ 타입(DOM 실측).** 실제 사용된 조합은 **네 개뿐**이다.

| 크기/굵기/행간 | 등장 수 | 쓰임 |
|---|---|---|
| `12px / 400 / lh16` | **120회** | 메타·칩 라벨·`View details` |
| `14px / 400 / lh16` | **82회** | 본문·필터 항목·설명 |
| `16px / 400 / lh20~24` | 9회 | 탭(`Content`/`Groups`), 헤더 메뉴 |
| `20px / 400 / lh30` | 1회 | 페이지 제목 `Search` |

폰트는 Avenir Next 단일, **굵기는 400 하나뿐이다.** 위계를 굵기가 아니라 **크기와 색으로** 만든다
(`#141414` 528회 / `#595959` 25회 / `#6B6B6B` 8회). 우리 §4의 "SUIT Medium 단일 굵기" 규칙과 정확히 같은 전략이다.

**④ 밀도.** 행 높이 중앙값 **56px → 900px당 16.1행**. 단 이 56px는 필터 트리 행이고, **아이템 카드는 158–190px**
(제목 20px + 설명 1–2줄 + 메타/칩 라인 + 1px 구분선 + 소유자 스트립). 900px에 **약 4.7개**. Earthdata와 같은
"두 밀도 병용" 구조다.

**⑤ 상태 표기 — 이번 조사에서 본 최고의 칩 문법.** 아이템 카드 메타 라인은
`[타입 아이콘] Feature layer │ Item updated: Sep 23, 2025 │ ✓Authoritative │ ⓐLiving Atlas │ ⛨Subscriber`
순서다. 규칙이 뚜렷하다.
1. **칩은 사실(fact)만 담는다** — 상태가 아니라 *속성*이다. `Authoritative`(검증됨), `Retired item type`(폐기 유형),
   `Has replacement`(대체본 있음), `OAuth 2.0 Credentials`.
2. **칩마다 14px 아이콘 + 12px 라벨**, 높이 24px, 좌우 패딩 10, 알약형. 배경은 **파스텔 채움**(연녹/연청/연보라),
   테두리 없음, 그림자 없음.
3. **부정적 사실도 같은 무게로 쓴다** — `Retired item type`을 빨강 경고로 만들지 않는다. 경고색을 아끼는 것이
   신뢰를 만든다.
4. 카드당 칩은 **최대 4개**, 그 이상은 잘라낸다.

**⑥ 수치.** KPI 카드가 **없다.** 총계는 `1-60 of 8,147` 한 줄(12px)로 목록 상단 좌측에 붙고, 우측에는
정렬 드롭다운(`Relevance`)이 온다. 숫자를 위해 면적을 쓰지 않는다.

**⑦ 지도.** 이 화면에는 **지도가 없다.** 대신 각 행 우측에 **128×72 썸네일**로 지도를 축소해 둔다 —
"지도는 목록의 삽화"라는 결정. 지도 크기 0인 대안(§2-C)의 실물 근거다.

**⑧ 목록.** 카드 하나 = `제목(20px) / 설명(14px, 최대 2줄) / 메타+칩 라인 / ─1px─ / 소유자 스트립`.
소유자 스트립은 좌측에 24px 아바타 + 소유자명(14px), 우측에 `Open in Map Viewer` + `⋯`. **행의 액션을
행 안쪽 아래에 따로 띠로 분리**한 것이 특징 — 우리 B13의 `검토` 버튼을 여기에 두면 그대로 맞는다.

**⑨ 빈 상태.** 썸네일이 없는 아이템은 회색 톤 아이콘 플레이스홀더로 채우고, 검색 0건일 때는
"필터를 조정해 보라"는 문장 + 적용된 필터 칩을 나열한다(제거 가능). §2-D05(dim-not-delete)의 온건판.

**⑩ 색.** 배경 `#FFFFFF` + 섹션 구분 `#F7F7F7`. **radius 0 · shadow 0**(실측: `radii` 목록이 비었고,
shadow는 `rgb(224,224,224) 0 1px 0`과 `1px 0 0 inset` 둘뿐 = 전부 헤어라인 대용). 테두리 `1px #D4D4D4`.
액센트는 `#007AC2` 하나.

**⑪ 첫 900px.** 이미지 점유율 **0%**, 최대 폰트 **20px**, 텍스트 노드 94개. 첫인상이 **완전히 텍스트**다.
정보 밀도로 승부하는 극단. 우리 클라이언트 취향(§3-5 "관리자 SaaS 크롬이 첫인상인 화면" 거부)과는
정면으로 충돌하는 지점이므로 **이 화면의 조판은 가져오되 첫 밴드는 반드시 다르게 처리해야 한다**(§3에서 다룸).

**⑫ 훌륭한 점.** 라운드 0 · 그림자 0 · 단일 굵기 · 헤어라인 구획 · 칩은 사실만. **우리가 가져올 것**:
261 / 31 / 1104 그리드, 12·14·16·20 4단 타입, 칩 규격(h24 · 12px 라벨 · 파스텔 채움 · 카드당 ≤4),
행 하단 액션 스트립, 총계를 `1-60 of 8,147` 한 줄로 쓰는 어법.

## 1.3 kepler.gl — 사이드 패널 〔L, DOM 실측〕

**캡처** `keplergl-1.jpg` `keplergl-data-1.jpg` `-2` `-3` `keplergl-data-hd-4.jpg` · 실측 `_measure-keplergl-data.json`

**② 그리드.** `side-panel--container` **w=324**, 그 안의 `side-bar` **w=292**(상하 12씩 인셋 → h=876),
다시 `side-panel__content__inner` **w=260**. 즉 **324 → 292 → 260**의 3중 인셋이고, 실제 콘텐츠 폭은 260px다.
패널은 화면에 붙지 않고 **16px 떠 있다**. 지도는 `1440×900 = 100%`로 패널 **밑을 지나간다**(패널이 지도를 자르지 않는다).
하단 시간 위젯은 `x≈380, w≈1020, h≈242`, 우측 아이콘 레일은 `x=1396, w=32, h=420`.

**③ 타입.** `11px/500/ls0.3px`(버튼·라벨) · `11px/400/ls1.25px`(섹션 라벨 `Datasets`) ·
`20px/400/ls1.25px`(`Layers`) · `11px/400`(`54,936 rows`) · `14px/500`(`01/05/1991`) · 차트 축은 `10px/400`.
**본문 폰트가 11px**다. 지도 위 패널이므로 극단적으로 작게 간다.

**④ 밀도.** 행 높이 중앙값 **38px → 900px당 23.7행**. 레이어 카드는 62px.

**⑤ 상태 표기.** 색 점(dot)과 칩이 거의 없다. 대신 **레이어 카드 좌측 3px 컬러 바**(레이어 색)와
**눈 아이콘(표시/숨김)** 으로 상태를 말한다. 필터가 걸리면 하단 히스토그램의 **선택 구간만 시안으로 켜지고
나머지는 회색으로 남는다** — 삭제가 아니라 감쇠(우리 D05와 동일).

**⑥ 수치.** `54,936 rows` 11px 한 줄. **거대 숫자 없음.** 대신 시간 범위를 `01/05/1991 — 03/30/2011`
14px/500로 지도 한가운데 떠 있는 알약에 띄운다 — **지금 보고 있는 범위**를 화면 중앙에 말한다.

**⑦ 지도.** 100%. 패널은 지도 위에 뜬 유리다(`#242730`, 실측 `rgb(36,39,48)`). 이 값은 우리 취향 프로필
§2.1에 이미 "유일하게 허용되는 유리"로 적혀 있는 바로 그 색이다 — **출처가 kepler.gl임이 실측으로 확인됐다.**

**⑨ 빈 상태.** 데이터 0건일 때 `Datasets` 섹션은 사라지지 않고 `+ Add Data` 버튼만 남는다(캡처 `keplergl-1.jpg`).
**빈 상태 = 다음 행동 버튼 하나.**

**⑩ 색.** radius `2px`(11회) 지배, 4px·9px 소수. shadow `0 6px 12px rgba(0,0,0,.16)`(11회).
액센트는 초록 `#0F9668`(Add Layer) / 파랑 `#2E7CF6` / 시안 `#1FBAD6`. UI 무채는 `#6A7485`·`#A0A7B4`.

**⑪ 첫 900px.** 이미지(지도 캔버스) **100%**, 최대 폰트 42px(로고). 첫인상이 **전부 데이터 그림**이다.
우리 §5-1(첫 프레임에 실영상 60% 이상)을 만족시키는 유일한 방식의 실물.

**⑫ 우리가 가져올 것**: 324/292/260 3중 인셋, 11px 본문 + 20px 섹션 제목, 패널이 지도를 자르지 않고 떠 있는 것,
"지금 보는 범위"를 지도 중앙 알약으로 말하는 장치, 감쇠 필터.

## 1.4 Copernicus Browser (Sentinel Hub EO Browser 후속) 〔L, DOM 실측〕

**캡처** `copernicus-browser-1.jpg` `-2` `-3` `copernicus-browser-hd-4.jpg` · 실측 `_measure-copernicus-browser.json`

**② 그리드.** 좌 패널 **0–450**(450px, 화면의 31%), 지도 **450–1440**(990px, **69%**). 패널은 지도를 **자른다**
(kepler처럼 떠 있지 않다). 패널 상단 헤더 `0–120`: 로고 + `EN` + `Log in` + 접기 화살표, 그 아래 2탭
(`VISUALISE` / `SEARCH`)이 **각 50%씩 정확히 반**을 먹는다. 패널 하단 `0–850..900`에 기관 로고 3개
(EU · Copernicus · ESA) + `About` `Support` + **버전 `v1.51.0`** 이 상주한다.

**③ 타입.** 탭 16px/700, 본문 13–14px/400, 지도 저작권 11px/400, 버전 8px/400. Helvetica Neue / Arial 계열.

**⑤ 상태 표기.** 우측 도구 레일(`1390–1430`, 40px 정사각 버튼 9개)이 **모드 상태**를 담당한다 —
폴리곤/라인/핀/자/이미지다운로드/타임랩스/**3D**/통계. 활성 버튼은 파란 채움. 즉 **상태를 칩이 아니라
'지금 켜져 있는 도구'로** 말한다.

**⑦ 지도.** 990×900 = 69%. Leaflet. 지도 위에 뜨는 것은 좌상단 `Go to Place` 검색(240px)과 레이어 버튼뿐이다.

**⑨ 빈 상태.** 익명 진입 시 **모달로 선택을 강요한다**(`Log in` / `Anonymously`). 화면을 비워두지 않고
"둘 중 하나를 고르라"고 말한다. 우리 가입 승인 대기(B7)·문의(B8)의 0건 상태에 쓸 수 있는 어법이다.

**⑩ 색.** 배경 흰색 + `#F5F5F5`. 헤더 남색 `#0A4393`. 액센트 초록 `#77CC09`(활성 탭 텍스트).
radius는 3px·4px 각 1회 = 사실상 **각진 사각형**. shadow는 `0 0 6px rgba(0,0,0,.2)` 12회(지도 위 컨트롤 전용).

**⑪ 첫 900px.** 이미지 점유율 **76.7%**(지도 타일 + 좌측 위성 일러스트 449×1080). 최대 폰트 22px.
**첫인상 = 지도 + 얇은 패널.** Earthdata(36%)와 kepler(100%) 사이의 중간값.

**⑫ 우리가 가져올 것**: 450/990 = 31:69 비율, 2탭 50:50 헤더, **패널 하단 상주 기관/버전 밴드**
(우리 B15 푸터를 여기에 접을 수 있다), 우측 40px 도구 레일.

## 1.5 UP42 콘솔 〔D — `docs.up42.com` 공식 스크린샷 원본 해상도 재렌더〕

**캡처** `up42-1.jpg`(2048×1524 원본), `up42-5..7.jpg`(마케팅)

콘솔은 로그인 벽이라 공식 문서의 제품 스크린샷을 원본 해상도로 다시 그려 캡처했다.

**② 그리드.** 콘솔 프레임 안쪽 기준: 페이지 제목 `Account details` 아래 **1px 전체폭 룰**, 그 아래
좌 서브레일 **≈242px** + 콘텐츠 **≈874px**, 거터 **≈40px**. 즉 UP42는 **1차 내비게이션(글로벌)과
2차 내비게이션(설정 그룹)을 분리**하고, 2차 레일을 콘텐츠와 같은 카드 안에 넣는다.

**③ 타입.** 페이지 제목 32px/400. 서브레일 **그룹 라벨 13px/700 대문자 + 보라**(`ACCOUNT` `ACCESS` `USERS`
`WORKSPACE`), 그 아래 항목 15px/400 검정. 콘텐츠 필드 라벨 14px/700, 배너 제목 14px/700.

**⑤ 상태 표기.** 활성 메뉴 = **연보라 채움 + 좌측 3px 세로 바**. 플랜 상태는 **연청색 정보 배너**
(`ⓘ Professional plan / Full access to UP42 offering`) — 칩이 아니라 **한 줄 배너**다.
민감정보(계정 ID)는 **분홍 마스킹 바**로 가린다 — "값이 있지만 안 보여준다"를 시각화한 좋은 사례.

**⑥ 수치.** 이 페이지에 KPI가 없다. UP42 콘솔은 **숫자를 대시보드가 아니라 각 도메인 페이지에 둔다.**

**⑨ 빈 상태.** 회사명 블록은 **회색 스켈레톤 바 3줄**로 그려져 있다(문서용 마스킹이지만, 결과적으로
"내용은 있고 지금은 안 보인다"는 상태를 표현). 스피너 금지 원칙(§5-11)과 같은 방향.

**⑩ 색.** 흰 배경 + 보라 액센트(`#6C5CE7` 계열) 하나 + 정보 배너 연청. radius 4–6px, 그림자 없음, 테두리 1px `#D9D9D9`.

**⑪ 첫 900px.** 문서 스크린샷이라 판정 제외. 다만 마케팅 페이지(`up42-6.jpg`)의 4단
**세로 1px 헤어라인 구획 피처 행**(`Centralization / Standardization / Visibility / Governance`)은
카드 없이 4열을 나누는 정확한 실물이다 — 우리 B14(관리 바로가기 4타일)에 그대로 쓸 수 있다.

**⑫ 우리가 가져올 것**: 1차/2차 내비 분리, 그룹 라벨 13px/700 대문자, 활성 항목의 3px 좌측 바,
**정보는 칩이 아니라 한 줄 배너**, 4열 세로 헤어라인 구획.

## 1.6 Vercel — Observability / Dashboard 〔D〕

**캡처** `vercel-1..6.jpg` `vercel-obs-1..4.jpg` `vercel-dash-*.jpg`

**② 그리드(문서 셸, DOM 기준).** 좌 문서 레일 **0–283**, 본문 **312–1120**(808px), 우 목차 **1170–1400**(230px).
제품 스크린샷 안쪽 그리드는: 차트 카드 하나에 **좌 큰 차트(≈500) + 우 작은 차트(≈256)** 를 붙이고
바로 아래에 **표**를 잇는다. 카드와 표 사이에 여백을 두지 않고 **1px 선으로만** 나눈다.

**③ 타입.** 차트 제목 15px/500, 축 라벨 12px/400 회색, 표 헤더 13px/500 + 정렬 캐럿, 표 셀 13px/400,
KPI 값 15px/600. **Geist(그로테스크) 단일 계열**.

**⑤ 상태 표기 — 이 조사에서 가장 절제된 KPI 문법.** `Error ● 2.2%` / `Timeout ● 0%`.
라벨(13px 회색) → **색 점 6px** → 값(15px/600 검정). **박스가 없다.** 차트 카드의 헤더 우측에
두 개가 나란히 붙는다. 값 자체가 작고, 색은 점에만 있다.

**⑥ 수치 조판.** 큰 숫자를 쓰지 않는다. 대신 **표의 값 칸에 가로 막대를 셀 배경으로 깔았다**
(`210k` 뒤에 옅은 회색 바). 숫자 + 미니 막대의 결합 = 카드 없이 순위를 보여주는 방법.
**우리 B10(프로젝트 Top5 가로막대)을 카드가 아니라 표의 셀 배경으로 옮길 수 있다는 실물 증거다.**

**⑦ 지도.** 없음.

**⑧ 목록.** `Route │ Invocations │ P75 Duration` 3열 표. 헤더에 정렬 캐럿, 첫 열에 12px 모노 경로,
행 높이 44px → 900px당 20행.

**⑩ 색.** 흰 배경, 텍스트 `#000`/`#666`, 액센트 파랑 `#0070F3` + 상태색(빨강/앰버) **점에만**.
radius 8px, 헤어라인 `#EAEAEA`, 그림자 없음.

**⑪ 첫 900px.** 문서 셸이므로 제품 판정 제외. 다만 Vercel 대시보드 자체는 **상단에 프로젝트 카드 그리드**,
그 위에 한 줄 상태 밴드를 두는 구조로 알려져 있고, 문서 스크린샷도 그 문법을 따른다.

**⑫ 우리가 가져올 것**: `라벨 · 점 · 값` 인라인 KPI(박스 없음), 표 셀 배경 막대, 차트+표를 1px로만 잇기.

## 1.7 Esri ArcGIS Dashboards — 실운영 공개 인스턴스 〔L〕

**캡처** `arcgis-dash-wildfire-1.jpg` `-2` `-hd-3` · `arcgis-dash-oregon-*` · `arcgis-dash-hanover-*` ·
`arcgis-dash-outage-*` · (참고: 폐기 확인용 `arcgis-dashboard-jhu-1.jpg` `arcgis-dashboard-county-2.jpg`)

**먼저 사실 하나.** 지시서가 지목한 **JHU COVID-19 대시보드는 이미 사망했다.** 열면
`Sign in — Please sign in to ArcGIS Online.` 모달과 `⊗ Data source does not exist or is inaccessible.`
가 뜨고, 자매 대시보드(US by County)는 `Fatal errors — ABORTED`를 반환한다. 그래서 ArcGIS Online의
공개 검색 API로 **최근 120일 내 수정된 공개 대시보드 중 조회수 상위**를 다시 뽑아
살아 있는 실운영 인스턴스 4개를 캡처했다(산불 상황판 · 오리건 화재 · Hanover 카운티 상황인지 · 정전 센터).

**⑥ 수치 조판 — Esri Indicator 위젯의 문법.** 이것이 이 제품의 정체다.
- **꽉 찬 색 블록**(radius 0, 간격 8px)에 **거대 숫자를 중앙 정렬**한다. 실측: 값 **≈72px/300**(경량),
  라벨 **13px/400** 값 바로 아래 중앙.
- **블록 색이 곧 상태다.** 심각 = 크림슨 `#C8324F` 계열, 주의 = 앰버 `#E9AE43`, 중립/누계 = 회색 `#DDE1E6`
  (이때 숫자는 검정). 즉 **같은 위젯을 색만 바꿔 3단계 심각도**를 만든다.
- 블록 위에 **섹션 제목 15px/600**(`Preparedness Levels` / `DNR Fires in Washington` / `NW Teams Deployed` /
  `Northwest GACC 2026 Statewide Totals`)이 붙어 **KPI를 주제별로 묶는다.** KPI를 한 줄에 다 늘어놓지 않는다.
- 숫자 길이에 따라 폰트가 **자동 축소**된다(`483.45`는 `5`보다 작게 렌더). 그래서 블록 높이가 고정돼도 안 깨진다.

**② 그리드.** 데스크톱 인스턴스는 `좌 KPI/목록 컬럼(≈250) │ 중앙 지도(≈780) │ 우 차트 컬럼(≈405)` 3단에
하단 **탭 바**(`Admin0 / Admin1 / Admin2`, `28-Day / Totals / Incidence / ...`)를 붙인다. 위젯 사이 간격은
**8px 균일**, 위젯 자체는 radius 0.

**⑤ 상태 표기.** 칩이 없다. **위젯의 배경색 = 상태**다. 목록 위젯은 행마다 좌측 4px 컬러 바로 심각도를 표시한다.

**⑨ 빈/오류 상태.** 데이터 소스가 죽으면 위젯 자리에 `⊗ Data source does not exist or is inaccessible.`
**한 줄이 남는다**(위젯이 사라지지 않는다). 최상위 실패는 `Fatal errors / ABORTED` 배너.
**우리 §5-10(결손 선언)의 산업 표준형**.

**⑪ 첫 900px.** 색 블록이 화면의 60% 이상. **강렬하지만 우리 §4 위반**(면 채움 색, 액센트 3개).
그래서 §2/§3에서는 **Indicator의 "주제 제목 + 블록 그리드" 구조만 가져오고, 색 채움은 버린다.**

**⑫ 가져올 것**: KPI를 **주제 제목으로 묶는 것**, 숫자 자동 축소, 위젯 간격 8px 균일, 오류 위젯의 한 줄 선언.
**버릴 것**: 면 채움 색으로 상태를 말하기.

## 1.8 Linear 〔M〕

**캡처** `linear-1..8.jpg` `linear-hd-1..3.jpg` `linear-9..14.jpg`

**③ 타입.** 히어로 문장이 **한 문단 안에서 흑/회 2색으로 갈라진다** — `A new species of product tool.`(흰 100%)
+ 나머지 문장(회색 60%). **한 덩어리 안에서 강조를 색으로만** 한다. 크기 42px/500, 행간 1.15, 자간 -0.02em.
캡션은 **모노 대문자 `FIG 0.2` `FIG 0.3` `FIG 0.4`, 11px, 자간 0.08em, 회색 40%**.

**② 그리드.** 다이어그램 3열은 **세로 1px 헤어라인으로만 구획**되고 카드가 없다. 열 폭 480 균등, 헤어라인 색은
배경보다 8% 밝은 회색.

**⑤/⑥.** 상태·수치 문법은 마케팅 페이지에서 확인 불가(제품은 로그인 벽).

**⑩ 색.** 배경 `#08090A` 단색, 텍스트 흰/회 2단, 액센트 **없음**(버튼만 흰 채움). 그림자 없음.

**⑫ 가져올 것**: **`FIG n.n` 모노 캡션 문법**(우리 §4 "숫자·좌표는 Inter tabular"와 결합 가능),
한 문단 내 2색 강조, 카드 없이 세로 헤어라인으로만 나누는 열.
**주의**: Linear는 어두운 바탕이므로 우리 흰 B안에서는 명도를 뒤집어야 한다.

## 1.9 Google Earth Engine — 코드 에디터 / 애셋 매니저 〔D〕

**캡처** `gee-1..5.jpg`

**② 그리드.** 4분할이다. 좌측 **Scripts / Docs / Assets 3탭 패널**(≈22%), 중앙 코드(≈40%),
우측 **Inspector / Console / Tasks 3탭 패널**(≈24%), 그리고 **하단 전체폭 지도**(≈40% 높이).
즉 **"지도는 아래, 목록·상태는 위"** 라는 배치.

**⑤ 상태 표기.** 우측 패널의 **`Tasks` 탭이 곧 처리 대기 큐**다. 실행 중/완료/실패가 탭 안의 행으로만 존재하고,
**전역 KPI로 승격하지 않는다.** 우리 B6(카드 발행 승인 대기)·B13(대기 목록)의 관계를 어떻게 다룰지에 대한 반례:
GEE는 큐를 **한 곳**에만 두고 숫자를 두 번 말하지 않는다.

**⑥.** 거대 숫자 없음. 전부 12–13px 텍스트.

**⑩ 색.** 흰 배경 + 구글 파랑 `#4285F4` 헤더. 액센트 1개. 회색 헤어라인.

**⑫ 가져올 것**: **애셋/작업 목록을 탭 하나에 몰고 숫자를 중복 노출하지 않는 절제**,
지도를 하단 밴드로 두는 배치(우리가 지도를 "축소"해야 하므로 후보 중 하나).

## 1.10 Foursquare Studio 〔D〕

**캡처** `foursquare-studio-1..8.jpg`

**② 그리드.** 좌 문서 레일 **≈290**, 콘텐츠 **≈1100**. 콘텐츠 안은 **3열 카드 그리드**(카드 폭 254, gap 18).

**⑤ 상태 표기.** 카드 안에 **알약 링크 칩**(`What is Studio?` `Use Cases` `Accounts` `Single Sign-On` `Teams`
`Dashboard Guide`)을 **줄바꿈으로 흘려** 배치한다. 칩 높이 30px, radius 15px, 1px 회색 테두리, 채움 없음.
**"테두리만 있는 칩"** 은 Esri의 "채움만 있는 칩"과 대비된다 — 둘 중 하나만 골라야 한다.

**② 좌 레일.** 그룹(`FSQ Spatial` / `Studio (Legacy)` / `Studio User Manuals` / `Use Case & Guides` /
`Notebooks` / `Developer`)이 **접히는 아코디언**이고, 활성 그룹은 **연보라 채움 + 좌측 3px 바**(UP42와 동일 문법).

**⑫ 가져올 것**: 테두리 칩(우리는 Esri식 채움 칩과 택일), 아코디언 그룹 레일.

## 1.11 Mapbox — Studio / Account 문서 셸 〔D〕

**캡처** `mapbox-1..4.jpg` `mapbox-account-5..10.jpg` `mapbox-studio-1..4.jpg` `mapbox-studio-ui-1.jpg`

**② 그리드.** 좌 레일 **≈300**(`Console Tools` 제목 20px/600 + 그룹 라벨 13px/700 대문자 +
항목 15px/400), 본문 **360–1110**(750px), 우 목차 **1170–1400**.
본문은 **섹션마다 위에 1px 룰**을 긋고 제목 28px/400을 얹는다 — 카드가 없다.

**⑤.** 활성 항목: **연파랑 알약 채움 + 파랑 텍스트**(radius 999). Esri/UP42와 또 다른 세 번째 문법.

**⑩ 색.** 흰 배경, 텍스트 `#273D56`, 액센트 파랑 `#4264FB`, 코드 인라인은 연회색 채움 + 모노 13px.

**⑫ 가져올 것**: **섹션을 카드가 아니라 "1px 룰 + 제목"으로 나누는 것** — 우리 §4 라운드0·그림자0과 정확히 호환.

### 1.7.1 `State of Oregon Fires and Hotspots Dashboard` — 살아 있는 지오 상황판 〔L〕

**캡처** `arcgis-dash-oregon-1.jpg` `-2.jpg`

**이번 조사에서 우리 12위젯 구성과 가장 가까운 실물이다.** 구조가 정확히 `헤더 밴드 → 지도 + 우측 목록 → 하단 탭`이다.

**② 그리드(1440×900 실측).**

| 영역 | x | 폭 | 비율 | 비고 |
|---|---|---|---|---|
| 헤더 밴드 | 0–1440 | 1440 | 100% | h=**76**, 남색 `#0B2C5E` |
| ├ 제목 | 24–1190 | — | — | 로고 40px + 제목 26px/400 흰색 |
| └ **필터 상태 셀** | 1190–1432 | 242 | 17% | 1px 세로 구분선으로 잘라냄 |
| 지도 | 8–1015 | **1007** | **70%** | 높이 8–830 |
| 우측 패널 | 1015–1432 | **417** | **29%** | 지도와 8px 간격 |
| 하단 탭 바 | 0–1440 | 1440 | 100% | h=**48** |

**⑥ 수치 — 박스 없는 2 KPI.** 우측 패널 최상단에 아이콘(60px) + `66`(**52px/400 크림슨**) +
`2,020,757.63`(**34px/400 크림슨**)이 **한 줄에 나란히** 온다. 배경도 테두리도 없다. 그 아래에
`Active Fires`(30px)와 `Active Fires Acres Burned`(14px, 우측 정렬)가 **각 숫자의 라벨로** 붙는다.
**라벨을 숫자 아래가 아니라 다음 줄 좌·우 끝에 배치**해 두 숫자와 두 라벨이 교차 대응한다 — 대담하고 정확하다.

**⑧ 목록.** 행 = `32px 원형 아이콘 │ 이름 14px/500 대문자 │ 3줄 메타 13px/400`.
메타 3줄이 **정확히 `양 / 진행률 / 최종 갱신시각`** 이다 —
`578,637.00 Acres` / `93.00% contained` / `Last Updated: 8/26/26, 9:24 AM`.
행 높이 **≈93px → 900px당 9.7행**. 행 사이는 1px `#E5E5E5`.
**아이콘이 곧 상태다**: 불꽃 아이콘 = 활성, 보라 원 = 다른 분류. 칩을 쓰지 않는다.

**⑤ 필터 상태의 표기.** 헤더 우측 셀에 `Started within # of Days` / `All Dates` **두 줄**로
"지금 어떤 필터가 걸려 있는가"를 상주 표시한다. **우리 B2(기준일 · 기준일 현재)가 정확히 이 자리다.**

**⑦ 지도.** 70%. 다만 심볼이 과하다(중첩된 큰 불꽃 아이콘) — §4에서 안티패턴으로 다룬다.

**⑨ 프로비넌스.** 지도 하단에 어두운 11px 크레딧 스트립
(`Esri | TomTom | Garmin | FAO | NOAA | USGS | EPA | USFWS | CalOES, ... | Powered by Esri`)과
그 아래 `Fire Data Source` 링크. **출처를 화면에 상주시킨다.**

**⑪ 첫 900px.** 지도 이미지가 약 **62%**, 최대 폰트 52px(KPI 숫자), 목록 5행 노출.
**"큰 숫자 2개 + 지도 + 목록 5행"이 첫인상**이다. 이게 우리가 원하는 첫인상의 실물 표본이다.

### 1.7.2 `Hanover County Situational Awareness Dashboard` — 0과 결측을 그대로 보여주는 법 〔L〕

**캡처** `arcgis-dash-hanover-1.jpg` `-2.jpg` `-hd-3.jpg` `-hd-4.jpg`

**② 그리드(1440×900 실측).** 좌에서 우로
`빈 위젯 컬럼 140 │ EOC 상태 컬럼 142 │ 지도 840(58%) │ KPI 스택 136 │ 기상/도로 스택 142`,
위젯 사이 간격 **7–8px 균일**, radius **0**. 헤더 76px + 하단 탭 48px.

**⑨ 빈 상태 — 이 캡처의 핵심.** 좌측 컬럼 위젯 3개가 전부 **`ⓘ No data`** 한 줄이다(아이콘 16px + 14px 텍스트,
수직·수평 중앙). **위젯을 숨기지 않는다.** 레이아웃이 흔들리지 않고, 사용자는 "이 자리에 무엇이 올지"를 안다.

**⑥ 0의 표기 — 두 번째 핵심.** `Structure Fires 0`, `Hanover Road Closures 0`, `Electrical Hazards 0` 이
**전부 40–56px 큰 숫자로** 렌더된다. **0이라고 작게 쓰거나 회색으로 죽이지 않는다.**
"오늘 구조물 화재 0건"은 그 자체로 좋은 소식이고, 그것을 크게 말하는 것이 상황판의 태도다.
→ **우리 B6 `2건` · B7 `1건` · B13 `2건`이 0이 되는 날에도 같은 크기로 `0건`을 쓴다.**

**⑤ 상태 표기.** `EOC Status: CLOSED` — 라벨 + 값을 **두 줄로 쪼개 중앙 정렬**한 텍스트 위젯 하나.
색으로 말하지 않는다(CLOSED가 회색·빨강이 아니다). 위젯 안에 **미니 탭**(`Incidents ‹ ›`)과
**미니 페이저**(`‹ › 1 of 1`)가 있어, 한 칸에 여러 지표를 넘겨 볼 수 있게 한다.

**③ 타입.** 헤더 제목 26px/400, 위젯 라벨 15px/600, KPI 값 40–56px/300, 보조값 15–18px/400, 메타 13px/400.

**⑩ 색.** 어두운 셸(`#1A1A1A` 바탕 / `#2B2B2B` 위젯) + 흰 지도. **명암 교대가 칼로 그은 듯 일어난다**
— 우리 §2.1 "하드 엣지" 원칙의 실물. 액센트 색 없음(지도 심볼만 컬러).

**⑫ 가져올 것**: `ⓘ No data` 위젯(자리 유지), **0을 큰 숫자로**, 라벨/값 2줄 중앙정렬 상태 위젯,
위젯 간격 8px 균일 + radius 0, 위젯 안 미니 페이저.

### 1.7.3 `Outage Center Desktop V2` — 큐 + 지도 + 범례의 교과서 〔L〕

**캡처** `arcgis-dash-outage-1.jpg` `-2.jpg`

**② 그리드(1440×900 실측).** `좌 큐 컬럼 344(24%) │ 지도 830(58%) │ 우 범례 235(16%)`, 간격 8px.
좌 컬럼 최상단에 **요약 블록**(연청 채움) — `9 Active Outage(s)` **34px/500** +
`Affecting 200 customer(s) total` **22px/300**. 그 아래 바로 큐 목록이 이어진다.

**여기가 결정적이다.** 이 대시보드는 **KPI 카드를 만들지 않고, 큐의 제목이 곧 KPI다.**
`9 Active Outage(s)` 한 줄이 "총 9건"을 말하고, 그 아래 9개 행이 그 9건이다. **숫자와 목록이 분리되지 않는다.**
→ 우리 **B6(카드 발행 승인 대기 2건)과 B13(대기 목록 2건)은 지금 두 곳에서 같은 숫자를 말하고 있다.**
이 실물은 그것을 **하나로 합치라**고 말한다.

**⑧ 목록 행.** `32px 상태 아이콘 │ 지역명 14px/600 │ Est. Restore Time: 8/26/26, 11:30 AM │ Customers Affected: 10`.
행 높이 **≈76px → 900px당 11.8행**. 라벨은 굵게(600), 값은 보통(400) — **한 줄 안에서 라벨/값을 굵기로 구분**한다.

**⑤ 상태 = 아이콘, 그리고 범례가 그것을 설명한다.** 우측 235px 범례 패널이 **일급 위젯**이다:
`Legend` 제목(밑줄) → 그룹 제목 15px/600(`Outage Center Points`) → 심볼 24px + 라벨 14px
(`△ New Outage` 빨강 / `◉ Assigned` 파랑) → 다음 그룹(`Outage Affected Area` 노란 폴리곤,
`MP Service Territory` 파란 폴리곤). **색의 의미를 화면 안에서 정의한다.**
우리 §5-9("HUD의 모든 숫자가 해설인가")의 색 버전이다.

**⑨ 탭.** 좌 컬럼 하단에 `Outage Summary | Location Summary` 2탭 — **탭은 목록 컬럼에만 걸리고
지도·범례에는 걸리지 않는다.** 전역 탭이 아니라 **국소 탭**이다.

## 1.12 NASA Earthdata Search — 입도(granule) 뷰와 시간 스트립 〔L, 추가 실측〕

**캡처** `earthdata-2.jpg` `earthdata-3.jpg` · 실측 `_measure-earthdata.json`

**③ 타입(DOM 실측) — 두 서체를 역할로 나눈다.**

| 조합 | 등장 | 쓰임 |
|---|---|---|
| `13px/500 / ls0.132px` **Public Sans** | 37회 | 메타(`4,058 Granules`) — 최다 |
| `14px/400` **Inter** | 22회 | 필터 항목 |
| `12px/500 / ls0.16px` Public Sans | 14회 | 출처 태그 `GEOSS` |
| `14px/700 / ls−0.5px` **Inter** | 7회 | 결과 제목 |
| `16px/500 / ls−0.5px` **Inter** | 3회 | 총계 `11,131 Matching Collections` |
| `12px/700` Public Sans | 4회 | 툴바 `Sort: Usage` |
| `10px/700 / ls0.132px` Public Sans | 2회 | **칩 `1 to 3 hours`** |
| `11px/400` Public Sans | 7회 | 하단 버전 `v26.1.9-2` |

즉 **제목·총계는 Inter(자간 −0.5px로 조임), 메타·칩·툴바는 Public Sans(자간 +0.13~0.16px로 벌림)**.
**우리 §4가 이미 "표시 SUIT / 본문 Pretendard / 숫자 Inter"로 3분할을 정해 두었으므로, 이 역할 분담 방식은
그대로 검증된 것이다.** 칩 라벨이 **10px/700**이라는 점이 특히 유효하다 — 작고 굵게.

**② 그리드(DOM 실측).** `sidebar` **328px**, 배경 `#F6F6F6`, 우측 경계 `1px #D1D1D1`.
**지도는 `1440×828`로 화면 전체를 깔고**(캔버스 100%W/92%H), 좌 필터 레일과 결과 패널이 **그 위에 뜬다.**
즉 kepler와 같은 "지도가 바닥, 패널이 부유" 구조인데, 패널이 불투명 흰색이라 3단처럼 **보인다.**
→ **"지도를 줄이되 지도가 화면의 바닥이게 하는" 방법**의 실물. 우리가 찾던 절충안이다.

**⑤ 키/값 스트립 — 입도 카드의 상태 문법.** 각 granule 카드 안에
`START` (연녹 배경 + 녹색 라벨) `2026-08-25 23:45:00` 우측 정렬 / `END` (연분홍 배경 + 붉은 라벨)
`2026-08-25 23:50:00` 우측 정렬. **행 전체에 옅은 색을 깔고 라벨만 진한 색**으로 쓴다. 칩보다 넓고 배너보다 좁은
제3의 형식이며, **시작/끝처럼 짝을 이루는 값**에 최적이다.
→ 우리 B13 행의 `제출 2026.06.10 14:30` 같은 값에 쓸 수 있다.

**⑧ 하단 시간 스트립.** 화면 맨 아래 어두운 밴드(h≈60)에 `YEAR ▲▼` 스케일 셀렉터 + 데이터셋 이름 +
`2018 … 2027` 연도 축 + **데이터가 있는 구간만 초록 틱**. 우리 조사가 이미 `D01 원-바 시간 밀도 스캔 스트립`
으로 정의해 둔 장치가 **실제로 이렇게 생겼다**는 증거다.

**⑩ 색(DOM 실측).** `#2E2E32`(318회) / `#77777A`(141) / `#959599`(77) 3단 회색.
배경 흰 + `#F6F6F6`. 액센트는 **빨강 `#F64137`(검색 버튼)** 하나 + 포커스 링 `#1C67E3`.
radius `2px`(7회) 지배 + 칩만 `16px`. shadow는 `0 0 4px rgba(0,0,0,.176)` 7회(지도 위 컨트롤 전용).

**⑪ 첫 900px.** 최대 폰트 **16px**. 텍스트 노드 86개, 노드 714개. 지도 캔버스가 바닥 100%.
**첫인상 = "지도 위에 얹힌 두 장의 종이"**.

## 1.13 V-World 포털 〔L, DOM 실측〕

**캡처** `vworld-portal-1.jpg` `-2` `-3` · `vworld-openapi-*` · 실측 `_measure-vworld-portal.json`

국내 지오 포털의 첫 화면 표준이다. **첫인상 설계만 놓고 보면 이번 조사에서 가장 공격적이다.**

**② 그리드.** `.inner` 컨테이너 **x=80, w=1280**(좌우 마진 80, 내부 패딩 36) — 즉 **콘텐츠 폭 1208**.
히어로 `article.content.visual` **1440×960 = 첫 화면의 100%**(야간 위성 사진).
중앙 통합검색 인풋 **480×64**. 그 아래 바로가기 타일 6개: **폭 160, 피치 184(간격 24), 높이 155**,
x=180부터 → 전체 1080폭이 중앙 정렬. 그 아래 **KPI 밴드 x≈336, w≈768, h≈110**, 3등분(각 256).

**⑥ 수치 — 국내식 KPI 밴드.** `방문자 / 다운로드 / API호출` 라벨 16px/600 흰색을 위에 두고,
아래 줄에 **`오늘`(14px) + `65,213`(≈34px/600) + `명`(14px)** 순으로 **접두 라벨 · 큰 수 · 단위 접미사**를
한 줄에 배열한다. `17,116,512건`처럼 자릿수가 커도 같은 줄에 들어간다.
**우리 B4–B8(21명 / 8건 / 2건 / 1건 / 6건)이 이 문법과 정확히 같은 모양**이다 — 한국어 단위 접미사(명/건)를
숫자보다 작게 쓰는 것이 관례임이 확인됐다.

**③ 타입.** S-Core Dream(본고딕 계열) 300/400/500/600. H1 **54px/600 ls−1px**,
섹션 제목 34px/600 ls−1px, 카드 제목 20px/600, 본문 16px/400 lh25, 라벨 14px/500, 각주 12px/300 ls1px.
**한글에서 ls −1px(약 −0.03em)** 을 큰 제목에 거는 것이 국내 관례 — 우리 §4의 `±0.01em`보다 훨씬 조인다.

**⑩ 색.** 액센트 인디고 `#4A63E1` + 보조 오렌지 `#FF6600`. radius `4px`(39회) · `8px`(18) · `100px`(6).
shadow `0 2px 4px rgba(0,0,0,.1)`(11회). **즉 라운드와 그림자를 쓴다** — 우리 §4(라운드0·그림자0)와 충돌.

**⑪ 첫 900px.** 이미지 **100%**, 최대 폰트 **54px**, 텍스트 노드 49개.
**"큰 사진 + 큰 제목 + 검색 + 바로가기 6 + KPI 3"** — 정보량은 적지만 첫인상은 가장 강하다.
우리 발주자의 *"첫 화면이니 잘 보이게"* 요구에 국내 맥락에서 가장 가까운 참조점이다.

**⑫ 가져올 것**: KPI 밴드의 `라벨 / 접두 · 큰 수 · 단위` 3분할 조판, 바로가기 타일 6열의 피치(160+24),
한글 큰 제목 자간 −1px. **버릴 것**: 유리 타일 6개 나열(§3-1 위반), radius/shadow.

## 1.14 Linear 〔M, DOM 실측 — 마케팅 페이지의 앱 목업이 실제 DOM이라 측정 가능〕

**캡처** `linear-1..14.jpg` `linear-hd-*.jpg` · 실측 `_measure-linear-mktg.json`

**② 그리드(DOM 실측).** 앱 목업의 최상위 그리드가 **`grid-template-columns: 232px 1072px`**,
그 안쪽이 다시 **`750px 320px`**(x=301, w=1070). 즉 Linear의 3단은
**레일 232 │ 목록 750 │ 상세 320 = 1302**다. 1440에서 좌우 여백 60씩.
**우리 대시보드의 `레일 / 원장 / 보조` 3단에 그대로 대입 가능한 유일한 실측치**다.

**③ 타입(DOM 실측) — 자간이 크기에 비례한다.**

| 크기 | 굵기 | 자간 | 환산 | 쓰임 |
|---|---|---|---|---|
| 48px | **510** | −1.056px | **−0.022em** | 히어로 헤드라인 |
| 24px | 400 | −0.288px | −0.012em | 섹션 리드 |
| 15px | 400 / 510 / 590 / 300 | −0.165px | −0.011em | 본문·강조·이름·인용 |
| 14px | 400 / 510 | −0.182px | −0.013em | 목록 제목 · `Initiatives` |
| 13px | 400 / 510 | −0.13px | −0.010em | 목록 본문 · 내비 |
| 12px | 400 / 510 / 590 | −0.12px / 0 | −0.010em | 메타 · 라벨 |
| 10px | 400 / 510 | −0.15px / 0 | −0.015em | **이슈 ID `ENG-2085` · 칩 `Bug`** |
| 12–14px | 400 | 0 | — | **Berkeley Mono** — 코드 · `FIG 0.2` 캡션 |

**굵기는 300 / 400 / 510 / 590 네 개뿐이고 700이 없다.** 자간은 전 크기에서 대략 **−0.011em 고정**.
→ 우리 §4의 `letter-spacing ±0.01em` 규칙이 **Linear에서 실측으로 재확인**됐다.
다만 Linear는 라틴 전용이므로, 한글에서는 −0.011em이 과할 수 있다(V-World는 큰 제목에만 −1px를 건다).

**⑤ 상태 표기.** 칩은 `10px/510` 라벨 + **파스텔 4색 팔레트**
(`#F79CE0` 분홍 · `#F7BF8B` 주황 · `#FFDF9F` 노랑 · `#83DCDC` 청록)만 쓴다. **채도 낮은 4색 고정**이
Linear 특유의 절제다. 식별자는 `10px/400` 무채(`ENG-2085`).

**⑩ 색·표면.** 바탕 `#08090A`. **표면(surface)이 불투명 색이 아니라 `rgba(255,255,255,.05)`**(83회) —
즉 **바탕 위에 5% 흰색을 얹어 층을 만든다.** 테두리도 `1px rgba(255,255,255,.05)`(49회).
그림자는 `0 1.2px 0 rgba(0,0,0,.03)`(31회) — **1.2px 헤어라인 그림자**.
radius는 `2px`(42) / `6px`(40) / `4px`(27) / `9999px`(18) / `50%`(38, 아바타).

**⑪ 첫 900px.** 최대 폰트 **64px**, 이미지(제품 스크린샷)는 **y=527부터** 시작해 1416×768.
즉 **위 500px은 글자, 아래 400px은 제품 화면의 윗부분**. "헤드라인 → 제품이 반쯤 올라온다"는
가장 흔하고 가장 잘 작동하는 첫인상 공식이다.

**⑫ 가져올 것**: `232 / 750 / 320` 3단, −0.011em 고정 자간, 굵기 4단(700 금지),
칩 `10px/510` + 파스텔 4색, 표면 = 바탕 + 5% 오버레이(우리 흰 바탕에서는 검정 3–5%).

## 1.15 Stripe — Dashboard 문서 셸(Sail 디자인 시스템) 〔D/M, DOM 실측〕

**캡처** `stripe-1..7.jpg` `stripe-docs-90.jpg` `stripe-mktg-*.jpg` · 실측 `_measure-stripe-docs.json`

대시보드 본체는 로그인 벽이라 열지 못했다. **정직하게 적는다: Stripe 대시보드 화면 자체는 캡처하지 못했고,
같은 디자인 시스템으로 만든 공식 문서 셸을 실측했다.** 문서 본문이 대시보드의 구조를 서술한다
(`Primary navigation / Home / Balances / Transactions / Customers / Product catalog / Shortcuts`).

**② 그리드(DOM 실측).** `SidebarContainer` **280px**(내부 252, 우측 경계 `1px #D4DEE9`),
콘텐츠 `x=280 w=1160`, 아티클 컨테이너 패딩 **32px 48px**, 본문 **x=328 w=799**, 우 목차 250.
**본문 측정폭 799px** — 긴 한글 문단에도 알맞은 값이다.

**③ 타입.** 16px/500(88회, 내비) · 16px/400 lh26(64회, 본문) · 14px/400(69회) · 21px/700(h2) ·
32px/700(h1) · 12px/600(보조 액션). **본문이 16px**로 이번 조사 중 가장 크다.

**⑩ 색.** `#414552`(321회) 본문, `#5469D4`(235회) **Stripe 인디고 = 링크·강조 전용**, `#1A1F36` 제목,
`#3C4257` 보조. 배경 흰 + `#F7FAFC` + `#F4F7FA`. **radius `4px`가 106회 — 4px 단일 시스템**.
그림자는 대부분 투명 자리표시자이고 실제로는 **`inset 0 -1px 0 #E3E8EE`(밑줄형)** 만 쓴다.

**⑫ 가져올 것**: 4px 단일 radius, 본문 16px/lh26, 측정폭 799, 링크색 하나로 통일,
그림자 대신 `inset 0 -1px 0`.

## 1.16 Roboflow 〔M, DOM 실측〕

**캡처** `roboflow-1..6.jpg` `roboflow-docs-7..9.jpg` `roboflow-app-10..13.jpg` · 실측 `_measure-roboflow-mktg.json`

**② 그리드 — 이번 조사에서 유일하게 12열 그리드가 실측으로 잡힌 곳.**
`x=95, w=1250, grid-template-columns: 60.16px ×12, gap 48px` (= 12×60.16 + 11×48 = 1250 ✓)
그리고 같은 컨테이너에 `82.17px ×12, gap 24px` (= 986 + 264 = 1250 ✓) 변형이 공존한다.
→ **콘텐츠 폭 1250, 12열, 거터 24 또는 48.** 1440 화면에서 좌우 마진 95로 쓰는 값이다.

**③ 타입.** Inter 14px/400(76회) · **16px/300**(52회, 얇은 대형 라벨) · 16px/500 · 20px/500 ·
그리고 **Ubuntu Mono 14px/400**(47회, 설치 명령 `pip install inference && inference server start`).
**모노를 "제품이 진짜임을 증명하는 물증"으로** 쓴다.

**⑤/⑧ 액자-not-카드.** 산업별 사례를 **탐지 박스가 그려진 실제 프레임 썸네일 필름스트립**으로 흘린다.
썸네일 안 좌상단에 `TIME: 08:12 / GLOVES: YES / PIPETTE: CLEANED / VIALS: 03` 같은 **오버레이 표**,
하단에 `Healthcare & Medicine` 캡션. **면적의 대부분이 실제 픽셀**이고 UI는 그 위 얇은 선이다.
→ 우리 §4 "액자 + 캡션, 면적의 70% 이상이 실제 픽셀"의 산업 실물.

**⑩ 색.** 액센트 보라 `#6D28D9`(260회). 본문 `#4B5563`. radius `10px`(104회).
**테두리를 `inset 0 0 0 1px #E5E7EB` 그림자로 구현**(77회) — 레이아웃을 밀지 않는 테두리 기법.

**⑪ 첫 900px.** 이미지 43%(탐지 데모 영상), 최대 폰트 42px.

**⑫ 가져올 것**: 12열 1250 / gap 24·48, 모노를 물증으로 쓰기, 액자 필름스트립,
`inset 0 0 0 1px`로 테두리 구현.

## 1.17 Planet · Felt · Cesium ion · Cloudflare 〔M/D, 요약〕

- **Planet** (`planet-1..6.jpg`, `planet-docs-*`, `planet-explorer-*`, 실측 `_measure-planet-mktg.json`):
  컨테이너 **x=176 w=1088**, 3열 `341.33 ×3 gap 32`, 2열 `631 / 421 gap 36`, 히어로 3분할 `538.7 / 400 / 501.3`.
  타입 Gotham SSm: **12px/500이 52회로 최다**(라벨), 30px/700 헤드라인, 14px/700 대문자 eyebrow, 22px/400 인용.
  액센트 **시안 `#04CBE4` 하나**. 그림자 **0개**. 첫 900px 이미지 173%(위성사진 풀블리드).
  **상태 표기의 정수**: 위성사진 위에 **실선 시안 = 실제 촬영 풋프린트 / 파선 시안 = 요청 AOI**.
  색 하나에 **선 스타일 두 가지**로 "확보 / 요청"을 구분한다 — 우리 §5-10(측정 / 추정 / 미확정) 3단 태깅의 2단 축약형.
- **Felt** (`felt-1..4.jpg`): 올리브 단색 바탕 + 세리프 대형 헤드라인(60px) + 지도 실루엣 워터마크.
  제품 화면은 로그인 벽. **"GIS 제품인데 첫 화면에 지도 UI를 안 보여주고 지도 '모양'만 보여주는" 선택**이 특징.
- **Cesium ion** (`cesium-ion-1..6.jpg`): 문서 좌 레일이 **3단 들여쓰기**(그룹 → 항목 → 하위 항목, 15px/400)로 깊다.
  본문은 중앙 정렬 마케팅 블록 + 3열 카드(썸네일 위로 흰 카드가 겹쳐 올라오는 조판).
  액센트 **연두 `#D8DF4F` 버튼** — 우리 기준으로는 액센트 규율 위반 사례.
- **Cloudflare** (`cloudflare-1..7.jpg`): 주황 그라디언트 배너 + 일러스트 중심. 제품 스크린샷이 아니라
  **일러스트로 대시보드를 암시**한다. **가져올 것 없음** (§4 안티패턴에서 다룬다).

## 1.18 네이버 클라우드 · 토스 비즈니스/페이먼츠 〔M, DOM 실측 — 국내 관례〕

- **네이버 클라우드** (`ncloud-1..4.jpg`, 실측 `_measure-ncloud-mktg.json`):
  **Pretendard Variable** 단일 패밀리. 16px/600(56회) · 15px/600(45) · 24px/700(24) · 18px/700(20) ·
  14px/400(20) · 12px/400(9) · **12px/600(배지)**. 행 높이 중앙값 60 → 900px당 15행.
  **상태 배지**: `New`(초록 `#00D25A` 채움) / `Update`(초록 아웃라인) — **알약, 12px/600, 제목 옆에 붙음**.
  2열 아이콘 리스트(아이콘 28 + 제목 18px/700 + 2줄 설명 14px/400)가 **우리 B14 관리 바로가기 4타일과 같은 형식**.
  radius **20px(33회)·22px(16회)** — 국내 SaaS의 큰 라운드 관례. 그림자 `0 1px 4px rgba(0,0,0,.07)`.
  텍스트 `#222` / `#9E9E9E` / `#707070`.
- **토스 비즈니스·페이먼츠** (`toss-business-1..5.jpg`, `tosspayments-1..5.jpg`, 실측 `_measure-toss-biz.json`):
  **Toss Product Sans** 자체 서체, 굵기별 별도 파일(Regular / Medium / Semibold / Bold).
  48px/Bold 히어로 · 32px/700 · 24px/700 · 24px/600 · 20px/500 · 17px/400 lh27 · 15px/400.
  **그림자 0개**, radius **24px(19회)·100px(15회)**. 액센트 파랑 `#3182F6`.
  첫 900px 이미지 160%(인물 사진 풀블리드). 행 높이 38 → 900px당 23.7행.
  **관찰**: 토스는 **대시보드를 로그인 뒤로 완전히 숨기고**, 공개 화면은 사진 + 큰 문장 + 24px 라운드 섹션 카드만 쓴다.
  국내 B2B에서 "첫 화면 = 감성, 대시보드 = 로그인 후"라는 분리가 표준임을 보여준다.
  **우리는 그 분리를 할 수 없다** — 대시보드가 곧 첫 화면이다. 그래서 토스식 히어로는 참조하되 그대로 못 쓴다.

## 1.19 대조군 — 원본 `landxi7/dashboard.html` 실측 〔L〕

**캡처** `ZZ-original-landxi7-1.jpg` `-2` `-3` (Playwright 1440×900, ko-KR)

이식 대상을 정확히 재기 위해 **원본을 같은 조건으로 다시 실측**했다.

| 요소 | 실측 |
|---|---|
| 좌 레일 | **x 0–76 (76px)** · 아이콘 24px + 라벨 11px 2줄 · 항목 피치 ≈70px · 활성 항목 연청 채움 + 파란 아이콘 |
| 콘텐츠 | **x 108–1408 (1300px)** · 레일과의 거터 32 |
| B1 제목 / 부제 | 28px/700 `LX 관리자 대시보드` + 15px/400 부제 |
| B2 기준일 | 22px/700 파랑 우측 정렬 `2026.08.26` + 13px/400 `기준일 현재` |
| B3 공지 스트립 | 전체폭 1300 × h **52** · 남색 채움 · `공지` 아웃라인 칩(12px) + 제목 16px/600 흰색 + 날짜 13px + `전체 보기 ›` |
| B4–B8 KPI | 카드 5개 · **폭 247, 피치 263(간격 16), 높이 134** · 라벨 14px/500 · 수 **32px/700** + 단위 14px · 부제 12px/400 · **B6만 연분홍 채움 + 빨간 수** · 카드 우하단에 흐린 아이콘 워터마크 |
| B9 백본 스트립 | 전체폭 × h **56** · 아이콘 + 13px 라벨 + 17px/700 `XI-VFM v2.1` + 우측 `최종 적용 2026.03.12 │ 연결된 분석 과제 14개` |
| B10–B12 | **3열 · 폭 420 / 420 / 422 · 간격 18 · 높이 345** · 카드 헤더 62(아이콘 + 16px/700 제목 + 버튼) |
| B13 | 전체폭 카드 · 헤더 62 + 행 · 행 우측 `검토` 버튼 |
| radius / 테두리 | **≈10px** · `1px #E5E9F0` · 그림자 거의 없음 |
| 액센트 | 파랑 `#1D4ED8` 계열 + 위험 빨강(B6) |
| 첫 900px | 이미지 **0%** · 최대 폰트 32px(KPI 수) · B1~B13 첫 행까지 노출 |

**진단.** 원본은 이미 "균일 카드 그리드"다(취향 프로필 §3-1의 명시 거부 대상).
첫 900px에 사진이 0%이고 가장 큰 글자가 32px다. 즉 **첫인상이 없다.**
발주자의 *"첫 화면이니 잘 보이게"* 는 바로 이 진단에 대한 요구다.
동시에 원본은 **기능 배치가 이미 합리적**이다 — 공지 → KPI 5 → 백본 → 차트 3 → 큐 → 바로가기의 순서는
Esri 상황판의 `요약 → 지표 → 큐` 순서와 사실상 같다. **바꿔야 할 것은 순서가 아니라 조판·타이포·첫 밴드다.**
