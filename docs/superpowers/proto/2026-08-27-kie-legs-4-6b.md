# v3 Leg 4·5·6·6b — 영상 재생성 + 스크럽 마운트 기록 (kling/v2-1-pro, 2026-08-27, 배치 4-6b)

- 클라이언트 결정(원문): **"D1 그대로, D2 go"** — A05 는 v3d 그대로(도로 포함), 앵커 체인은 13장(A06→A06b→A07). 생성 대상 Leg 4(A04→A05) · Leg 5(A05→A06, 구름 그림자 문장 제거된 프롬프트) · Leg 6(A06→A06b, 신규) · Leg 6b(A06b→A07, 신규).
- 앵커/프롬프트: `tools/kie/anchors-v3.json` `motion_prompt` **토씨 그대로**, `negative` 는 `trimNeg()` 500자 규칙(앵커 고유 항목 전부 + base 앞부분, 생략만). 보낸 문자열은 원장 `negative` 필드에 그대로.
- 러너: `tools/kie/legs-v3.mjs` — 13앵커 체인 지원(`tail` 로 다음 앵커, leg 키 `6b` → `v3-leg-06b`), `LEGS_V3_PREV=rev1` 로 이전 4–6 산출물 백업 접미사 지정. 실행: `LEGS_V3_REV=3 LEGS_V3_BATCH=4-6b LEGS_V3_PREV=rev1 LEGS_V3_CAP=220 node tools/kie/legs-v3.mjs run 4 5 6 6b`.
- 원장: `shots/kie/legs-v3-credits.json` (`batch:"4-6b"` 4건) · 로그 `shots/kie/legs-4-6b-run.log` · 시트 `shots/kie/v3-legs-4-6b-sheet.jpg` (4행 × t=0,1,2,3,4,4.85 + 다음 앵커) · 씸 `shots/kie/v3-legs-4-6b-seams.jpg` · 세부 `shots/kie/v3-legs-4-6b-detail.jpg` (leg 4 t1.5/2.5 · leg 5 t1.5/2.5 · leg 6b t0.5/1.5/2.5/3.5). 시트·씸 사본은 `landxi/assets/proto/review/`.
- 규칙 준수: leg 당 과금 호출 **정확히 1회**, 자동 재시도 0, 실패 0, 무과금 오류 0, 추가 스틸 0, 캡 220 중 **200 사용**.

## 결론 (한 줄)

**넷 다 씸이 붙고(0.14–0.20 %), 지난 라운드의 세 결함(온실 유령 · 크레이트 · 지형 모핑)은 사라졌다.** Leg 6(수직 상승)은 이번 라운드에서 처음으로 "카메라가 움직인다"로 읽히는 이동 레그다. 남은 결함은 둘 — Leg 4 에서 A05 의 **도로가 t≈1.3–2.5 에 능선 너머에서 나타나는 것**(D1 로 수용한 앵커의 대가), Leg 6b 의 **t≈2–3 헤이즈 디졸브**(남원 곡면 → 여수 해안을 한 클립에 시키자 중간 1초를 안개로 메웠다). 둘 다 스크럽에서는 견딜 수 있고, 판·카피·테스트는 그대로 돈다.

## 크레딧

| 시점 | 잔액 | 증감 |
|---|---|---|
| 세션 시작 실측 | 9,946.5 | — |
| leg 4 `7d082b1e…` | 9,896.5 | −50 |
| leg 5 `ccf71401…` | 9,846.5 | −50 |
| leg 6 `aa8dbd9f…` | 9,796.5 | −50 |
| leg 6b `4b03a493…` | **9,746.5** | −50 |

`creditsConsumed`(50) 와 잔액 차이(50)가 네 건 모두 일치. createTask 거부/무과금 오류 0건. (원장의 `spent` 필드는 전 배치 누계 650; 캡 판정은 배치별 `spentOfRev` = 200/220.)

## 공통 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · `image_url` = 앵커 head png · `tail_image_url` = 앵커 tail png · 출력 1932×1072 / 24 fps / 122 프레임 / 5.083 s.
negative: A04 829 → 494 · A05 898 → 497 · A06 916 → 498 · A06b 924 → 495 자.
스크럽 인코딩: `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an` → `src/v3-leg-0n.mp4` (5.9 / 8.9 / 6.7 / 6.7 MB). 씸 프레임은 인코딩본 `-sseof -0.15` → `gen/v3-leg-0n.seam.png`. 이전 4–6 산출물은 `gen/·src/v3-leg-0{4,5,6}.rev1.mp4`.

---

## Leg 4 — A04 → A05 (남원 평야 · 농지이용)

- taskId `7d082b1e19029f6577c28f1c96ac293a` · 과금 50 · 생성 171 s / wall 188 s · 원본 14.2 MB
- 프롬프트(verbatim): A04 `motion_prompt` — "The quilt of green and ochre felt parcels stays filling the lower two thirds of the frame throughout, the river of cobalt resin stays running through the middle distance, the top of the frame stays open and empty, and the greenhouse cluster stays small and far on the right the whole time; the warm key holds from upper camera left. The camera glides slowly and steadily forward and down over the parcels in one smooth continuous move. Nothing enters or leaves the frame, nothing moves on the ground. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→5 s 전진+하강 하나. 능선이 t≈2.5 에 하단 좌측으로 빠진다. 컷 없음. |
| 느림 | **통과** | 지난 라운드보다 완만. 필지 패턴이 t 0→4.85 내내 추적 가능. |
| 피사체 잔류 | **통과** | 펠트 필지·코발트 강 전 구간 유지, 모핑 없음. **온실 군락은 t 1.5 부터 우측 가장자리에 작게 붙어 있고 끝까지 작다** — 지난 라운드의 유령 돔 없음. |
| 아무것도 들어오지 않음 | **부분 실패** | **A05 의 회색 도로가 t≈1.3 에 좌상단 능선 너머에서 옅은 띠로 나타나 t≈2.5 에 능선을 넘어오는 고속도로 리본으로 굳고, t≥3 좌측 가장자리를 따라 내려간다.** tail 이 강제한 A05 요소(D1 "그대로"). 스튜디오 판/기둥은 없다. |
| 환각 | 도로 1건 | 위. 돔·크레이트·배경 없음. |
| 씸 → A05 | **양호** | t=4.85 ↔ A05 구도·필지·강·도로·군락 일치(인코딩 diff 0.199 %). |

**종합: 통과(도로 진입 1건).** 도로를 없애려면 A05 리롤(D1 번복)뿐이다.

## Leg 5 — A05 → A06 (비닐하우스 실태)

- taskId `ccf714017346797fa5ed86f65d17897f` · 과금 50 · 생성 538 s / wall 545 s · 원본 20.0 MB
- 프롬프트(verbatim): A05 `motion_prompt` — "The greenhouse cluster stays in frame and growing throughout, its brass hoops and translucent skins catching the same warm key from upper camera left, and the top of the frame stays open and empty. The camera descends slowly and steadily toward the cluster in one smooth continuous move with a very slight lateral drift to the right, so the rows open up. Nothing enters or leaves the frame, nothing moves on the lanes. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거 |
|---|---|---|
| 연속 이동 하나 | **통과** | 하강 + 우측 드리프트 하나. 도로가 t 0→1 좌측으로 빠진다(카메라 이동의 결과). 컷 없음. |
| 느림 | **통과** | t 0–1.5 느림, t 1.5–3 군락이 커지며 t≥3 정착. 지난 라운드의 t 1.7–2.5 점프 없음. |
| 피사체 잔류 | **부분** | 군락이 "우측 멀리 작게"에서 t≈1.2–1.5 에 **중앙 우측에 납작한 반투명 블록으로 해상(resolve)** 되고, t 2.5 부터 A06 의 후프 군락이 된다. 확대라기보다 해상이지만 재배열은 아니다. |
| 아무것도 들어오지 않음 | **통과(경미)** | **크레이트 없음, 구름 없음.** t≈1.5 상단 중앙에 작은 회색 건물 덩어리(마을처럼)가 잠깐 보였다가 t 2.5 에 필지에 묻힌다. |
| 환각 | 경미 1 | 위 건물 덩어리. t≥3 온실 스킨 안쪽의 따뜻한 광점은 A06 앵커의 앰버 시인성이라 의도 범위. |
| 씸 → A06 | **양호** | diff 0.168 %. |

**종합: 통과.** 카운트업 창(창 10–70 %)이 군락 해상 구간(t 1.2–2.5)과 겹쳐 "숫자가 올라가며 온실이 드러난다"로 읽힌다.

## Leg 6 — A06 → A06b (남원 상공 수직 상승, 신규)

- taskId `aa8dbd9f98a9627b1325934e93ab6026` · 과금 50 · 생성 701 s / wall 708 s · 원본 16.3 MB
- 프롬프트(verbatim): A06 `motion_prompt` — "The felt-parcel quilt and the greenhouse cluster stay in frame throughout, the top of the frame stays open and empty; the warm key holds from upper camera left. The camera rises in one smooth continuous climb, altitude only increases, the quilt and the cluster shrink away below-left, the ground curls into the arc of a model world, warm haze at the horizon. Nothing enters or leaves the frame, nothing moves on the ground. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거 |
|---|---|---|
| 연속 이동 하나 | **통과** | 상승 하나. t 0–2 군락이 줄고, t≈2.5 지평선이 휘기 시작, t 3–4.85 곡률+헤이즈 띠. 고도 단조 증가. |
| 느림 | **통과** | 5초에 걸쳐 고르게. |
| 피사체 잔류 | **통과** | 군락(광점 포함)이 끝까지 좌하단에 남는다. 필지 패턴 유지. |
| 아무것도 들어오지 않음 | **통과** | 우하단 코발트 바다가 t≈3 부터 곡면 가장자리로 들어오지만 A06b 가 요구한 요소이고 "곡면이 드러나며" 나타난다. 프롭·판·배경 없음. |
| 환각 | 없음 | — |
| 씸 → A06b | **양호** | diff 0.144 %. |

**종합: 통과.** 지난 라운드 leg 6(정지+모핑)의 교훈 1·4가 그대로 적중 — 이동을 상승 하나로 줄이자 kling 이 카메라를 움직였다.

## Leg 6b — A06b → A07 (곡면 남동 활강 → 여수, 신규)

- taskId `4b03a493a7770fd976f904f4b846e27f` · 과금 50 · 생성 100 s / wall 111 s · 원본 15.5 MB
- 프롬프트(verbatim): A06b `motion_prompt` — "The curved surface of the model world stays in frame throughout, the top of the frame stays open and empty; the warm key holds from upper camera left. The camera slides south-east along the curved surface and descends gently, the cobalt sea widening beneath, moss headlands, low fields meeting the shore, a harbour with a long grey breakwater arm coming up ahead. Nothing enters or leaves the frame, nothing moves on the water. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거 |
|---|---|---|
| 연속 이동 하나 | **부분** | t 0–1.5 곡면을 따라 활강(군락이 좌하로 빠지고 바다가 우측에서 넓어진다 — 진짜 이동). **t≈2–3 은 헤이즈+블러 속 디졸브**(t 2.5 프레임: 상단에 바다 띠, 필지가 흐릿하게 다른 패턴으로 바뀐다). t 3–4.85 여수 해안·항구·섬이 해상되고 카메라가 완만히 내려간다. |
| 느림 | **통과** | 컷은 없고 디졸브가 안개로 가려져 있어 스크럽에서 점프로 튀지 않는다. |
| 피사체 잔류 | **부분** | "곡면"은 유지되지만 지형 내용이 t 2–3 에 바뀐다(남원 필지 → 여수 해안). 지난 leg 6 보다는 이동으로 읽힌다. |
| 아무것도 들어오지 않음 | **부분** | 항구·섬·방파제가 t≥3 에 해상된다(프롬프트가 "coming up ahead" 로 허용). |
| 환각 | 디졸브 1 | 위. 프롭·판·배경 없음. |
| 씸 → A07 | **양호** | t=4.85 ↔ A07 방파제·섬·헤이즈 일치. |

**종합: 부분 통과.** 남원→여수 ~110 km 를 한 클립으로 시키는 한 중간 1초는 헤이즈 디졸브로 남는다. 다음에 더 줄이려면 A06b 를 두 장(곡면 고고도 → 여수 원경)으로 한 번 더 쪼개거나, 페이지 쪽 크로스페이드로 이 구간을 대신한다.

---

## 씸 실측 (assemble.mjs, 인코딩본 64×36 gray)

| 씸 | diff | luma | 눈(montage) |
|---|---|---|---|
| 03→04 | 0.165 % | 47.1→46.9 | 동일 프레임(능선+분지). |
| 04→05 | 0.199 % | 57.6→57.3 | 동일. 좌측 도로·우상단 온실 패치까지 같은 프레임. |
| 05→06 | 0.168 % | 61.8→61.7 | 동일. 남원 인계 판이 이 씸에서 올라온다(의도). |
| 06→06b | 0.144 % | 49.7→49.6 | 동일. 곡면+군락. 룰러 `여수` 가 여기서 켜진다. |
| 06b→07 | 13.26 % | 49.8→47.6 | **컷** — 미니어처 여수 항구 ↔ 여수 위성 정사(플레이스홀더 레그 07). 크로스페이드 0.16vh 가 삼킨다. AI 레그 7 이 오면 A07 tail 로 붙는다. |

## 스크럽 마운트 (`tools/scrub/assemble.mjs --only 04,05,06,06b`)

- PLAN 에 레그 **06b** 추가(06 과 07 사이). 06 = `상승`(wp 남원, 카메라 52.4 km → 140 km authored, 좌표는 남원 상공에 머문다), 06b = `여수 이동`(wp 여수, 140 km → 레그 07 시작 카메라 17 km; 좌표 차 0.77° 라 scrub.js JUMP 로 중간에서 컷). `handoffFinal.legIndex` 는 마지막 레그 인덱스(7)로 계산. 인계 #1 은 레그 05 끝 그대로(`legIndex:4`).
- 필름 35.94 → **41.02 s**, 트랙 7.834 → **8.942 vh**, 페이스 0.218 vh/s 편차 0 %(밴드 0.212–0.225 안). 데스크톱 47.8 MB / 모바일 13.0 MB.
- `landxi/proto/scrub/index.html`: 세그먼트 06b(w06b) 추가. **06 의 waypoint 를 `여수` → `남원`** 으로 — 06 은 남원 위 상승이므로 인계 판이 떠 있는 동안 레일이 여수를 가리키던 0.04 vh 문제가 함께 사라졌다. 룰러 `여수` 는 06b 에서 켜진다.
  - 카피 창은 전부 재계산: 레그 1–06 창은 **vh 위치를 그대로 두고** 새 총 트랙으로 나눔(×7.834/8.942), 07·피날레 창은 +1.108 vh 이동. 06b 창 `0.756 0.858`(레그 시작 +0.110 vh / 끝 −0.086 vh — 기존 +0.014/−0.011 규칙의 vh 환산).
  - 06 카피 `06 · 상승 · 남원 상공 / 땅이 다시 지구본으로 말린다`, 06b 카피 `06b · 지구본 이동 · 남원 → 여수 / 곡면을 따라 남쪽 해안으로`. 숫자 없음. leg 4 브래킷·leg 5 카운트업(9,664동 · 1,674필지)은 그대로. 타입은 토큰(`lx-label/sb-h2/lx-cap`)만 쓴다.
- 타임라인(`landxi/proto/film/timeline.html`): 행 06(상승)·**06b**(지구본 이동 → 여수) — 시작 25.42 / 30.50, 07 35.58, 미생성 08–12 41.02 → 61.35. 헤더 8 leg(13 계획 중 · 6b 포함) · 41.02 s.

## 테스트 · 촬영

- `npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed (5.2 m)**. 기대값 수정 3곳, 전부 정당한 변화: `M.legs.length` 7→8 · `seek(0.5)` = 4.47 vh → 레그 **05 '비닐하우스'**(이전 04 '남원') · 지연 로딩의 마지막 레그 인덱스 6→7. 항로 5지점·인계 카메라·마감 판 검사는 그대로 통과.
- `node tools/scrub/shoot-strip.mjs --legs 4-6b --prefix legs-4-6b` (id 를 manifest 에서 찾도록 수정, `#sb-caption` 없음에 null-safe) → `shots/scrub/legs-4-6b-{1..11}-*.png`, 콘솔/페이지 오류 0. 몽타주 `shots/scrub/legs-4-6b-montage.jpg`.
  - seam 03→04 / 04→05 / 05→06 / 06→06b mid: 두 레그 op 1/0.5, 같은 프레임 — 크로스페이드가 보이지 않는다.
  - L04-mid(film 2.52 s): 좌상단에 A05 도로 리본이 들어와 있다(위 leg 4 결함). 브래킷 렌더 확인.
  - L05-mid(film 2.53 s): 카운트업 `6,738동 · 1,167필지` 진행 중, 군락 대형. 크레이트 없음.
  - seam 05→06 mid: 남원 인계 판(검출 9,664 점) 위, **레일 `남원`**(고쳐짐).
  - L06-mid(film 2.53 s): 고도 85 km, 곡률 시작, 06 카피. seam 06→06b: 140 km, 레일 `여수` 점등.
  - L06b-mid(film 2.53 s): 헤이즈 디졸브 프레임(위 leg 6b 결함) 위에 06b 카피 — 안개 속이라 카피가 잘 읽힌다.
  - seam 06b→07 in/mid/out: 미니어처 항구 → 위성 정사 크로스페이드. 계기 17 km · 127.7305, 34.5630.

## 다음 라운드에 반영할 것

1. **이동 leg 는 "상승만" 또는 "활강만"으로 쪼개면 kling 이 카메라를 움직인다**(leg 6 통과). 남은 디졸브는 6b 의 앞뒤 앵커 내용 차이(남원 필지 ↔ 여수 해안). 레그 8(여수→울주)·11 도 같은 방식으로 중간 앵커를 둔다.
2. leg 4 도로는 A05 앵커의 것이라 프롬프트로는 못 막는다. 도로를 없애려면 D1 번복(A05 리롤).
3. leg 7 생성 시 레그 07 플레이스홀더를 A07 tail 로 교체하면 06b→07 씸(13 %)도 0.2 % 대로 내려간다. 여수 인계·브랜드 마감 카메라는 07 끝 카메라에 묶여 있으므로 그때 함께 옮긴다.

## 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-0{4,5,6,6b}.mp4` (+ `.rev1.mp4`) | kling 원본(이전 4–6 백업) |
| `landxi/assets/proto/film/legs/gen/v3-leg-0{4,5,6,6b}.seam.png` | 인코딩본 `-sseof -0.15` 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-0{4,5,6,6b}.mp4` (+ `.first/.last.webp`, `.rev1.mp4`) | 스크럽본 |
| `landxi/assets/proto/film/legs/w0{4,5,6,6b}{,-m}.mp4`, `w0n{,-last}.webp`, `manifest.json` | 마운트 산출물(8 레그) |
| `shots/kie/v3-legs-4-6b-sheet.jpg` · `-seams.jpg` · `-detail.jpg` · `v3-leg-0n-t*.jpg` · `legs-4-6b-run.log` | 검수 시트 · 씸 · 세부 · 프레임 · 로그 |
| `landxi/assets/proto/review/v3-legs-4-6b-{sheet,seams}.jpg` | 리뷰 사본 |
| `shots/scrub/legs-4-6b-*.png` · `legs-4-6b-montage.jpg` · `legs-4-6b-strip.json` · `e2e-legs-4-6b.log` | 스크럽 실촬영 · e2e 로그 |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(batch 4-6b 4건) |
| `tools/kie/legs-v3.mjs` · `tools/scrub/assemble.mjs` · `tools/scrub/shoot-strip.mjs` · `tests/e2e/proto-scrub.spec.mjs` | 러너(13앵커) · 조립기(06b) · 촬영(manifest id) · 스펙(8 레그) |
