# v3 Leg 4·5·6 — 영상 생성 + 스크럽 마운트 기록 (kling/v2-1-pro, 2026-08-27)

- 클라이언트 승인(원문): "go" — Leg 4·5·6 생성. 앵커 v3 그대로(A04 v3b 소품 0 · A05 우상단 회색 판 수용 · A06 · A07), 프롬프트 토씨 그대로.
- 앵커/프롬프트: `tools/kie/anchors-v3.json` (`motion_prompt` verbatim, `negative` 는 1–3 과 같은 `trimNeg()` 500자 규칙)
- 러너: `tools/kie/legs-v3.mjs` — `LEGS_V3_REV=3 LEGS_V3_BATCH=4-6` (배치 개념 추가: 1회/캡 규칙과 시트 이름을 (rev, batch) 별로 센다. 코드 외에는 1–3 과 동일)
- 원장: `shots/kie/legs-v3-credits.json` (`batch:"4-6"` 3건) · 검수 시트: `shots/kie/v3-legs-4-6-sheet.jpg` (3행 × t=0,1,2,3,4,4.85 + 다음 앵커) · 씸 비교: `shots/kie/v3-legs-4-6-seams.jpg`
- 규칙 준수: leg 당 과금 호출 **정확히 1회**, 자동 재시도 0, 실패 0, 추가 스틸 0, 캡 160 중 **150 사용**.

## 결론 (한 줄)

**셋 다 씸은 붙는다**(인코딩본 마지막 프레임 ↔ 다음 레그 첫 프레임 diff 0.17 %, 눈으로도 동일). 그러나 세 leg 모두 **"카메라가 이동한다"가 아니라 "세계가 그 자리에서 바뀐다"** 로 해석됐다 — leg 4 는 온실 유령 덩어리가 지나가고, leg 5 는 A05 의 회색 판이 **나무 격자 상자**로 굳었다가 빠지고, leg 6 은 카메라가 거의 서 있는 채로 **온실 군락이 항구로 모핑**한다. 1–3 라운드의 교훈("tail 고정 + 앵커 쌍의 내용 차이 = 디졸브")이 소품이 아니라 지형 전체로 확장된 셈이다.

## 크레딧

| 시점 | 잔액 | 증감 |
|---|---|---|
| 1–3 rev3 종료(원장 마지막) | 204.5 | — |
| **이 세션 시작 실측** | **154.5** | **−50 — 원장 밖에서 소모됨** (로컬 원장 어디에도 기록 없음. 이 저장소의 러너가 아닌 다른 경로(웹 UI/다른 세션)로 보인다. 확인 필요) |
| leg 4 `1473459678…` | 104.5 | −50 |
| leg 5 `4f826ad3…` | 54.5 | −50 |
| leg 6 `830d2b5c…` | **4.5** | −50 |

`creditsConsumed`(50) 와 잔액 차이(50)가 세 건 모두 일치. createTask 거부/무과금 오류 0건.

## 공통 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · `image_url` = A0n.png · `tail_image_url` = A0(n+1).png · 출력 1932×1072 / 24 fps / 122 프레임 / 5.083 s.
negative: A04 829자 → 494 · A05 827 → 492 · A06 828 → 493 (앵커 고유 항목 전부 + base 앞부분, 생략만). 보낸 문자열은 원장 `negative` 필드에 그대로.
스크럽 인코딩: `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an` → `src/v3-leg-0n.mp4` (8.1 / 7.3 / 5.8 MB). 씸 프레임은 인코딩본 `-sseof -0.15` → `gen/v3-leg-0n.seam.png`.

---

## Leg 4 — A04 → A05 (남원 평야 · 농지이용)

- taskId `1473459678e54c7f221a6cd2073b431d` · 과금 50 · 생성 107 s / wall 112 s · 원본 17.7 MB
- 프롬프트(verbatim): A04 `motion_prompt` — "The quilt of green and ochre felt parcels stays filling the lower two thirds of the frame throughout, the river of cobalt resin stays running through the middle distance, the top of the frame stays open and empty; the warm key holds from upper camera left. The camera glides slowly and steadily forward and down over the parcels in one smooth continuous move, the distant cluster of arched translucent structures on the right growing gradually larger. Nothing enters or leaves the frame, nothing moves on the ground. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→5 s 전진+하강 하나. 앞 능선이 t≈2.5 에 하단으로 빠진다(A05 에 능선이 없으므로 의도). 컷 없음. |
| 느림 | **경계** | 능선을 넘어 평야 한가운데까지 가는 거리가 5초치고 길다. 스크럽에서는 견딜 수 있는 속도. |
| 피사체 잔류 | **통과** | 펠트 필지·코발트 강 전 구간 유지. 필지 모핑 없음. |
| 아무것도 들어오지 않음 | **실패** | **반투명 온실 유령 덩어리가 t≈1.3 우중단에 나타나 t 1.7–3.0 화면 우측을 크게 지나가고 t≈3.4–3.8 에 제자리에서 디졸브**. "distant cluster of arched translucent structures … growing larger" 를 kling 이 "큰 반투명 돔이 카메라 옆을 스쳐 간다" 로 풀었다. 이 leg 의 유일하지만 큰 결함. |
| 환각 | **2건** | 위 온실 유령. 그리고 t≥4.0 우상단에 **회색 판(A05 의 것) + 그 뒤 어두운 수직 기둥** 이 들어온다 — tail 이 강제한 A05 요소라 씸 앞 0.9초에 걸쳐 스튜디오 배경처럼 읽힌다. |
| 씸 → A05 | **양호** | t=4.85 와 A05 구도·필지·강·회색 판 위치까지 일치(인코딩 diff 0.17 %). |

**종합: 부분 실패.** 앞 1.3초와 뒤 1.2초는 쓸 수 있다. 중간 온실 유령(t 1.3–3.8)은 재생성 없이는 못 고친다 — 프롬프트에서 "arched translucent structures" 문장을 빼고 "the cluster stays small and far on the right edge" 로 바꾸거나, A05 자체에서 군락을 더 작게.

## Leg 5 — A05 → A06 (비닐하우스 실태)

- taskId `4f826ad3e98c6927ce0f6450fd7a0103` · 과금 50 · 생성 155 s / wall 173 s · 원본 17.0 MB
- 프롬프트(verbatim): A05 `motion_prompt` — "The greenhouse cluster stays in frame and growing throughout, its brass hoops and translucent skins catching the same warm key from upper camera left, and the top of the frame stays open and empty. The camera descends slowly and steadily toward the cluster in one smooth continuous move with a very slight lateral drift to the right, so the rows open up and the soft cotton-wool cloud shadow creeps along the far end of the cluster. Nothing enters or leaves the frame, nothing moves on the lanes. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거 |
|---|---|---|
| 연속 이동 하나 | **통과(형식상)** | 컷 없음. 다만 t 1.7–3.0 에 군락이 크게 재배열된다(아래). |
| 느림 | **부분** | t 0–1.5 는 느림. **t 1.7–2.5 에 군락이 갑자기 커지고 정렬**되며 필지가 함께 흐른다 — 시각적으로는 점프에 가깝다. t≥3.0 정지에 가까움. |
| 피사체 잔류 | **부분** | 온실은 프레임 안에 남지만 "우상단 멀리 작게" 에서 "중앙 대형 군락"으로 **모핑**한다(이동·확대가 아니라 재구성). 필지 패턴도 t 2.1 전후로 바뀐다. |
| 아무것도 들어오지 않음 | **실패** | (1) **A05 우상단 회색 판이 t≈0.4 부터 좌상단의 나무 격자 상자(창살 달린 크레이트)로 굳어** t≈2.1 까지 있다가 좌상단 밖으로 빠진다 — 우려했던 "회색 판이 벽이 된다" 가 그대로 일어났다. (2) **솜구름이 t≈0.85 우중단에서 무(無)에서 나타난다**(A06 의 구름 그림자를 tail 이 강제). |
| 환각 | **크레이트 + 구름 출현** | 위. 그 외 온실 스킨은 A06 과 같은 따뜻한 반투명. 배경 상단 좌측에 t 0–0.8 옅은 회백색 하늘/배경 조각. |
| 씸 → A06 | **양호** | t=4.85 와 A06 온실 열·구름·강·필지 거의 동일(diff 0.17 %). |

**종합: 부분 실패.** 뒤 2초(t 3.0–5.08)는 A06 그대로라 인계 판 직전 구간으로 쓸 만하다. 앞 2초는 크레이트가 화면 좌상단을 차지한다. A05 의 회색 판은 "수용"이 아니라 **리롤이 맞았다** — 영상에서 물체로 굳는다.

## Leg 6 — A06 → A07 (지구본 이동 → 여수)

- taskId `830d2b5c08a044a21d82a633858f04a8` · 과금 50 · 생성 453 s / wall 464 s · 원본 13.9 MB
- 프롬프트(verbatim): A06 `motion_prompt` — "The ground stays continuous and in frame throughout with no cut of any kind, the top of the frame stays open and empty; the warm key holds from upper camera left. The camera rises in one smooth continuous banking arc, climbing steadily while it slides south-east across the surface, so the ground curls away into the gentle arc of a model world and the quilted inland parcels give way to moss headlands, cobalt resin inlets and a harbour with a long grey breakwater arm coming up beneath. The altitude only increases, never dips. Nothing enters or leaves the frame. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거 |
|---|---|---|
| 연속 이동 하나 | **실패** | 카메라가 **거의 서 있다**. t 0–1.5 온실 정지 화면, t 1.7–2.8 **온실이 그 자리에서 항구·방파제로 모핑**(바다가 온실 밑에서 배어 나오고 군락이 항구 건물로 바뀐다), t≥3.0 여수 해안 정지 화면. "banking arc / slides south-east / ground curls" 어느 것도 일어나지 않았다. |
| 느림 | 통과(정지) | 느린 게 아니라 안 움직인다. 고도 상승 없음("altitude only increases" 무시). |
| 피사체 잔류 | **실패** | 피사체가 바뀐다(온실 → 항구). 지형 모핑 그 자체. |
| 아무것도 들어오지 않음 | **실패** | 바다·헤드랜드·방파제·어선이 t 1.7–2.8 에 생겨난다. 구름 그림자는 안개 띠로 바뀐다. |
| 환각 | **지형 모핑** | 위. 그 외 보드/배경 노출 없음(상단은 어둡게 유지). |
| 씸 → A07 | **양호** | t=4.85 와 A07 방파제·어선·헤드랜드·헤이즈 일치(diff 0.17 %). |

**종합: 실패(모핑).** 씸 두 개는 붙지만, 스크럽에서 이 레그는 "이동" 이 아니라 "장면 전환"으로 읽힌다. 남원→여수 사이 거리(직선 ~110 km)를 5초 한 클립에 tail 고정으로 시키면 kling 은 카메라 대신 세계를 바꾼다. 다음 라운드는 (a) 중간 앵커 1장(구름/헤이즈 화이트아웃 — 샷 리스트 v2 의 "상승 시 안개")을 두고 2클립으로 나누거나, (b) tail 없이 head-only 로 상승만 시키고 씸을 헤이즈로 가리는 설계가 맞다.

---

## 씸 실측 (assemble.mjs, 인코딩본 64×36 gray)

| 씸 | diff | luma | 눈 |
|---|---|---|---|
| 03→04 | 0.167 % | 47.1→46.9 | 동일 프레임. |
| 04→05 | 0.170 % | 62.8→62.7 | 동일. 우상단 회색 판/기둥 포함해서 동일. |
| 05→06 | 0.169 % | 64.0→63.8 | 동일. |
| 06→07 | 12.95 % | 42.7→47.6 | **컷** — 미니어처 항구 사선 ↔ 여수 위성 정사(플레이스홀더 레그 07). 크로스페이드 0.16vh 가 삼킨다(휘도 단조). AI 레그 7 이 오면 A07 tail 로 붙는다. |

## 스크럽 마운트 (`tools/scrub/assemble.mjs --only 04,05,06`)

- 레그 04·05·06 을 `mp4` kind 로 교체(1946×1080 24 fps 5.083 s, 데스크톱본 재인코딩 없음, 모바일본은 `gen` 원본에서 960×540 GOP 4). 레그 07 은 MapLibre 여수 플레이스홀더 유지 — 여수 인계 판·브랜드 마감이 그 카메라에 묶여 있다.
- 필름 32.05 s → **35.94 s**, 트랙 6.987 → **7.834 vh**, 페이스 0.218 vh/s 편차 0 %(밴드 0.212–0.225 안). 데스크톱 41.2 MB / 모바일 11.4 MB.
- **인계 #1(남원)을 레그 06 끝 → 레그 05 끝으로 옮겼다** (`handoff.afterLeg:'05', legIndex:4`). 레그 06 이 여수 항구로 끝나므로 그 뒤에 남원 실지도가 뜨면 프레임과 모순이다. 인계 카메라 값(127.4250, 35.4290 · ALT 52.4 km · 온실 검출 9,664동)은 이전 레그 06 끝 카메라를 레그 05 끝 카메라로 그대로 넘겨 판·카피·테스트가 바뀌지 않게 했다. AI 레그 카메라는 전부 `cameraSource:"authored"`.
- 오버레이(`landxi/proto/scrub/index.html`, 샷 리스트 v2, 실수치만):
  - leg 4 브래킷 `농지이용 현황 2,098필지 · 경작 1,291 / 비경작 807 · 2025 드론` — `namwon-farmland-2025.geojson` 실측(2,098 피처, cls 경작지 1,291 · 비경작지 807). 코너 브래킷은 system.css §16 관용구(`.sb-bracket`).
  - leg 5 `비닐하우스 9,664동 · 1,674필지` 카운트업 — `namwon-greenhouse-2025.geojson` 1,674 피처, nobj 합 9,664. 엔진의 `data-sc-count` 는 act 전용이라 `scrub.js paintCounts()` 가 카피 창 진행도로 센다(`data-sb-count`).
  - leg 6 룰러 `여수` 점등 — `data-sc-waypoint="여수"` 를 레그 06 에 둬 항로 레일의 여수 마커가 이 레그에서 켜진다. 숫자 없음.
  - 카피 창은 총 트랙이 바뀌어 전부 재계산(레그 시작 +0.014 / 끝 −0.011 규칙 유지). 마스트헤드 35.9초.
- 타임라인(`landxi/proto/film/timeline.html`) 04–06 행: AI 생성 (kling v2-1-pro) · 5.08 s, 시작 시각 15.25 / 20.33 / 25.42, 07 30.50, 미생성 08–12 35.94 → 56.27. 헤더 35.94 s (AI 6 · 임시 렌더 1).

## 테스트 · 촬영

- `npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed (5.1 m)**, 기대값 수정 0. (seek(0.5) 는 7.834 vh 에서 3.917 vh = 레그 04 '남원' 그대로, 인계 판 카메라는 `manifest.handoff` 로 읽으므로 legIndex 이동에 영향 없음, 레그 07 지연 로딩 반경 밖 유지.)
- `node tools/scrub/shoot-strip.mjs --legs 4-6 --prefix legs-4-6` → `shots/scrub/legs-4-6-{1..9}-*.png`, 콘솔/페이지 오류 0. 몽타주 `shots/scrub/legs-4-6-montage.jpg`.
  - 씸 03→04 mid: 두 레그 op 1/0.5, 같은 프레임(능선+분지) — 크로스페이드가 보이지 않는다.
  - L4-mid(film 2.53 s): 온실 유령 덩어리가 화면 우측에 크게 — 위 leg 4 결함이 스크럽에서 그대로 보인다. 브래킷 `농지이용 현황 2,098필지 · 경작 1,291 / 비경작 807 · 2025 드론` 렌더 확인.
  - 씸 04→05 mid: 동일 프레임. 우상단 회색 판은 두 레그가 공유하므로 씸에서 튀지 않는다.
  - L5-mid(film 2.53 s): 카운트업 진행 중 `6,748동 · 1,169필지`(창 70 % 지점에서 9,664 / 1,674 도달). 크레이트는 이미 좌상단 밖.
  - 씸 05→06 mid: 남원 인계 판(온실 검출 9,664 점) 이 올라와 있다 — 의도. 항로 레일은 이 지점에서 이미 `여수` 로 넘어간다(--sc-seg 가 레그 06). 판이 켜진 동안 레일이 여수를 가리키는 0.04 vh 가 있다 — 사소하지만 다음에 밴드 끝을 씸 시작으로 당길 것.
  - 씸 06→07 mid: 미니어처 항구(op 1) 위에 여수 위성(op 0.5) 크로스페이드 — 컷이 페이드로 읽힌다. 계기 17 km · 127.7305, 34.5630.

## 다음 라운드에 반영할 것

1. **tail 고정은 "앵커 쌍의 내용 차이"를 전부 디졸브/모핑으로 메운다.** 소품(1–3)뿐 아니라 지형(6)·구조물(4·5)도 그렇다. 앵커 쌍은 같은 장면의 카메라 차이만 있어야 하고, 장면이 바뀌는 leg(6·8·11)는 중간 앵커(헤이즈)로 쪼개거나 head-only 로 간다.
2. **A05 회색 판은 리롤.** 스틸에서는 "판"이지만 영상에서는 나무 크레이트가 된다.
3. leg 4 프롬프트의 "arched translucent structures … growing larger" 는 빼거나 "stays small on the far right" 로.
4. 이동 leg 의 카메라는 kling 이 잘 못 한다("banking arc", "slides south-east" 무시). 이동은 페이지 쪽(크로스페이드·헤이즈)으로 옮기고 클립은 상승/하강 하나만 시킨다.
5. 잔액 4.5 — 다음 라운드 전 충전 필요. 원장 밖 −50 의 출처 확인.

## 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-0{4,5,6}.mp4` | kling 원본 |
| `landxi/assets/proto/film/legs/gen/v3-leg-0{4,5,6}.seam.png` | 인코딩본 `-sseof -0.15` 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-0{4,5,6}.mp4` (+ `.first/.last.webp`) | 스크럽본 |
| `landxi/assets/proto/film/legs/w0{4,5,6}{,-m}.mp4`, `w0n{,-last}.webp`, `manifest.json` | 마운트 산출물 |
| `shots/kie/v3-legs-4-6-sheet.jpg` · `v3-legs-4-6-seams.jpg` · `v3-leg-0n-t*.jpg` | 검수 시트 · 씸 비교 · 검수 프레임 |
| `shots/scrub/legs-4-6-*.png` · `legs-4-6-strip.json` | 스크럽 페이지 실촬영 |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(batch 4-6 3건) |
| `tools/kie/legs-v3.mjs` · `tools/scrub/assemble.mjs` · `tools/scrub/shoot-strip.mjs` | 러너(batch) · 조립기(04–06 mp4, 인계 이동) · 촬영(`--legs`) |
