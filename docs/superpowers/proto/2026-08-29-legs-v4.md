# legs v4 — 브라스 링 폐기 뒤 레그 재촬영 (kling/v2-1-pro, rev.4)

앵커가 rev.3k(A01) · rev.3m(A02) 로 바뀌면서 **브라스 아마추어 링이 앵커에서 사라졌다**.
링을 안고 굳어 있는 레그들을 하나씩 다시 굽는 라운드다. 레그 번호마다 §n 으로 적는다.

- 러너: `tools/kie/legs-v3.mjs` (`LEGS_V3_REV=4 LEGS_V3_BATCH=…`), 원장 `shots/kie/legs-v3-credits.json`
- 규칙: 레그당 과금 호출 **정확히 1회**, 자동 재시도 없음. 프롬프트는 `tools/kie/anchors-v3.json` 의 `motion_prompt` 토씨 그대로.

---

## §1 — 레그 01 (A01 rev.3k → A02 rev.3m · 궤도 · 모형 지구본)

### 크레딧

| 항목 | 값 |
|---|---|
| taskId | `b6d6753673bb382e64075bcd608db8f8` |
| 과금 | **50** (`credits_reported` 50, 잔액차 50) |
| 잔액 | 9,102.5 → **9,052.5** |
| 호출 | createTask 1회 (rev.4 batch "1", 캡 60 중 50 사용) · 재생성 0 |
| 시각 | 2026-08-29T02:34:11Z 발주 → 02:38:28Z 완료 (wall 251 s, kie `gen_ms` 233) |

폴링 중 회선이 끊겨 이전 세션이 인코딩 도중 죽었다. **kie 재호출 0** — 디스크에 이미 있는
`gen/v3-leg-01.mp4`(14,431,866 B) 로만 이어 붙였다. 이 문서를 쓰는 시점(2026-08-31)에도 잔액 9,052.5 그대로.

### 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A01.png`(rev.3k) · tail `anchors-v3/A02.png`(rev.3m)
· 출력 1932×1072 / 24 fps / 122 프레임 / **5.083 s** (v3a 와 동일 — 매니페스트 길이 불변)
· `negative` 원문 1,012자 → 500자(`trimNeg`, 생략만) · `prompt_sha256 0f36e29db90d5a63`

프롬프트(verbatim, A01 `motion_prompt`):

> The model globe and the tiny foil-wrapped satellite in the upper right stay perfectly still and in frame throughout; the tiny satellite stays tiny and in place, holding its size and position with no push-in on it, no zoom toward it and no growth, and the single warm key holds from upper camera left. The camera pushes slowly and steadily forward toward the globe, a smooth continuous dolly-in centred on the Korean peninsula with a very slight downward tilt; the dark unlit workbench plane slides out of the bottom of the frame as the push crops it away. There is no ring, hoop or arc anywhere. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

스크럽 인코딩(`legs-v3.mjs post 1`): `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an`
→ `src/v3-leg-01.mp4` (6,092,138 B). 씸 프레임 `-sseof -0.15` → `gen/v3-leg-01.seam.png`.

### 판정 — **통과 (마운트함)**

검수 프레임 t=0 / 1.25 / 2.5 / 3.75 / 5 → `shots/kie/leg01-v4-frames.jpg`, 리뷰 사본 960w `landxi/assets/proto/review/leg01-v4.jpg`.
추가로 4 fps 20프레임 전수 스캔으로 확인.

| 기준 | 판정 | 근거 |
|---|---|---|
| 브라스 링 전 구간 부재 | **통과** | 20/20 프레임에 링·후프·아크 없음. 지구본 림의 금색은 앵커 A01 rev.3k 자체의 해안선 하이라이트다(앵커와 동일). |
| 위성이 작게 머무름 (≤ ~20 % 화면폭) | **통과** | 본체+패널 바운딩박스 t=0 **8.5 %** → t=5 **12.6 %** (임계 55 라벨링, 1946 px 기준). 눈으로 읽는 패널 끝까지 포함해도 t=5 에서 ~20 %. 같은 5초에 지구본은 화면폭 40 % → 화면 가득(2.5×+) 으로 자란다 — **위성은 장면 대비 오히려 작아진다**. 밀어 넣기·확대·복제 없음, 상시 1기. |
| 한반도 정면 돌리인 연속 | **통과** | 컷·점프·셰이크 없는 한 테이크. 워크벤치 판은 t≈2.6 에 프레임 아래로 빠진다. |
| A02(rev.3m) 로 이어짐 | **통과** | t=5 프레임이 A02.1280.jpg 와 구도·구름·위성 위치까지 같은 프레임으로 읽힌다. |

### 씸 — **01→02 0.236 % → 16.175 % (규칙 B)**

`assemble.mjs` 실측(인코딩본 64×36 gray, 씸 법칙 6 — 레그 02 의 씸 상대 프레임은 **새로 인코딩한 w01.mp4 의 마지막 프레임**):

| 씸 | 이전 | 지금 | luma |
|---|---|---|---|
| **01→02** | 0.236 % | **16.175 %** | 59.31 → 65.63 (+6.32) |
| 02→03 … 10→11 | 0.15–0.27 % | 변동 없음 | — |

**원인은 레그 01 이 아니라 레그 02 다.** 레그 02 는 2026-08-26 에 A02 **rev.3b**(브라스 링 있음) 를 head 로 구운
그대로다(taskId `89933a08474a581b288ac194f2954466`). w02 첫 프레임에는 지구본 위를 가로지르는 **금색 링과 링을 타는 위성**이
그대로 남아 있고, w01 마지막 프레임(A02 rev.3m)에는 링이 없다 — 그 차이가 16 %다.
→ **필름 5.08 s 지점에서 링이 튀어나온다. §2(레그 02 를 A02 rev.3m 로 재촬영)가 붙어야 닫힌다.**
규칙 B 는 휘도 단조만 보므로 `pass:true` 지만, 실제로는 크로스페이드가 삼키기엔 큰 컷이다(full.mp4 에서 0.734 s 디졸브로 보인다).

### 마운트 (`node tools/scrub/assemble.mjs --only 01`)

- `w01.mp4` 3,173,889 → **3,548,302 B**, `w01-m.mp4` 1,236,843 → **1,280,519 B** (crf 24 / 모바일 27, `-g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an`)
- 포스터·씸 포스터 `w01.webp` / `w01-last.webp`, 소스 옆 `src/v3-leg-01.first/.last.webp` 재생성
- 합계 데스크톱 50.21 → **50.57 MB** (≤60) · 모바일 17.53 → **17.57 MB** (≤20) · 포스터 1.22 MB
- **필름 66.079 s · 트랙 14.404 vh · 스페이서 15.404 vh — 전부 불변**(길이 5.083 s 동일). 페이스 편차 0 %.
  → `landxi/proto/film/timeline.html` · `landxi/proto/scrub/index.html` 수정 **없음**.
- 풀 영상본 `tools/film/full.mjs` 재생성: **57.292 s · 1,375 프레임 · 1920×1080 · 38.77 MB**, xfade 0.734 s × 12, crf 25. `full.webp` 동반.

### 백업 (v3a)

`legs-v3.mjs` 가 발주 전에 남긴 것 + 마운트 산출물 사본. 되돌리려면 이 파일들을 원래 이름으로 복사하고 `--only 01` 을 다시 돌리면 된다.

`gen/v3-leg-01.v3a.mp4` · `gen/v3-leg-01.seam.v3a.png` · `src/v3-leg-01.v3a.mp4` · `src/v3-leg-01.first/.last.v3a.webp` · `w01.v3a{,-m}.mp4` · `w01.v3a{,-last}.webp`

### 테스트

`npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed + 1 skipped (8.2 m)**. 기대값 수정 0.

씸 16 % 는 e2e 를 깨지 않는다 — 스펙 5번은 픽셀 diff 가 아니라 "씸 밴드에서 완전 불투명한 레그가 항상 하나 이상"인 구조 보증이고,
`M.seams` 의 pct 임계(< 1 %)는 `frameShared` 씸에만 걸린다(전 씸이 규칙 B 라 해당 없음). 바이트 예산도 통과(50.57 ≤ 60 · 17.57 ≤ 20).
스킵 1건은 D16 ① 폐기로 이미 비활성인 드론 오버레이 테스트다.

### 다음

1. **§2 — 레그 02 재촬영(A02 rev.3m → A03).** 링 폐기의 남은 절반이고, 01→02 씸 16 % 를 닫는 유일한 길이다.
2. 이후 레그들(03 이하)은 head 앵커가 바뀌지 않았으므로 그대로 둔다 — rev.3l 에서 리롤된 A03 를 쓰는 레그 02·03 만 확인하면 된다.

### 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-01.mp4` · `.seam.png` | kling 원본(v4) · 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-01.mp4` (+ `.first/.last.webp`) | 스크럽본(crf 20) |
| `landxi/assets/proto/film/legs/w01{,-m}.mp4` · `w01{,-last}.webp` · `manifest.json` | 마운트 산출물(13 레그) |
| `landxi/assets/proto/film/legs/full.mp4` · `full.webp` | 풀 영상본(crf 25) |
| `shots/kie/leg01-v4-frames.jpg` · `v3-leg-01-t*.jpg` · `legs-v4-1-run.log` | 검수 프레임 · 로그 |
| `landxi/assets/proto/review/leg01-v4.jpg` | 리뷰 사본 960w |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(rev 4, batch "1") |
