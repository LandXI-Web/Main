# legs v4 — 브라스 링 폐기 뒤 레그 재촬영 (kling/v2-1-pro, rev.4)

앵커가 rev.3k(A01) · rev.3m(A02) 로 바뀌면서 **브라스 아마추어 링이 앵커에서 사라졌다**.
링을 안고 굳어 있는 레그들을 하나씩 다시 굽는 라운드다. 레그 번호마다 §n 으로 적는다.

- 러너: `tools/kie/legs-v3.mjs` (`LEGS_V3_REV=4 LEGS_V3_BATCH=…`), 원장 `shots/kie/legs-v3-credits.json`
- 규칙: 레그당 과금 호출 **정확히 1회**, 자동 재시도 없음. 프롬프트는 `tools/kie/anchors-v3.json` 의 `motion_prompt` 토씨 그대로.

---

## §1 — 레그 01 (A01 rev.3k → A02 rev.3m · 궤도 · 모형 지구본)

### 크레딧

| 항목 | 값 |
|---|---|
| taskId | `b6d6753673bb382e64075bcd608db8f8` |
| 과금 | **50** (`credits_reported` 50, 잔액차 50) |
| 잔액 | 9,102.5 → **9,052.5** |
| 호출 | createTask 1회 (rev.4 batch "1", 캡 60 중 50 사용) · 재생성 0 |
| 시각 | 2026-08-29T02:34:11Z 발주 → 02:38:28Z 완료 (wall 251 s, kie `gen_ms` 233) |

폴링 중 회선이 끊겨 이전 세션이 인코딩 도중 죽었다. **kie 재호출 0** — 디스크에 이미 있는
`gen/v3-leg-01.mp4`(14,431,866 B) 로만 이어 붙였다. 이 문서를 쓰는 시점(2026-08-31)에도 잔액 9,052.5 그대로.

### 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A01.png`(rev.3k) · tail `anchors-v3/A02.png`(rev.3m)
· 출력 1932×1072 / 24 fps / 122 프레임 / **5.083 s** (v3a 와 동일 — 매니페스트 길이 불변)
· `negative` 원문 1,012자 → 500자(`trimNeg`, 생략만) · `prompt_sha256 0f36e29db90d5a63`

프롬프트(verbatim, A01 `motion_prompt`):

> The model globe and the tiny foil-wrapped satellite in the upper right stay perfectly still and in frame throughout; the tiny satellite stays tiny and in place, holding its size and position with no push-in on it, no zoom toward it and no growth, and the single warm key holds from upper camera left. The camera pushes slowly and steadily forward toward the globe, a smooth continuous dolly-in centred on the Korean peninsula with a very slight downward tilt; the dark unlit workbench plane slides out of the bottom of the frame as the push crops it away. There is no ring, hoop or arc anywhere. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

스크럽 인코딩(`legs-v3.mjs post 1`): `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an`
→ `src/v3-leg-01.mp4` (6,092,138 B). 씸 프레임 `-sseof -0.15` → `gen/v3-leg-01.seam.png`.

### 판정 — **통과 (마운트함)**

검수 프레임 t=0 / 1.25 / 2.5 / 3.75 / 5 → `shots/kie/leg01-v4-frames.jpg`, 리뷰 사본 960w `landxi/assets/proto/review/leg01-v4.jpg`.
추가로 4 fps 20프레임 전수 스캔으로 확인.

| 기준 | 판정 | 근거 |
|---|---|---|
| 브라스 링 전 구간 부재 | **통과** | 20/20 프레임에 링·후프·아크 없음. 지구본 림의 금색은 앵커 A01 rev.3k 자체의 해안선 하이라이트다(앵커와 동일). |
| 위성이 작게 머무름 (≤ ~20 % 화면폭) | **통과** | 본체+패널 바운딩박스 t=0 **8.5 %** → t=5 **12.6 %** (임계 55 라벨링, 1946 px 기준). 눈으로 읽는 패널 끝까지 포함해도 t=5 에서 ~20 %. 같은 5초에 지구본은 화면폭 40 % → 화면 가득(2.5×+) 으로 자란다 — **위성은 장면 대비 오히려 작아진다**. 밀어 넣기·확대·복제 없음, 상시 1기. |
| 한반도 정면 돌리인 연속 | **통과** | 컷·점프·셰이크 없는 한 테이크. 워크벤치 판은 t≈2.6 에 프레임 아래로 빠진다. |
| A02(rev.3m) 로 이어짐 | **통과** | t=5 프레임이 A02.1280.jpg 와 구도·구름·위성 위치까지 같은 프레임으로 읽힌다. |

### 씸 — **01→02 0.236 % → 16.175 % (규칙 B)**

`assemble.mjs` 실측(인코딩본 64×36 gray, 씸 법칙 6 — 레그 02 의 씸 상대 프레임은 **새로 인코딩한 w01.mp4 의 마지막 프레임**):

| 씸 | 이전 | 지금 | luma |
|---|---|---|---|
| **01→02** | 0.236 % | **16.175 %** | 59.31 → 65.63 (+6.32) |
| 02→03 … 10→11 | 0.15–0.27 % | 변동 없음 | — |

**원인은 레그 01 이 아니라 레그 02 다.** 레그 02 는 2026-08-26 에 A02 **rev.3b**(브라스 링 있음) 를 head 로 구운
그대로다(taskId `89933a08474a581b288ac194f2954466`). w02 첫 프레임에는 지구본 위를 가로지르는 **금색 링과 링을 타는 위성**이
그대로 남아 있고, w01 마지막 프레임(A02 rev.3m)에는 링이 없다 — 그 차이가 16 %다.
→ **필름 5.08 s 지점에서 링이 튀어나온다. §2(레그 02 를 A02 rev.3m 로 재촬영)가 붙어야 닫힌다.**
규칙 B 는 휘도 단조만 보므로 `pass:true` 지만, 실제로는 크로스페이드가 삼키기엔 큰 컷이다(full.mp4 에서 0.734 s 디졸브로 보인다).

### 마운트 (`node tools/scrub/assemble.mjs --only 01`)

- `w01.mp4` 3,173,889 → **3,548,302 B**, `w01-m.mp4` 1,236,843 → **1,280,519 B** (crf 24 / 모바일 27, `-g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an`)
- 포스터·씸 포스터 `w01.webp` / `w01-last.webp`, 소스 옆 `src/v3-leg-01.first/.last.webp` 재생성
- 합계 데스크톱 50.21 → **50.57 MB** (≤60) · 모바일 17.53 → **17.57 MB** (≤20) · 포스터 1.22 MB
- **필름 66.079 s · 트랙 14.404 vh · 스페이서 15.404 vh — 전부 불변**(길이 5.083 s 동일). 페이스 편차 0 %.
  → `landxi/proto/film/timeline.html` · `landxi/proto/scrub/index.html` 수정 **없음**.
- 풀 영상본 `tools/film/full.mjs` 재생성: **57.292 s · 1,375 프레임 · 1920×1080 · 38.77 MB**, xfade 0.734 s × 12, crf 25. `full.webp` 동반.

### 백업 (v3a)

`legs-v3.mjs` 가 발주 전에 남긴 것 + 마운트 산출물 사본. 되돌리려면 이 파일들을 원래 이름으로 복사하고 `--only 01` 을 다시 돌리면 된다.

`gen/v3-leg-01.v3a.mp4` · `gen/v3-leg-01.seam.v3a.png` · `src/v3-leg-01.v3a.mp4` · `src/v3-leg-01.first/.last.v3a.webp` · `w01.v3a{,-m}.mp4` · `w01.v3a{,-last}.webp`

### 테스트

`npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed + 1 skipped (8.2 m)**. 기대값 수정 0.

씸 16 % 는 e2e 를 깨지 않는다 — 스펙 5번은 픽셀 diff 가 아니라 "씸 밴드에서 완전 불투명한 레그가 항상 하나 이상"인 구조 보증이고,
`M.seams` 의 pct 임계(< 1 %)는 `frameShared` 씸에만 걸린다(전 씸이 규칙 B 라 해당 없음). 바이트 예산도 통과(50.57 ≤ 60 · 17.57 ≤ 20).
스킵 1건은 D16 ① 폐기로 이미 비활성인 드론 오버레이 테스트다.

### 다음

1. **§2 — 레그 02 재촬영(A02 rev.3m → A03).** 링 폐기의 남은 절반이고, 01→02 씸 16 % 를 닫는 유일한 길이다.
2. 이후 레그들(03 이하)은 head 앵커가 바뀌지 않았으므로 그대로 둔다 — rev.3l 에서 리롤된 A03 를 쓰는 레그 02·03 만 확인하면 된다.

### 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-01.mp4` · `.seam.png` | kling 원본(v4) · 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-01.mp4` (+ `.first/.last.webp`) | 스크럽본(crf 20) |
| `landxi/assets/proto/film/legs/w01{,-m}.mp4` · `w01{,-last}.webp` · `manifest.json` | 마운트 산출물(13 레그) |
| `landxi/assets/proto/film/legs/full.mp4` · `full.webp` | 풀 영상본(crf 25) |
| `shots/kie/leg01-v4-frames.jpg` · `v3-leg-01-t*.jpg` · `legs-v4-1-run.log` | 검수 프레임 · 로그 |
| `landxi/assets/proto/review/leg01-v4.jpg` | 리뷰 사본 960w |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(rev 4, batch "1") |

---

## §2 — 레그 02 · 03 · 11 · 12 재촬영 (D18 승인 "Leg 2·3·11·12 (200)")

클라이언트 승인(리뷰 허브 D18 원문): *"OK → Leg 1 먼저(50) 위성 크기 유지 확인 → Leg 2·3·11·12(200)"*.
§1 에서 레그 01 이 통과했으므로 나머지 넷을 **한 번에 하나씩** 굽는다.

- 러너: `LEGS_V3_REV=4 LEGS_V3_BATCH=2-3-11-12 LEGS_V3_PREV=v3a LEGS_V3_CAP=200 node tools/kie/legs-v3.mjs run <leg>`
- 규칙: 레그당 과금 호출 **정확히 1회**, 자동 재시도 없음, 세션 캡 **200**(50 × 4). 프롬프트는 `anchors-v3.json` 의
  시작 앵커 `motion_prompt` **토씨 그대로**, `negative` 는 `trimNeg()` 500자(생략만). 호출 **전에** 이 절에 프롬프트를 적는다.
- pending 줄은 createTask 직후 원장(`shots/kie/legs-v3-credits.json`)에 적힌다. 회선이 끊기면 `fetch <leg>` 로 taskId 회수 — **재생성 금지**.
- 순서: **02 → 03 → 11 → 12.** 레그마다 5프레임 추출 + 육안 판정 + 이 문서에 판정표를 쓰고 나서 다음 레그로 간다.
- 씸 법칙(변경 없음): 러너의 head/tail 은 **앵커 PNG** 다. "이전 레그의 인코딩본 마지막 프레임"은 `assemble.mjs` 가
  씸을 **측정**할 때의 기준 프레임이지 생성 입력이 아니다(§1 과 동일 절차).
- 시작 잔액 **9,052.5**.

### §2-1 레그 02 — A02(rev.3m) → A03(rev.3n) · 성층운 · 구름 천장

호출 전 기록. `model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A02.png`(rev.3m) · tail `anchors-v3/A03.png`(rev.3n)
· `prompt_sha256 63426e23fb9a2444` · `negative` 원문 871자 → 489자(`trimNeg`, 생략만).

프롬프트(verbatim, A02 `motion_prompt`):

> The Korean peninsula stays dead centre and in frame throughout; the warm key holds from upper camera left. The tiny satellite in the upper right stays tiny and in place, holding its size and position with no push-in on it, no zoom toward it and no growth. The camera descends slowly and steadily toward the surface in one smooth continuous move, tilting gently down from level to about sixty degrees as the curvature flattens. The cotton-wool cloud bank rises past the lens and thins away, and as it clears, a small white aircraft model is revealed high in the upper right and a small white drone model is revealed low in the lower third of the frame, both already there beneath the cloud, holding their places. Because the lens tilts down, the tiny satellite slips above the top edge of the frame within the first second and does not return. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

negative(trimNeg 489자):

```
stars, starfield, black space, outer space background, blue sky paint, city lights, atmosphere glow ring, drone, quadrotor, rotors, aircraft, airplane, plane model, ring, hoop, armature, arc, meridian, meridian ring, orbit line, string, wire, brass cap, gilded rim, large satellite, oversized spacecraft, close-up spacecraft, board, table, plinth, base plate, studio edge, seamless backdrop, white background, text, letters, numbers, captions, subtitles, watermark, logo, signature, people
```

백업: `gen/v3-leg-02.mp4` → `gen/v3-leg-02.v3a.mp4`, `src/v3-leg-02.mp4` → `src/v3-leg-02.v3a.mp4` (러너가 발주 전에 옮긴다).

#### 결과 · 판정 — **통과**

| 항목 | 값 |
|---|---|
| taskId | `ccf7d6d127b5e4dff7bc9f34e38b18f5` |
| 과금 | **50** (잔액 9,052.5 → **9,002.5**) |
| 호출 | createTask 1회 · 재시도 0 · 회선 끊김 0 (wall ≈ 120 s) |
| 산출 | `gen/v3-leg-02.mp4` 15,241,613 B → 스크럽본 `src/v3-leg-02.mp4` 7,524,230 B (1932×1072 → 1080p, 24 fps, 122 프레임, 5.083 s — 길이 불변) |
| 검수 | 5프레임 `shots/kie/leg02-v4-frames.jpg`(t=0/1.25/2.5/3.75/5) · 960w `landxi/assets/proto/review/leg02-v4.jpg` · 4 fps 20프레임 전수 스캔 |

| 기준 | 판정 | 근거 |
|---|---|---|
| 브라스 링 전 구간 부재 | **통과** | 20/20 프레임에 링·후프·아크·자오선 없음. 옛 레그 02(A02 rev.3b)의 상단을 가로지르던 금색 호가 사라졌다. |
| 위성이 작게 머무름·밀어넣기 없음 | **통과** | 우상 위성 폭 t=0 **≈19 %**(A02 rev.3m 그대로) → t=1.25 ≈17 % → t=2.5 ≈14 % → t=3.75 초점 밖 작은 점 → t≈3.9 프레임 위로 퇴장. **한 번도 커지지 않고** 같은 CAS500-1 형 1기만 유지. |
| 〃 (프롬프트 대비) | **부분** | 프롬프트는 "첫 1초 안에 상단 밖으로"인데 실제로는 ≈3.9 s 에 나간다. 크기·위치는 조건을 지키므로 판정 기준(작게·안 커짐)에는 걸리지 않는다. |
| 하강·틸트다운 연속 | **통과** | 컷·점프·셰이크 없는 한 테이크. 곡률이 t≈3.0 에 평탄해지고 구름 뱅크가 렌즈를 지나 얇아진다. |
| 흰 항공기 / 흰 드론 해상 | **통과** | 항공기(흰색, 금색 아님)가 t≈2.25 우측에서, 드론이 t≈4.0 하단 1/3 에서 "이미 거기 있던 것처럼" 해상된다. |
| A03(rev.3n) 로 이어짐 | **통과** | 씸 프레임 ↔ `A03.jpg` 64×36 gray diff **1.394 %**(기존 통과 레그 1.2–1.9 % 대역). 흰 드론 본체의 초록 "LX", 검정 로터, 흰 항공기, 강·해안선까지 같은 그림. |
| head 정합 | **통과** | t=0 ↔ `A02.jpg`(rev.3m) diff **0.821 %**. |
| 모프·고스트 | 없음 | 물체 복제·잔상·용해 없음. |

### §2-2 레그 03 — A03(rev.3n) → A04 · 드론 · 지리산 서릉 → 남원 분지

호출 전 기록. `model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A03.png`(rev.3n) · tail `anchors-v3/A04.png`(무변경)
· `prompt_sha256 6284029b8b48d7ca` · `negative` 원문 1,052자 → 497자(`trimNeg`, 생략만).

프롬프트(verbatim, A03 `motion_prompt`):

> The basin ahead stays in frame throughout; the warm key holds from upper camera left. The camera drifts slowly and steadily forward and down in one smooth continuous move, the ridgeline in the far haze swelling toward the lower third of the frame as the basin opens out beyond it. The small white drone with black propellers and the small green "LX" letters on its body flies on ahead toward the far right, keeping its white body, black rotors and green "LX" letters unchanged, steadily shrinking into the distance until it passes out of the right edge of the frame. The small white airplane crosses slowly toward the left and passes out of the left edge of the frame, sinking into the warm haze. By the end of the clip only the miniature landscape remains. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

negative(trimNeg 497자):

```
blue sky paint, saturated sky, sun disc, sunburst, stars, city lights, runway, airport, satellite, brass satellite, satellite model, brass ring, brass arc, armature, meridian ring, hoop across the sky, landed, parked, on the ground, contact shadow, giant, oversized, close-up drone, grey drone, dark body, dark grey drone, black body, orange, DJI logo, text, letters, extra drones, two drones, second drone, gold aircraft, brass aircraft, metallic plane, gold plane, brass plane, extra text, board
```

백업: `gen/v3-leg-03.mp4` → `gen/v3-leg-03.v3a.mp4`, `src/v3-leg-03.mp4` → `src/v3-leg-03.v3a.mp4`.

#### 결과 · 판정 — **통과**

| 항목 | 값 |
|---|---|
| taskId | `f56b95e7cbf7b054c1ab88a362e48fb7` |
| 과금 | **50** (잔액 9,002.5 → **8,952.5**) |
| 호출 | createTask 1회 · 재시도 0 · 회선 끊김 0 (wall ≈ 150 s) |
| 산출 | `gen/v3-leg-03.mp4` → 스크럽본 `src/v3-leg-03.mp4` 7,917,590 B (24 fps · 122 프레임 · 5.083 s — 길이 불변) |
| 검수 | `shots/kie/leg03-v4-frames.jpg` · `landxi/assets/proto/review/leg03-v4.jpg` · 4 fps 20프레임 전수 스캔 |

| 기준 | 판정 | 근거 |
|---|---|---|
| 드론 = 흰 본체 / 검정 로터 / 초록 "LX" | **통과** | 유광 흰 본체 + 흰 암, **검정 무광 프로펠러 4**, 본체 측면 **초록 "LX" 글자**, 랜딩 레그. 회색·검정 본체, 오렌지, DJI, 추가 드론 없음 — 시종 1대이고 t=0 → t≈2.6 까지 색·형태 불변. D16 ① 충족. |
| 항공기 = 흰색 | **통과** | 금색·황동 항공기 없음. 흰 모형 항공기 1대. |
| 브라스 링·위성 부재 | **통과** | 20/20 프레임에 링·호·위성 없음(A03 이후 위성은 프레임 밖이 맞다). |
| 전진·하강 연속 | **통과** | 컷·점프·셰이크 없는 한 테이크. 원경 능선이 t≈2.6 에 하단 1/3 으로 부풀고 분지가 그 너머로 열린다. |
| 두 기체 퇴장 · 마지막엔 지형만 | **통과(주석)** | t≈2.75 부터 화면에 미니어처 지형만 남는다. 다만 **퇴장 방향이 프롬프트와 다르다** — 드론은 "우측 끝으로"가 아니라 항공기와 함께 **상단 헤이즈 쪽으로 작아지며** 빠지고, 항공기는 좌측 가장자리를 넘지 않고 좌상 헤이즈에서 사라진다. t≈1.4–1.9 구간에 항공기가 한 번 커진다(근접 통과). 잔상·복제·모프는 없다. |
| A04 로 이어짐 | **통과** | 씸 프레임 ↔ `A04.jpg` diff **1.218 %**. 능선·계단 논·개울 굽이까지 같은 그림. |
| head 정합 | **통과** | t=0 ↔ `A03.jpg`(rev.3n) diff **0.796 %**. |
| 모프·고스트 | 없음 | — |

### §2-3 레그 11 — A11 → A12(rev.3l) · 줌아웃 · 국토 변화 → 모형 지구본 귀환

호출 전 기록. `model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A11.png`(무변경) · tail `anchors-v3/A12.png`(rev.3l)
· `prompt_sha256 4c2dc5071bf2ddf6` · `negative` 원문 901자 → 494자(`trimNeg`, 생략만).

프롬프트(verbatim, A11 `motion_prompt`):

> The clearing and the ridge behind it stay in frame throughout as they shrink; the warm key holds from upper camera left. The camera climbs away in one smooth continuous vertical rise, tilting gently up as it goes, so the ridge country, then the plains, then the whole peninsula gather into the frame and the horizon curves into the gentle arc of a model world; the fine etched brass grid on the land and the small warm points lit in a few of its cells come into view already in place, resolving rather than appearing, and the tiny foil-wrapped satellite comes into view high in the upper right, already in place, resolving rather than appearing. The tiny satellite stays tiny and in place, with no push-in on it and no growth. There is no ring, hoop or arc anywhere. The altitude only increases, never dips. Nothing enters or leaves the frame. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

negative(trimNeg 494자):

```
workers, incinerator building, brass satellite, satellite model, aircraft, airplane, plane model, drone, quadrotor, rotors, brass ring, brass arc, armature, meridian ring, hoop across the sky, wildfire, forest fire, large fire, flames filling frame, orange sky, fire glow across the hillside, smoke filling frame, clearing filling frame, close-up of fire, board, table, plinth, base plate, studio edge, seamless backdrop, white background, text, letters, numbers, captions, subtitles, watermark
```

백업: `gen/v3-leg-11.mp4` → `gen/v3-leg-11.v3a.mp4`, `src/v3-leg-11.mp4` → `src/v3-leg-11.v3a.mp4`.

#### 결과 · 판정 — **통과**

| 항목 | 값 |
|---|---|
| taskId | `8508c5962f511b4eed746c9ffcbd05c1` |
| 과금 | **50** (잔액 8,952.5 → **8,902.5**) |
| 호출 | createTask 1회 · 재시도 0 · 회선 끊김 0 (wall ≈ 105 s) |
| 산출 | `gen/v3-leg-11.mp4` → 스크럽본 `src/v3-leg-11.mp4` 6,821,842 B (24 fps · 122 프레임 · 5.083 s — 길이 불변) |
| 검수 | `shots/kie/leg11-v4-frames.jpg` · `landxi/assets/proto/review/leg11-v4.jpg` · 4 fps 20프레임 전수 스캔 + 2.1–2.8 s 0.1 s 간격 정밀 스캔 |

| 기준 | 판정 | 근거 |
|---|---|---|
| 브라스 링 전 구간 부재 | **통과** | 20/20 프레임 + 전이 구간 8프레임에 링·호·아마추어·자오선 없음. 옛 레그 11 은 t=3 에 "황동 링이 상단에 닫히던" 것이 이번엔 전혀 없다. |
| 위성이 작게·제자리 | **통과** | t≈2.45 상단 우측에서 카메라가 위로 열리며 **해상**된다(프레임 밖에서 들어오는 물체가 아니라 이미 그 자리에 있던 것으로 읽힌다). 날개폭 ≈**19 %** 로 t=2.75 → t=5 내내 거의 불변, 밀어넣기·확대 없음. A12 rev.3l(≈18 %)과 같은 CAS500-1 형 1기. |
| 상승 연속·고도 단조 | **통과** | 컷·점프 없는 한 테이크. 능선 → 평야 → 반도 전체 → 곡률로 **끊김 없이** 이어진다. **이전 v3 판정의 "t≈1.5–2.5 모핑(웅덩이 속 반도)" 이 사라졌다** — 0.1 s 간격으로 봐도 격자가 제자리에서 해상되고 지형이 축소될 뿐이다. |
| 격자·점등 (A12 그대로) | **통과** | 황동 에칭 격자가 t≈1.75 부터 남한 지형 위에 해상되고, 남서 내륙·남해안 점등 셀이 A12 와 같은 자리에 선다. |
| A12(rev.3l) 로 이어짐 | **통과** | 씸 프레임 ↔ `A12.jpg` diff **1.137 %**. 반도 위치·일본 열도·구름·위성 자세까지 같은 프레임. |
| head 정합 | **통과** | t=0 ↔ `A11.jpg` diff **0.856 %**. 공터의 작은 불꽃 두 점·연기 기둥이 t≈1.5 까지 같은 자리. |
| 모프·고스트 | 없음 | — |

### §2-4 레그 12 — A12(rev.3l) → A01(rev.3k) · 귀환 · 작업대로 루프 닫기

#### 발주 전 확인 두 가지

**① 레그 12 는 "재촬영"이 아니라 신규다.** 현재 필름은 **13 레그(01–11)** 이고 레그 12 는 한 번도 구운 적이 없다 —
2026-08-27 `kie-legs-final.md` 에서 *"Leg 12(A12 홀드 + 브랜드 마감)는 생성하지 않는다 — 페이지가 그린다"* 로 결정됐고,
`manifest.finale`(legIndex 12 · zoom 6.6047 · A12 홀드) 위에서 국토 실지도 판이 이어받아 브랜드 마감(LEAD 0.30 + SPAN 1.54 + TAIL 0.16 vh)이 선다.
D18 승인문("Leg 2·3·11·12(200)")대로 **영상은 굽되**, 붙이면 필름이 14 레그가 되고 마지막 프레임이 A12(국토 판)가 아니라
A01(작업대 지구본)이 되어 **승인된 브랜드 마감의 인계 카메라가 무효가 된다**. 그래서 이번 라운드에서는
**02·03·11 만 마운트하고 12 는 굽고 판정만 한다** — 마운트는 마감 재설계와 함께 별도 결정으로 올린다(§2-6).

**② `trimNeg()` 버그를 먼저 고쳤다(발주 전).** A12 의 `negative` 는 `negative_base` 와 마지막 한 항목(`grid lines`)만 다르다
(A12 는 땅에 각인된 황동 격자가 주인공이라 뺐다). 그래서 `s.startsWith(base)` 가 **false** 가 되고, 예전 `trimNeg` 는
앵커 고유 항목을 통째로 버린 뒤 공통 base 앞 500자만 보냈다 — 즉 **링 재발 위험이 가장 큰 레그에서 `ring, hoop, armature,
arc, meridian, orbit line, large satellite …` 부정어가 전부 사라진다.** `tools/kie/legs-v3.mjs` 의 `trimNeg` 에 분기를 추가해
공통 접두를 쉼표 경계까지만 인정하고 그 뒤를 고유 항목으로 잡게 했다. `negative` 가 base 로 시작하는 A01–A11 은 이 가지를
타지 않아 **결과 문자열이 바뀌지 않는다**(A02 489 · A03 497 · A11 494 — §2-1~2-3 과 동일). A12 만 495 → **496자**로 바뀌며
고유 항목이 전부 살아난다.

#### 발주 파라미터 (호출 전 기록)

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head `anchors-v3/A12.png`(rev.3l) · tail `anchors-v3/A01.png`(rev.3k)
· `prompt_sha256 d93371fe3897b276` · `negative` 원문 1,024자 → 496자(`trimNeg`, 생략만).

프롬프트(verbatim, A12 `motion_prompt`):

> The peninsula stays dead centre and in frame throughout, the etched brass grid stays lying on the land with the same cells lit; the warm key holds from upper camera left. The tiny satellite in the upper right stays tiny and in place, holding its size and position with no push-in on it, no zoom toward it and no growth. The camera continues climbing away in one smooth continuous rise, so the surface curls further into the sphere of the model globe, and the dark unlit workbench plane with its single long warm woodgrain highlight comes up softly from the bottom of the frame as the world settles back into the pool of key light where it began. There is no ring, hoop or arc anywhere. The altitude only increases, never dips. Nothing else enters or leaves the frame. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled.

negative(trimNeg 496자):

```
stars, starfield, black space, digital overlay, heatmap, choropleth, glowing lines, screen graphics, rainbow gradient, blue sky paint, aircraft, airplane, plane model, drone, quadrotor, rotors, blue laser, laser beams, light beams, glowing bands, acrylic bands, translucent bands, neon lines, uniform city lights, every cell lit, ring, hoop, armature, arc, meridian, meridian ring, orbit line, string, wire, brass cap, gilded rim, large satellite, oversized spacecraft, close-up spacecraft, board
```

백업: 없음(신규 레그 — 덮어쓸 이전 산출물이 없다).

#### 결과 · 판정 — **클립 자체는 통과 (단, 이번 라운드 마운트 보류)**

| 항목 | 값 |
|---|---|
| taskId | `6fb0f28c5ea893a51ee3d730f86c768c` |
| 과금 | **50** (잔액 8,902.5 → **8,852.5**) |
| 호출 | createTask 1회 · 재시도 0 · 회선 끊김 0 (wall ≈ 135 s) |
| 산출 | `gen/v3-leg-12.mp4` → 스크럽본 `src/v3-leg-12.mp4` 6,393,963 B (24 fps · 122 프레임 · 5.083 s) |
| 검수 | `shots/kie/leg12-v4-frames.jpg` · `landxi/assets/proto/review/leg12-v4.jpg` · 4 fps 20프레임 전수 스캔 + 1.8–3.2 s 0.2 s 간격 정밀 스캔 |

| 기준 | 판정 | 근거 |
|---|---|---|
| 브라스 링 전 구간 부재 | **통과** | 20/20 + 정밀 8프레임에 링·후프·아크 없음. `trimNeg` 수정 덕에 이번엔 링 부정어가 실제로 전달됐다. |
| 위성이 작게·제자리 | **통과** | 우상 위성이 같은 CAS500-1 형 1기로 전 구간 유지. 화면폭 대비 ≈19 %(t=0) → ≈14 %(t=5) — 지구본이 축소되는 동안 위성은 **거의 같은 크기**라 A01 rev.3k(13.6 %) 로 자연스럽게 안착한다. 밀어넣기·확대·복제 없음. |
| 상승 연속·고도 단조 | **통과** | 컷·점프·셰이크 없는 한 테이크. 곡면이 t≈2.4 에 완전한 구로 말리고, 어두운 아웃포커스 워크벤치 판이 t≈2.4 부터 아래에서 부드럽게 올라온다. 판의 잘린 모서리·다리·받침 없음(앵커 규칙 준수) — 구 아래 어두운 것은 접지 그림자이고 A01 과 같다. |
| 격자·점등 유지 | **부분(구조적)** | 격자·점등 셀은 t≈2.6 까지 반도 위 제자리에 있다가 반도가 림으로 물러나며 자연스럽게 읽히지 않게 된다. 프롬프트는 "격자가 계속 놓여 있다"지만 **tail 앵커 A01 에 격자가 없다** — tail 이 강제하는 수렴이다. 급격한 소멸·깜빡임은 없다. |
| "반도가 정중앙" | **부분(구조적)** | 반도가 중앙 → 좌측 림으로 이동한다. 역시 A01 구도(아시아가 좌측 림)로 수렴하기 위한 것이고, 회전은 매끄럽다. |
| A01(rev.3k) 로 이어짐 | **통과** | 씸 프레임 ↔ `A01.jpg` diff **0.991 %** — 네 레그 중 가장 좋다. 워크벤치 나뭇결 하이라이트·접지 그림자·위성 자세까지 같은 그림. 루프가 닫힌다. |
| head 정합 | **통과** | t=0 ↔ `A12.jpg`(rev.3l) diff **0.837 %**. |
| 모프·고스트 | 없음 | — |

### §2-5 마운트 — 02·03·11 만 (`node tools/scrub/assemble.mjs --only 02,03,11`)

레그 12 는 §2-4 ① 때문에 붙이지 않았다. `--only 02,03,11` 로 세 레그만 다시 굽는다(crf 24 / 모바일 27,
`-g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an`).

| 레그 | w·mp4 | 모바일 | 이전(v3a) |
|---|---|---|---|
| 02 | 3,916,350 → **4,585,387 B** | 1,398,907 → **1,408,341 B** | `w02.v3a{,-m}.mp4` |
| 03 | 4,539,953 → **4,794,345 B** | 1,474,745 → **1,582,955 B** | `w03.v3a{,-m}.mp4` |
| 11 | 4,066,866 → **4,095,388 B** | 1,510,015 → **1,474,860 B** | `w11.v3a{,-m}.mp4` |

- 포스터·씸 포스터 `w{02,03,11}.webp` / `-last.webp`, 소스 옆 `src/v3-leg-{02,03,11}.first/.last.webp` 재생성.
- 합계 데스크톱 50.57 → **51.47 MB** (≤60) · 모바일 17.57 → **17.65 MB** (≤20) · 포스터 **1.21 MB**.
- **필름 66.079 s · 트랙 14.404 vh · 스페이서 15.404 vh — 전부 불변**(13 레그 × 5.083 s 동일). 페이스 편차 **0 %**(밴드 0.218–0.218).
  → `landxi/proto/scrub/index.html` 수정 없음. 매니페스트는 레그 바이트·씸 값만 갱신됐다.
- 풀 영상본 재생성: **57.292 s · 1,375 프레임 · 1920×1080 · 41,231,589 B (39.32 MB)**, xfade 0.734 s × 12, crf 25. `full.webp` 동반.

#### 씸 실측 (전 13 씸, 인코딩본 64×36 gray · 씸 법칙 A/규칙 B)

| 씸 | 이전 | 지금 | luma |
|---|---|---|---|
| **01→02** | **16.175 %** | **0.175 %** | 59.31 → 59.49 (+0.18) |
| **02→03** | 0.215 % | **0.191 %** | 69.59 → 69.48 (−0.11) |
| **03→04** | 0.216 % | **0.255 %** | 47.27 → 46.88 (−0.39) |
| 04→05 | 0.267 % | 0.267 % | 57.76 → 57.30 (−0.46) |
| 05→06 | 0.219 % | 0.219 % | 61.77 → 61.71 (−0.06) |
| 06→06b | 0.203 % | 0.203 % | 54.41 → 54.15 (−0.26) |
| 06b→07 | 0.183 % | 0.183 % | 50.01 → 49.77 (−0.24) |
| 07→08 | 0.166 % | 0.166 % | 40.09 → 40.32 (+0.23) |
| 08→08b | 0.153 % | 0.153 % | 38.30 → 38.17 (−0.13) |
| 08b→09 | 0.217 % | 0.217 % | 40.92 → 40.63 (−0.29) |
| 09→10 | 0.173 % | 0.173 % | 41.74 → 41.84 (+0.10) |
| **10→11** | 0.195 % | **0.189 %** | 37.44 → 37.27 (−0.17) |

**전 씸 0.153 – 0.267 %, 전부 PASS.** §1 이 남긴 16.175 % 컷(5.08 s 에서 링이 튀어나오던 자리)이 **0.175 %** 로 닫혔다 —
링 폐기가 필름 전체에서 완결됐다.

#### 테스트

`npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed + 1 skipped (8.2 m)**. 기대값 수정 0.
스킵 1건은 D16 ① 폐기로 이미 비활성인 드론 오버레이 테스트다.

### §2-6 크레딧 · 백업 · 다음

| 레그 | taskId | 과금 | 잔액 |
|---|---|---|---|
| 02 | `ccf7d6d127b5e4dff7bc9f34e38b18f5` | 50 | 9,052.5 → 9,002.5 |
| 03 | `f56b95e7cbf7b054c1ab88a362e48fb7` | 50 | 9,002.5 → 8,952.5 |
| 11 | `8508c5962f511b4eed746c9ffcbd05c1` | 50 | 8,952.5 → 8,902.5 |
| 12 | `6fb0f28c5ea893a51ee3d730f86c768c` | 50 | 8,902.5 → **8,852.5** |

배치 합계 `spent_by_rev["rev4-legs2-3-11-12"] = 200` / 캡 200. **과금 호출 4회(레그당 정확히 1회) · 재시도 0 · 무과금 실패 0 · 회선 끊김 0.**
v4 누계(§1 50 + §2 200) = **250**, 잔액 9,102.5 → 8,852.5.

**백업(v3a).** 되돌리려면 아래를 원래 이름으로 복사하고 `--only 02,03,11` 을 다시 돌린다.
`gen/v3-leg-{02,03,11}.v3a.mp4` · `src/v3-leg-{02,03,11}.v3a.mp4` · `src/v3-leg-{02,03,11}.first/.last.v3a.webp` ·
`w{02,03,11}.v3a{,-m}.mp4` · `w{02,03,11}.v3a{,-last}.webp`. (레그 12 는 신규라 백업 없음.)

**다음 — D19 (검토 허브에 올림).** 레그 12 를 필름에 붙일지. 붙이면 14 레그 · **71.16 s** · 트랙 15.51 vh 가 되고
`manifest.finale`(legIndex 12 · A12 홀드 · zoom 6.6047)과 브랜드 마감의 인계 판을 A01(작업대 지구본)로 다시 잡아야 한다 —
D14 ②(마감 바닥)와 한 묶음이다. 선택지 ① 그대로 ② 붙이고 마감 재설계 ③ 붙이되 마감은 레그 11 끝에 고정하고 12 는 에필로그.

### §2-7 산출물

| 경로 | 내용 |
|---|---|
| `gen/v3-leg-{02,03,11,12}.mp4` · `.seam.png` | kling 원본(v4) 16.78 / 17.56 / 15.63 / 13.89 MB · 씸 프레임 |
| `src/v3-leg-{02,03,11,12}.mp4` (+ `.first/.last.webp`) | 스크럽본(crf 20). **12 는 미탑재** |
| `w{02,03,11}{,-m}.mp4` · `w{02,03,11}{,-last}.webp` · `manifest.json` | 마운트 산출물(13 레그) |
| `full.mp4`(39.32 MB) · `full.webp` | 풀 영상본(crf 25) |
| `shots/kie/leg{02,03,11,12}-v4-frames.jpg` · `v3-leg-{02,03,11,12}-t*.jpg` · `legs-v4-{2,3,11,12}-run.log` | 검수 프레임 · 로그 |
| `landxi/assets/proto/review/leg{02,03,11,12}-v4.jpg` | 리뷰 사본 960w |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(rev 4, batch "2-3-11-12") |
| `tools/kie/legs-v3.mjs` | `trimNeg()` — base 로 시작하지 않는 negative(A12) 처리 수정 |
| `landxi/proto/review/index.html` · `landxi/proto/film/timeline.html` | 씸·잔액 갱신, D19 신설·D14 갱신, 레그 12 미탑재 표시 |
