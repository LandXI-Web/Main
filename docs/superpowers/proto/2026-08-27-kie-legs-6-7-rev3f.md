# v3 Leg 6·7 rev.3f — 재생성 + 스크럽 마운트 기록 (kling/v2-1-pro, 2026-08-27, 배치 6-7-rev3f)

- 클라이언트 결정(원문): **"06, 07은 시작한다. 06b는 06 재생성된걸 보고 판단해야겠다."** → Leg 6(A06 → **A06b 신규 rev.3f**) · Leg 7(A07 → **A08 신규 rev.3f**) 각 1회. **Leg 6b 는 생성하지 않았다**(옛 영상 그대로 마운트).
- 앵커/프롬프트: `tools/kie/anchors-v3.json` `motion_prompt` **토씨 그대로**(A06 "the greenhouse cluster stays the same and stays centre-left…", A07 "dives … until the grey sand strand with its many white polystyrene drifts and buoys fills the frame"), `negative` 는 `trimNeg()` 500자(생략만).
- 러너: `tools/kie/legs-v3.mjs` — `LEGS_V3_REV=3 LEGS_V3_BATCH=6-7-rev3f LEGS_V3_PREV=rev2 LEGS_V3_CAP=110 node tools/kie/legs-v3.mjs run 6` 그리고 `run 7` (레그마다 별도 호출, 앞 레그 완료 확인 후). 1087aa7 의 **pending 줄이 createTask 직후 원장에 적히는 것을 두 레그 모두 확인**(`[leg 6] pending taskId=e90d2238… (원장 기록)` / `[leg 7] pending taskId=8edd57b6… (원장 기록)`, 로그 5초 안). 폴링은 끊기지 않아 `fetch` 는 쓰지 않았다.
- 원장: `shots/kie/legs-v3-credits.json` (`batch:"6-7-rev3f"` 2건) · 로그 `shots/kie/legs-6-7-rev3f-run.log`(leg 6) · `legs-6-7-rev3f-run7.log`(leg 7) · 시트 `shots/kie/v3-legs-6-7-rev3f-sheet.jpg` (2행 × t=0,1,2,3,4,4.85 + 다음 앵커) · 사본 `landxi/assets/proto/review/v3-legs-6-7-rev3f-sheet.jpg`. 이전 산출물은 `gen/·src/v3-leg-0{6,7}.rev2.mp4`.
- 규칙 준수: leg 당 과금 호출 **정확히 1회**, 재시도 0, 실패 0, 추가 스틸 0, 캡 110 중 **100 사용**.

## 결론 (한 줄)

**둘 다 프롬프트가 요구한 카메라를 kling 이 처음으로 그대로 해냈다.** Leg 6 은 온실 군락이 형태·행 배치·불 켜진 두 동까지 그대로인 채 카메라만 오르고 지평선이 t≈2 부터 휜다. Leg 7 은 t≈3 부터 방파제 발치의 흰 스티로폼 더미와 부표가 읽히고 끝 프레임은 A08 과 같은 클로즈업이며 방파제 팔은 끝까지 프레임 상단에 남는다. 값을 치른 것은 옛 6b 와의 씸 — 새 A06b 는 옛 A06b(v3b)와 12.9 % 다르므로 **06→06b 씸이 0.144 % → 12.9 % 컷**으로 돌아갔다(6b 재생성 전까지 임시). 06b→07 은 A07 이 rev.3f 에서 안 바뀌어 0.135 % 그대로.

## 크레딧

| 시점 | 잔액 | 증감 | 비고 |
|---|---|---|---|
| 세션 시작 실측 (05:17 UTC) | 9,667.5 | — | 앵커 rev.3f 2장(29) 이후 |
| leg 6 `e90d2238…` 05:17:12 pending → 05:19:34 ok | 9,617.5 | −50 | kie 보고 gen 138 ms / wall 141.8 s, 원본 15.7 MB |
| leg 7 `8edd57b6…` 05:20:15 pending → 05:23:38 ok | **9,553.0** | −64.5 | kling `creditsConsumed` **50**; 나머지 **14.5 는 폴링 중 다른 세션이 만든 앵커 스틸 `anchors-v3/A08b.png`(14:21:12 KST, 스틸 단가 14.5)** — 이 배치 과금이 아니다. 원장 줄은 `credits_charged:50` + `credits_balance_delta_raw:64.5` + `charge_note` 로 손봤다. wall 203 s, 원본 13.0 MB |

배치 합계 `spent_by_rev["rev3-legs6-7-rev3f"] = 100`, 캡 잔여 10.

## 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head/tail 앵커 png · 출력 1932×1072 / 24 fps / 5.083 s. negative A06 916 → ≤500 · A07 870 → ≤500 자.
스크럽 인코딩: `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an` → `src/v3-leg-06.mp4` 6.4 MB · `src/v3-leg-07.mp4` 6.6 MB. 씸 프레임 `-sseof -0.15` → `gen/v3-leg-0n.seam.png`.

## Leg 6 — A06 → A06b(rev.3f) (남원 상공 수직 상승, 군락 유지)

- taskId `e90d223881a8ff54f5b23ca28acecdd8` · 과금 50
- 프롬프트(verbatim): A06 `motion_prompt` — "The felt-parcel quilt and the greenhouse cluster stay in frame throughout, the top of the frame stays open and empty; the warm key holds from upper camera left. The camera rises in one smooth continuous climb, altitude only increases, the greenhouse cluster stays the same and stays centre-left in frame (it neither shrinks away nor changes), the quilt and river stretch out around it, the ground curls into the arc of a model world, warm haze at the horizon; nothing enters or leaves the frame, nothing moves on the ground. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→4.85 s 상승 하나. t=0–1 군락 정면, t≈2 지평선 곡률 시작(상단이 어두운 호가 된다), t=3–4.85 곡률+헤이즈 띠가 상단 1/4. 컷·점프 없음. |
| 느림 | **통과** | 5초에 걸쳐 고르게. |
| **군락 유지(이번 질문)** | **통과** | 군락의 행 배치·각도·불 켜진 두 동(좌하 큰 동 + 우측 중앙)이 t=0 부터 4.85 까지 같은 물체. 모핑·재배열·유령 없음. 프레임 안 크기는 화면폭 ~65 % → ~45 % 로 줄지만 이것은 상승의 결과이고 "shrink away"(멀리 사라짐)는 아니다 — 끝에도 가장 큰 물체. 위치는 중앙(좌)에 머문다. |
| 아무것도 들어오지 않음 | **통과** | 강·필지·군락 외 없음. 곡률 가장자리에 바다 없음(A06b 신규는 바다 없음 — 옛 6b 앵커와의 차이). |
| 환각 | 없음 | — |
| 씸 → A06b(새) | **양호** | 인코딩 seam 프레임 ↔ A06b.png 64×36 gray diff **1.32 %**(tail 조건부 생성의 통상치). 새 6b 는 여기서 붙는다. |

**종합: 통과.** rev.3e 의 leg 6("군락이 좌하로 빠진다")와 달리 군락이 중심에 남는다 — 06b 재생성 판단 자료로 충분.

## Leg 7 — A07 → A08(rev.3f) (여수 해안 다이브 · 해양쓰레기 클로즈업)

- taskId `8edd57b6e070c9a9bfe88fe1b70a79e4` · 과금 50
- 프롬프트(verbatim): A07 `motion_prompt` — "The breakwater arm and the moored fishing-boat models stay in frame throughout, the cobalt resin water stays still and wakeless; the warm key holds from upper camera left. The camera dives in one smooth continuous descent toward the shore at the foot of the breakwater, dropping fast and steadily and tilting down, until the grey sand strand with its many white polystyrene drifts and buoys fills the frame; the breakwater arm and boats stay in frame at the top and only grow larger; altitude only decreases, never rises. Nothing enters or leaves the frame. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→4.85 s 하강+틸트다운 하나. t=0 곡률 원경(섬·헤이즈), t=1 방파제 팔이 화면폭 1/3, t=2 마을+방파제 1/2, t=3 방파제 발치 자갈 revetment 와 흰 더미 해상, t=4–4.85 A08 클로즈업. 컷·점프 없음. |
| 느림 | **통과** | 빠르지만 고른 가속 — 5초에 배율 ~8×. 스크럽에서 프레임 간 변화가 크지만 방향 하나. |
| **다이브·스티로폼(이번 질문)** | **통과** | t≥3 방파제 발치 모래톱에 흰 스티로폼 조각·둥근 흰/주황 부표가 개별로 읽히고, t=4.85 는 모래톱+더미가 프레임 하단 2/3, 방파제 팔이 상단을 가로지른다. 프롬프트의 종료 조건 그대로. |
| 방파제 잔류 | **통과** | 방파제·어선이 끝까지 프레임 안(상단), 프레임 밖으로 나가지 않는다. |
| 아무것도 들어오지 않음 | **부분(구조적)** | 마을 건물이 t≈1–2.5 에 해상(A07 원경엔 점)되고 t≥3 상단 밖으로 빠진다; 밭·섬도 상단/좌측으로 빠진다. 8× 다이브에서 피할 수 없는 것이고 프롬프트가 요구한 끝 구도의 결과. 프롭·판·배경 없음. |
| 환각 | 경미 1 | t≈0.5–1.5 방파제가 짧은 갈고리 → 긴 직선으로 뻗는 모핑(rev.3e 와 같은 A07↔A08 형상 차이의 대가), 이번엔 배율 변화에 묻혀 rev.3e 보다 덜 보인다. |
| 씸 → A08(새) | **양호** | seam 프레임 ↔ A08.png diff **1.10 %**. |

**종합: 통과.** rev.3e 의 "끝 고도 2–3 km, 스티로폼 안 보임"이 A08 리롤 한 장으로 해결됐다 — "종료 조건은 숫자 대신 앵커에 맡긴다"(kie-leg-7.md 다음 라운드 2) 가 맞았다.

## 씸 실측 (assemble.mjs --only 06,07, 인코딩본 64×36 gray)

| 씸 | 이전(rev.3e) | 지금 | luma | 비고 |
|---|---|---|---|---|
| 05→06 | 0.168 % | **0.176 %** | 61.8→61.7 | 새 06 head = A06(불변). |
| **06→06b** | 0.144 % | **12.895 %** | 54.3→49.6 (−4.7) | **임시 컷** — 새 06 끝(A06b rev.3f: 군락 중앙, 바다 없음) ↔ 옛 6b 첫 프레임(A06b v3b: 군락 좌하, 우하 바다). A06b 새↔옛 앵커 diff 12.88 % 와 같다. 씸 밴드 0.16vh 크로스페이드가 삼키지만 군락이 튄다. **6b 재생성으로만 사라진다**(새 06 끝 ↔ 새 A06b 1.32 %). |
| 06b→07 | 0.133 % | **0.135 %** | 49.8→49.76 | A07 은 rev.3f 에서 안 바뀌어 옛 6b 끝 ↔ 새 07 head 그대로 붙는다. 미스매치 없음. |

전 씸 0.13–0.20 % (06→06b 제외). 페이스 0.218 편차 0 %. 데스크톱 51.34 MB / 모바일 13.58 MB. 필름 40.66 s · 트랙 8.864 vh (불변).

## 스크럽 마운트

- PLAN 07 끝 카메라: **2.4 km/피치 40 → 0.3 km/피치 50**(A08 신규: "약 50°, 수십 m 상공" — 계기판 고도는 MapLibre 카메라 환산값이라 0.3 km), 좌표는 국동항 방파제 발치(127.7305, 34.5630) 그대로. 06 카메라(52.4 → 140 km, 남원 상공)는 불변 — 새 A06b 도 같은 고도대.
- `handoffFinal`: `legIndex` 7 · center 필름 끝 그대로 · **zoom 18.824 / pitch 50 / altitudeM 300**(이전 15.82 / 40 / 2400). `dzFor(18.824)`: room 0.804 ≥ 0.35 → dz 0.621, floor 18 불변(e2e 계약). 수축 끝 z18.2 의 1440×900 프레임은 lon ±0.002° — V-World 모자이크 공백과 더 멀다. V-World maxzoom 19 안.
- `landxi/proto/scrub/index.html`: 인계 카피 `ALT 2.4 km → 0.3 km`. 07 카피·브래킷(항공 1,857건 · 드론 2,078건 · 8종)·창 불변. 세그먼트 폭 불변(1.108).
- 타임라인 `landxi/proto/film/timeline.html`: 불변(07 여전히 AI, 5.08 s).

## 테스트 · 촬영

- `npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed (4.6 m)** (`shots/scrub/e2e-legs-6-7-rev3f.log`). 기대값 수정 0 — `M.legs.length` 8 · 인계 카메라(manifest 그대로 z18.82) · 마감 판 floor(zoom) 18 불변, 수축 ×0.65 전부 통과.
- `node tools/scrub/shoot-strip.mjs --legs 6-7 --prefix legs-6-7-rev3f` → `shots/scrub/legs-6-7-rev3f-{1..9}-*.png`, 몽타주 `legs-6-7-rev3f-montage.jpg`, 콘솔/페이지 오류 0.
  - seam 05→06 mid(52 km): 남원 인계 판 위, 새 06 head 는 A06 그대로.
  - L06-mid(필름 2.53 s / 85 km): 군락이 화면 중앙에 형태 그대로, 06 카피 `땅이 다시 지구본으로 말린다`.
  - **seam 06→06b mid(140 km): 이중상** — 새 06 끝(군락 중앙)과 옛 6b 첫 프레임(군락 좌하+바다)이 op 1/0.5 로 겹쳐 군락이 두 개로 보인다. 위 씸표의 임시 12.9 % 컷이 실제로 보이는 모습. 6b 재생성 전까지.
  - L06b-mid(49 km): 옛 6b 헤이즈 디졸브 프레임, 불변. seam 06b→07 mid(17 km): 같은 프레임(0.135 %).
  - L07-mid(필름 2.53 s / 계기 2.28 km): 방파제·마을·어선 확대 중, 07 카피 + 브래킷 `해양쓰레기 항공 1,857건 · 드론 2,078건 · 8종`.
  - handoff-07-in(필름 3.2 s / 1.34 km): 방파제 발치 자갈·흰 더미가 이미 읽힌다. handoff-07-mid/end(300 m): 인계 카피 `ALT 0.3 km` 위 V-World 위성 판이 z18.8 로 켜진다 — 국동항 발치의 V-World 타일은 이 배율에서 부드럽고 세부가 적다(바다+방파제 콘크리트만). 계기 `300 m · 27.8 cm/px`. 모자이크 공백 없음.

## 다음 라운드에 반영할 것

1. **6b 재생성 판단**: 새 06 끝은 새 A06b 에 1.32 % 로 붙고, 옛 6b 첫 프레임과는 12.9 % 컷. 6b 를 새 A06b→A07(rev.3e 프롬프트: 군락이 좌하로 빠지며 바다가 우하에서 올라온다)로 1회 재생성하면 06→06b 도 0.2 % 대로 돌아온다. 06b→07 은 이미 붙어 있으므로 6b 재생성으로 손해 볼 씸은 없다.
2. A07↔A08 방파제 모핑(t≈0.5–1.5)은 남는다 — 없애려면 A07 방파제를 직선형으로 리롤(별도 결정).
3. 07 끝이 0.3 km 라 여수 인계 판이 z18.8 위성 타일로 켜진다 — 국동항 발치의 V-World 해상도는 충분하지만 검출 점(38k 피처)이 이 프레임엔 몇 개만 든다. 브랜드 마감 수축(−0.62 z)도 z18.2 안. 검출 밀도가 아쉬우면 인계 center 만 방파제 안쪽으로 옮긴다(dz 불변).

## 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-0{6,7}.mp4` (+ `.rev2.mp4`) · `.seam.png` | kling 원본(rev.3e 백업) · 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-0{6,7}.mp4` (+ `.first/.last.webp`, `.rev2.mp4`) | 스크럽본 |
| `landxi/assets/proto/film/legs/w0{6,7}{,-m}.mp4`, `w0n{,-last}.webp`, `manifest.json` | 마운트 산출물(8 레그, 06b 는 옛 영상) |
| `shots/kie/v3-legs-6-7-rev3f-sheet.jpg` · `v3-leg-0{6,7}-t*.jpg` · `legs-6-7-rev3f-run.log` · `legs-6-7-rev3f-run7.log` | 시트 · 프레임 · 로그 |
| `landxi/assets/proto/review/v3-legs-6-7-rev3f-sheet.jpg` | 리뷰 사본 |
| `shots/scrub/legs-6-7-rev3f-*.png` · `legs-6-7-rev3f-montage.jpg` · `legs-6-7-rev3f-strip.json` · `e2e-legs-6-7-rev3f.log` | 스크럽 실촬영 · e2e 로그 |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(batch 6-7-rev3f 2건) |
| `tools/scrub/assemble.mjs` · `landxi/proto/scrub/index.html` | 07 끝 카메라 0.3 km/피치 50 · 인계 카피 |
