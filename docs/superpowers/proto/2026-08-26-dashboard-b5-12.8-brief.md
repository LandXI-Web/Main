# 대시보드 구현 브리프 — B5 12.8 (2026-08-26, 발주자 "일단 대시보드 적용하자")

## 한 줄
`landxi/proto/dashboard.*`(현재 B2 "지도 위 원장" 조판)를 **B5 12.8 원판**으로 재조판한다. 기능은 원본 1:1(레일 A1–A11 · 위젯 B1–B16), 새 위젯 0. 판 하나 = 대한민국 전도 위 0.25° 그리드 + 토글 2 + 등급 범례 + 셀 호버 콜아웃.

## 법전(이것만 읽고 짠다)
1. `design/system.md` — THE SYSTEM. 서체 3 · 색 5+액센트 · 좌표 · 모션 · 콘티 원칙 · 3-비평가 게이트.
2. **픽셀 기준 템플릿**: `design-canvas/v2/B5-Dashboard.dc.html` (AI 분석 결과 선택 상태) + `design-canvas/v2/B5-Dashboard-Data.dc.html` (학습데이터 선택 상태). 렌더 `design-canvas/v2/renders/B5-Dashboard.png`, `B5-Dashboard-Data.png` (1440×900). 좌표·폰트 크기·헤어라인 위치는 이 파일의 인라인 style 값을 **그대로** 옮긴다(반올림 금지).
3. 조판 근거: `design-canvas/v2/NOTES.md` §12.4(밴드·폰트 실측) + §12.8(판 내부: 토글·그리드·등급·범례·콜아웃). §12.5–12.7은 폐기본 — 읽지 말 것.
4. 기능 대조표: `docs/superpowers/proto/2026-08-26-dashboard-parity.md` (A1–A11 / B1–B16). 원본 https://mini531.github.io/namwon-smart-village/landxi7/dashboard.html

## 조판 (12.4 그대로 — x 128–1384, 마진 56)
`0–64` 마스트헤드(B3 공지 + B2 기준일 `기준일 현재 2026.08.26`) → `92–132` B1 제목 SUIT 32 + 부제 15 → hl 156 → `174–278` KPI 띠 B4·B5·B6·B7·B8(라벨 12 / 값 Inter 56 / 부제 12, `|` 헤어라인 4, 카드 0) → hl 302 → `326–694` EVIDENCE-PAIR 1:3 — 좌 572: B9 백본 헤더(`AI 기반 모델 (백본)` 16 + `XI-VFM v2.1` 24 + 최종 적용 줄) + **판 572×254 (y 378–632)** + 캡션 644 + 출처 줄 676 / 우 648: ① B10 랭크드 바 10px×5(1위만 `#006DF7`, 2–5위 `#CCC`) ② B11 7일 폴리라인 36 + 값·요일 12 ③ B12 스택 바 16 + 범례 12, 각 단 출처 줄 + hl → hl 708 → `720–826` B13 승인 대기 원장(캡션 16 · 헤더 12 · 행 28 · 13.5) → `840–868` B14 CHIP-RAIL → hl 866 → B15 푸터 + Family Site.
- 좌 레일 72(B4 계측 글리프, 선택 항목 잉크). 액센트 `#006DF7`은 **B10 1위 바 1곳뿐**(grep 1). 앰버 0 · radius/shadow/gradient/backdrop 0. 청록 `#0FA9A0`은 판 안 셀 + 콜아웃 라벨에만.
- 지도 위젯 0(판은 위젯이 아니라 B9의 증거 자리). 12.4 캡션·출처 문구는 §12.8 값으로.

## 판 내부 (구현 사양)
- **베이스**: MapLibre GL, 위성만(EOX Sentinel-2 cloudless 2024 raster, `landxi/proto/js/sources.js` 참조). 라벨·벡터 스타일 없음. 초기 뷰 = 원판 캡처 bounds `[119.536, 33.0, 135.964, 38.9]`(판 2.25:1 비율에 맞춘 fitBounds, 회전·피치 0). 상호작용: 휠 줌·드래그 끄기(판은 계기판, 탐색은 XI맵). `interactive:false` + 셀 hover/click 만.
- **그리드 레이어**: 0.25° 셀 GeoJSON(124–131 / 33–39, 코드 생성). 그래티큘 선: 1° `rgba(255,255,255,.3)`, 0.25° `.13`.
- **셀 등급 계산(a)**: 손 값 금지. `RESULTS`(`landxi/assets/data/results.js`)의 각 결과 bbox(GeoJSON: `assets/data/geo/results/*`, `marine-debris*.geojson`, `namwon-change*.geojson`, `jeju-illegal.geojson`; bbox 없으면 결과의 `extent`/`aoi` 필드) ∩ 셀 → `ai = 겹치는 결과 건수`. `IMAGERY`(`imagery.js`)의 각 시점 bounds ∩ 셀 → `data = 겹치는 영상 시점 수`. 울주 = `조사 예정 · 시연` 셀 1개(점선, 129.0–129.25 / 35.5–35.75) — 서비스 라인업의 예정 항목이므로 `시연` 태그.
- **토글 2**(판 좌상 12,12): 세그먼트 `[AI 분석 결과] [AI 학습데이터 구축 현황]`, 라운드 0, 흰 헤어라인, 선택 = 흰 채움·잉크 글자. 전환 시 fill 레이어의 `fill-color/opacity` 표현식만 교체(레이어 재생성 금지). 키보드 접근(role=radiogroup, 좌우 화살표).
- **등급 채움**: AI = 청록 `rgba(15,169,160, .82 / .55 / .3)` (3건+ / 2건 / 1건), 결과 0·영상 있음 = 빈 테두리 `rgba(255,255,255,.45)`. 데이터 = 흰 `.78 / .5 / .26` (4시점+ / 2–3 / 1). 테두리 1px(AI 청록 / 데이터 흰). 점선 = 예정.
- **범례**(판 우하 12,10, 흰 12px): 헤더 `그리드 0.25° · 청록 진하기 = 결과 건수`(모드별 문구) + 등급 행 + 예정 행. **셀 수는 계산값**을 찍는다.
- **호버(셀)**: 흰 브래킷 4(팔 5) + 셀 중앙 5px 점 + 직각 리더 → 토글 아래(12,48) 콜아웃 244px(흰 바탕·잉크 1px·라운드 0). 내용 = 그 셀에 겹치는 결과/영상을 데이터에서 뽑아 §12.8 콜아웃 문구 형식으로: AI = `결과 n건` + 결과명·수치 줄들 / 데이터 = `n시점 · 센서` + 시점·GSD 줄 + `라벨 연결` 줄(연결이 데이터에 없으면 쓰지 않는다 — 0으로 지어내지 않음). 원판의 남원 콜아웃과 같은 셀(127.25–127.50 / 35.25–35.50)에서 같은 문구가 나와야 한다.
- **(b) 확대 없음**: 판 호버 확대는 하지 않는다(유보로 남김). **(c) 제주**: `jeju-illegal.geojson`이 결과로 잡히면 셀에 넣고, 발행 카드 8건과 매핑되지 않으면 콜아웃에 `추정` 태그.
- **클릭 → XI맵**: 셀 클릭 시 `ximap.html?bbox=<셀 bbox>`로 이동(원본의 지도 서비스 진입과 동일 레지스터). 링크 대상이 아직 없으면 `href`만 두고 e2e에서 href 형식만 검사.
- 판 4모서리 흰 브래킷 14(12.4) 유지.

## 콘티 원칙
지어낸 운영 서사 금지(담당자명·대기 일수·추세 문구 0). 수치는 `results.js / imagery.js / models.js / dashboard.js` 실측만. `시연`·`추정` 태그는 12.4 규칙(태그 없음 = 측정 · 시연 = 운영 집계 시드 · 추정 = 우리가 연결한 값). 푸터 콜로폰 그대로.

## 파일
- 수정: `landxi/proto/dashboard.html`, `dashboard.css`, `dashboard.js`, `db-plate.js`(판 로직 교체 — 기존 Ⅰ/Ⅱ/Ⅲ 등급 판은 12.8 판으로 대체), 필요 시 `db-data.js`(셀 집계 함수 추가: `cellsFor(step)` → `{lon,lat,ai,data,results:[],imagery:[]}[]`), `db-charts.js`(3단 차트 12.4 규격), `db-strip.js`(쓰지 않으면 삭제).
- 로그인 관문·레일·푸터·`v3-tokens.css`는 그대로. `login.html?next=dashboard.html` 관문 유지.
- 테스트: `tests/e2e/proto-dashboard.spec.mjs` 재작성 — 관문 / 레일 순서·활성 / KPI 5값 / B10 1위만 액센트(grep `#006DF7` 계산값 1) / 토글 전환 시 fill 표현식 변경·범례 문구 변경 / 남원 셀 호버 콜아웃 문구(원판과 동일) / 범례 셀 수 = 집계값 / 클릭 href bbox / radius·shadow·gradient 0 / 콘솔 오류 0. `tests/unit/`에 셀 집계 단위 테스트(bbox ∩ 셀, 경계값).
- 실행: `PORT=4186 node tools/serve.mjs`, `PLAYWRIGHT_BROWSERS_PATH=E:/Land-XI 플랫폼/_env/ms-playwright`, `npx playwright test tests/e2e/proto-dashboard.spec.mjs --workers=1`, `npm run test:unit`. `build/`·GDAL·원천 데이터는 이 PC에 없다 — 커밋된 `landxi/assets`만 쓴다. 네트워크는 됨(EOX·unpkg).

## 보고 전 게이트 (system.md §7)
1. **나란히 비교**: 구현 1440×900 스크린샷(로그인 플래그 세팅 후 `dashboard.html`) vs `renders/B5-Dashboard.png`, 그리고 토글 전환 후 vs `B5-Dashboard-Data.png` — `shots/proto/dashboard-vs-B5.png`(좌 원판 / 우 구현) 2장 제출. 밴드 y·폰트 크기·헤어라인 위치 편차 ≤ 2px.
2. 3-비평가(Brief / System / Craft) 자체 통과 기록.
3. `npm test` 전체 녹색(다른 spec 깨뜨리지 않기).
4. 커밋(브랜치 `plan1-foundation`, 20분마다 WIP 커밋, `git reset`/`--amend` 금지). 푸시는 하지 않는다(PAT 없음).

## 보고 형식
파일 목록 · 비교 스크린샷 경로 2개 · 테스트 결과 수치 · 원판과 다르게 한 곳(있으면 이유) · 유보 목록. 그 외 서술 금지.
