# v3 Leg 8·8b — 생성 + 스크럽 마운트 + 풀 영상본 (kling/v2-1-pro, 2026-08-27, 배치 8-8b)

- 클라이언트 결정(원문): **"디오라마 D11을 진행한다. 그리고 필름타임라인 한 공간에 풀 영상본도 만들어 놓는다."** → Leg 8(A08 신규 → **A08b**) · Leg 8b(A08b → A09) 각 1회 과금, 그리고 `legs/full.mp4`.
- 앵커/프롬프트: `tools/kie/anchors-v3.json` `motion_prompt` **토씨 그대로**(A08 "The camera rises in one smooth continuous climb … the shore and breakwater fall away below-left and stay in frame shrinking, the cobalt sea widens, the ground curls into the arc of a model world", A08b "The camera slides north-east along the curved surface and descends gently toward the moss ridges, the sea receding behind and to the left, terraced fields and ridgelines coming up beneath"). `negative` 는 `trimNeg()` 500자(생략만).
- 러너: `tools/kie/legs-v3.mjs` — `LEGS_V3_REV=3 LEGS_V3_BATCH=8-8b LEGS_V3_CAP=110 node tools/kie/legs-v3.mjs run 8` 그리고 `run 8b` (레그마다 별도 호출, 앞 레그 완료 후). 14앵커 체인(`A08 → A08b → A09`)을 `tail` 로 짝지어 `v3-leg-08` / `v3-leg-08b` 로 이름 붙는 것을 확인. pending 줄이 createTask 직후 원장에 적히는 것 두 레그 모두 확인(`[leg 8] pending taskId=9df3ab83…`, `[leg 8b] pending taskId=b85c624f…`). 폴링은 끊기지 않아 `fetch` 는 쓰지 않았다.
- 원장: `shots/kie/legs-v3-credits.json` (`batch:"8-8b"` 4줄 — ok 2 + 무과금 실패 2) · 로그 `shots/kie/legs-8-8b-run8.log` · `legs-8-8b-run8b.log` · 시트 `shots/kie/v3-legs-8-8b-sheet.jpg` (2행 × t=0,1,2,3,4,4.85 + 다음 앵커, `git add -f`) · 사본 `landxi/assets/proto/review/v3-legs-8-8b-sheet.jpg`.
- 규칙 준수: leg 당 과금 호출 **정확히 1회**, 과금 재시도 0, 캡 110 중 **100 사용**(잔여 10).

## 결론 (한 줄)

**둘 다 통과.** Leg 8 은 방파제 발치 클로즈업에서 한 번의 상승으로 A08b(항구가 좌하 구석, 곡률)에 닿고, Leg 8b 는 곡면을 따라 북동으로 미끄러지며 여수 방파제가 좌하로 빠지고 산릉·계단밭이 올라온다. **A09 의 마을·방파제는 t≈2.5 부터 좌하에 새로 "자라 들어온다"**(여수 방파제가 t≈1.8 에 프레임 밖으로 나간 뒤 — 같은 물체가 아니다). 씸은 07→08 0.128 % · 08→08b 0.140 %. 스크럽은 07 → **여수 인계 판** → 08 → 08b 순으로 이어지고, 여수 판은 08 씸 1/4 에서 닫힌다.

## 크레딧

| 시점 | 잔액 | 증감 | 비고 |
|---|---|---|---|
| 세션 시작 실측 | 9,553 | — | rev3f 배치 끝(9,553.0) 그대로 |
| leg 8 `9df3ab83…` 07:38:50 pending → 07:44:46 ok | 9,503 | −50 | gen 343 ms 보고 / wall 356 s, 원본 12.7 MB |
| leg 8b 시도 1·2 07:45 | 9,503 | **0** | `createTask` 500 "Input exceeds maximum length" 2회 — **무과금 확인 후** 러너 내부 1회 재시도(규칙 그대로), 둘 다 원장에 `ok:false, credits_charged:0` |
| leg 8b `b85c624f…` 07:46:36 pending → 07:48:42 ok | **9,453** | −50 | trimNeg 수정 후 1회. gen 113 ms / wall 126 s, 원본 16.0 MB |

배치 합계 `spent_by_rev["rev3-legs8-8b"] = 100`, 캡 잔여 10.

### trimNeg 버그(무과금)

A08b 의 `negative` 는 1,159자이고 **앵커 고유 항목만 573자**라 `negative_base` 를 잘라도 500자 안에 들지 않았다(이전 앵커는 고유 항목이 전부 300자 이하). `tools/kie/legs-v3.mjs trimNeg()`: 고유 항목이 500자를 넘으면 고유 항목도 뒤에서 쉼표 경계로 자르고, base 가 한 항목도 못 들어가면 붙이지 않는다(끝 `, bo` 같은 잘린 단어 방지). 여전히 **생략만** 한다 — 단어를 더하거나 바꾸지 않는다. A08b 최종 495자, A08 490자.

## 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head/tail 앵커 png · 출력 1932×1072 / 24 fps / 5.083 s.
kie post 스크럽 인코딩 `src/v3-leg-08.mp4` 5.4 MB · `src/v3-leg-08b.mp4` 7.1 MB. 씸 프레임 `-sseof -0.15` → `gen/v3-leg-08{,b}.seam.png`.

## Leg 8 — A08 → A08b (여수 방파제 발치 → 수직 상승)

- taskId `9df3ab8308c5a65a22cff722404a71b7` · 과금 50

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과(주석)** | 0→4.85 s 상승 하나, 컷·점프 없음. 다만 **t≈1.0–2.5 에 카메라가 상승하며 요(yaw)를 함께 돈다** — 방파제 팔이 t=1 의 좌상→우하 대각선에서 t=2 에 거의 수평, t=2.5 에 좌하로 뻗는 방향으로 돌아간다. "shore and breakwater fall away below-left" 를 kling 이 회전으로 풀었다. 스크럽에서는 배율 변화에 묻히지만 풀 영상에서는 보인다. |
| 느림 | **통과** | 고른 가속, 5초에 배율 ~15×(수십 m → 수 km). |
| 방파제·해안 좌하로 축소 잔류 | **통과** | t=3 부터 방파제가 좌하 1/4 에 작게 남고 t=4–4.85 는 A08b 와 같은 자리(좌하 구석, 배가 점). 프레임 밖으로 나가지 않는다. |
| 바다 확장·곡률 | **통과** | t=2.5 부터 상단에 헤이즈 띠와 지평선 호, t=3 에 코발트 바다가 화면 좌측 절반, 곡률이 A08b 와 같은 방향(좌상 호). |
| 아무것도 들어오지 않음 | **부분(구조적)** | t≈1.5–2.5 에 항구 마을(적갈색 지붕)·계단밭이 상단에서 해상돼 들어온다 — 15× 상승의 결과이고 A08b 의 "low ochre harbour buildings … faint cluster" 그대로. 프롭·판·배경 없음. |
| 환각 | 경미 1 | 위 요 회전과 함께 t≈2 에 방파제 뿌리 쪽 콘크리트 블록이 잠깐 크게 읽힌다(t=1.5 프레임) — 이후 사라진다. |
| 씸 → A08b | **양호** | 인코딩 seam 프레임 ↔ A08b.png 64×36 gray diff **0.97 %**. |

**종합: 통과.** 06 과 같은 상승 문법이 여수에서도 됐다. 요 회전이 거슬리면 A08 프롬프트에 "no rotation, camera heading fixed" 를 더해 리롤(별도 결정).

## Leg 8b — A08b → A09 (곡면 따라 북동 → 울주 산릉)

- taskId `b85c624f02d267943badb2a99ad65427` · 과금 50

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→4.85 s 북동 슬라이드+완만 하강 하나. t=0 A08b, t=1 방파제가 좌하 가장자리로, t=2 산릉이 상단에 겹겹이, t=3–4.85 A09 구도. 컷·점프 없음. |
| 느림 | **통과** | 일정한 슬라이드, 고도 변화 작다(프롬프트 "descends gently"). |
| 북동 슬라이드·바다 좌측으로 후퇴 | **통과** | 바다가 t=0 좌측 2/3 → t=3 좌하 1/3, 우상으로 좁아지며 A09 의 "sea fills lower left, narrows to upper right" 그대로. |
| 능선·계단밭 올라옴 | **통과** | t=2 부터 이끼 능선이 상단 헤이즈 속에서 겹겹이 올라오고 계단밭 필지가 중앙에 해상된다. t=4 능선 안쪽 라이팅·헤이즈가 A09 와 같다. |
| **A09 의 마을·방파제 "grow in"?** | **그렇다 — 기록** | 여수 방파제는 t≈1.8 에 좌하 프레임 밖으로 나간다. 그 뒤 **t≈2.5 부터 좌하에 새 작은 항구(짧은 방파제 + 마을 군락)가 해상돼 자라 들어온다** — A09 앵커 자체가 좌하에 마을·방파제를 갖고 있어서 tail 조건이 만든 것이다. 여수 방파제와 이어진 물체가 아니라 다른 항구로 읽힌다(모핑 없음, 위치도 다르다). 09 앵커 리롤(방파제 없는 A09)로만 없앨 수 있고, 지금 컷에서는 "울주 해안의 다른 항구"로 자연스럽다. |
| 아무것도 들어오지 않음 | **부분(구조적)** | 위 항구 + 능선(상단). 프롭·판·배경 없음. |
| 환각 | 없음 | — |
| 씸 → A09 | **양호** | seam 프레임 ↔ A09.png diff **1.38 %**. |

**종합: 통과.**

## 씸 실측 (assemble.mjs 전체 재빌드, 인코딩본 64×36 gray)

| 씸 | 이전 | 지금 | luma | 비고 |
|---|---|---|---|---|
| 06b→07 | 0.135 % | 0.142 % | 49.75→49.76 | 데스크톱 crf 20 → 22 재인코딩 차이 |
| **07→08** | — | **0.128 %** | 40.19→40.28 (+0.09) | 07 끝 = A08 = 08 head. |
| **08→08b** | — | **0.140 %** | 38.33→38.15 (−0.18) | 08 끝(0.97 %) ↔ A08b ↔ 08b head. |
| 06→06b | 12.895 % | 12.876 % | 54.36→49.62 | 임시 컷 그대로(6b 미재생성). |

전 씸 0.13–0.24 % (06→06b 제외). 페이스 0.218 편차 0 %. 필름 **50.83 s** · 트랙 **11.08 vh** · 스페이서 12.08 vh.

### 데스크톱 예산

10 레그 × src(crf 20) = **63.29 MB > 60 MB**(worldflight 예산, e2e 검사). `tools/scrub/assemble.mjs` 가 AI 레그의 데스크톱본을 src 복사 대신 **kling 원본(gen)에서 같은 GOP 규격·crf 22** 로 다시 굽게 바꿨다(src/ 는 kie post 산출물 그대로). 결과 **48.65 MB** / 모바일 16.39 MB / 포스터 0.93 MB. 씸 diff 는 0.02–0.04 %p 움직였다(위 표).

## 스크럽 마운트 (07 → 여수 인계 판 → 08 → 08b)

- `tools/scrub/assemble.mjs` PLAN: **08** `wp 여수 · 상승` — a = 07 끝(127.7305, 34.5630 · 0.3 km / 피치 50), b = (127.74, 34.57 · **6.0 km** / 피치 60 / 방위 20) (A08b "several thousand metres, ~60°"). **08b** `wp 울주 · 울주 이동` — a = 08 끝, b = **울주 산지 (129.15, 35.55 · 4.5 km / 피치 65 / 방위 −40)** (A09 "several thousand m, ~65°"). 좌표는 먼 이동이라 계기는 중간에서 컷(scrub.js JUMP). 전부 authored.
- **여수 인계 판은 07 끝에 그대로**: `manifest.handoffFinal.legIndex` 가 `legs.length-1` 에서 **`'07'` 의 index(7)** 로. `landxi/proto/scrub/scrub.js` 는 legIndex 가 마지막 레그가 아니면 남원 판과 같은 문법으로 밴드를 닫는다 — `BAND_Y = [07 끝 −0.28vh, 07 끝 +씸/4]`, `gate(PLATES[1], t 가 밴드 안)`. 필름은 판 뒤로 이어진다(strip 5 `handoff-07-end`: 판 위에 08 이 op 0.5 로 올라온다).
- 브랜드 마감(ending.js)은 그대로 `PLATES[1]`(여수 지도)의 줌으로 수축을 잰다 — 필름 끝(08b, 울주 능선)에는 판이 없어 **수축은 숨은 지도 위에서 재고 화면에는 08b 마지막 프레임이 붙잡힌다**(e2e 계약 zoom −dz 통과). 임시 — 레그 09–12(귀환)가 들어오면 마감 판의 카메라를 다시 정한다.
- `landxi/proto/scrub/index.html`: 세그먼트 08(`waypoint 여수`) · 08b(`waypoint 울주`) 추가. 트랙이 8.864 → 11.08 vh 라 **모든 카피 창 ×0.8** (02 `0.110 0.192` … 07 `0.710 0.764`). 새 카피: `인계 · 여수` 슬림(0.777–0.802, `ALT 0.3 km · 검출 1,857건`) · `08 · 상승 · 여수 상공 — 물가에서 다시 / 곡면 위로` (0.808–0.892) · `08b · 지구본 이동 · 여수 → 울주 — 곡면을 따라 / 동쪽 산릉으로` (0.908–0.955). 피날레 카피(0.958–1.0)는 운(w01 이미지)·CTA 를 두고 문구만 `다음 · 울주 산림 → 귀환 / 필름은 여기서 멈추고 지도는 계속된다 / 울주 산지 · 129.1500, 35.5500 · ALT 4.5 km · 레그 09–12 예정` 으로. 항로 레일 6지점(울주 점등은 08b).
- 타임라인 `landxi/proto/film/timeline.html`: 08 · 08b 행 → AI(w08/w08b.mp4), 09–12 시작 시각 밀림(50.83 s~), 헤더 10 leg / 50.83 s, **맨 위 "풀 영상" 섹션**.

## 풀 영상본 (PART B)

- `tools/film/full.mjs`(신규) — `manifest.legs` 순서로 `w{id}.mp4` 10개를 ffmpeg `xfade=fade` 로 잇는다. 크로스페이드 폭 = 씸 밴드 0.16 vh ÷ 0.218 vh/s = **0.734 s**(× 9). scale/crop 1920×1080, fps 24, libx264 high crf 20, GOP 8, faststart, 무음. 포스터 `full.webp`(첫 프레임 960px). 결과를 `manifest.full` 에 적는다. `XFADE=0` 이면 하드컷 concat.
- `tools/scrub/assemble.mjs` 가 매니페스트를 쓴 뒤(`--verify` 아니면) 이 스크립트를 부른다 → 레그가 바뀌면 같이 재생성.
- **`landxi/assets/proto/film/legs/full.mp4` = 44.21 s · 1061 프레임 · 1920×1080 · 52.9 MB**(레그 합 50.83 s − 0.734 × 9). `full.webp` 30 KB.
- 타임라인 페이지: `http://localhost:4173/landxi/proto/film/timeline.html` — 상단 `<video controls>` + `10 leg · 44.21 s (레그 합 50.83 s, 씸 크로스페이드 0.734 s × 9) · 1920×1080 · 52.9 MB`, 그 아래 레그별 행.

## 테스트 · 촬영

- `npx playwright test tests/e2e/proto-scrub.spec.mjs` → **3 passed + 1 failed(6.6 m, `shots/scrub/e2e-legs-8-8b.log`)** → 브랜드 마감 테스트만 고쳐 재실행 **1 passed(`e2e-legs-8-8b-ending.log`)** = 4/4. 실패 원인: 시작 줌 표본을 e=0.030 에서 잤는데 수축(비트 C)이 e=0.0286 에 이미 시작돼 지도가 0.0054 물러나 있었다(허용 0.005) — 트랙 11.08vh 의 px 반올림이 e 를 0.0004 밀었다. 표본을 C 앞(e=0.020)으로 옮겼다(pre-existing fragility). 그 밖의 기대값 수정(전부 트랙 변화에 따른 정당한 것): `M.legs.length` 10 · seek 0.5→**0.55**(0.5 는 05→06 씸 정중앙에 떨어진다) → 레그 06 `상승` · 여수 판 검사를 `seek(0.99)` 대신 **`bands.yeosu` 한가운데**에서 · 항로 `['궤도','성층운','한반도','남원','여수','울주']` · 지연 로딩은 index 9(`w08b`, 정규식 `w\d\d[a-z]?`) · 포스터 ≥10 · 마감 훑기에 0.78 추가(여수 판 생성 시점).
- `node tools/scrub/shoot-strip.mjs --legs 7-8b --prefix legs-7-8b` → `shots/scrub/legs-7-8b-{1..9}-*.png`, 몽타주 `legs-7-8b-montage.jpg`, 콘솔/페이지 오류 0. shoot-strip 은 `handoffFinal.legIndex` 레그 뒤에 인계 3장을 끼우고, 마지막 레그에 판이 없으면 `film-end` 1장을 찍게 고쳤다.
  - seam 06b→07 mid(17 km) · L07-mid(2.28 km, 브래킷 카운트업) 불변.
  - handoff-07-in(1.34 km): 방파제 발치 흰 더미. handoff-07-mid(300 m): 여수 V-World 판 + `인계 · 여수` 슬림 카피. **handoff-07-end: 판 위로 08 이 op 0.5** — 판이 닫히며 필름이 이어진다.
  - L08-mid(필름 2.52 s / 계기 1.33 km): 방파제가 좌하로 빠지는 중, 카피 `물가에서 다시 곡면 위로`, 룰러 `여수`.
  - seam 08→08b mid(6.0 km): A08b 프레임, 룰러 **`울주`** 점등. L08b-mid(5.2 km): 능선·계단밭 + 카피 `곡면을 따라 동쪽 산릉으로`, 계기 좌표 127.74(컷 전). film-end-08b(4.5 km): A09 구도, 계기 129.1500, 35.5500.

## 다음 라운드에 반영할 것

1. **6b 재생성**(06→06b 12.9 % 컷)은 여전히 남아 있다 — 풀 영상에서는 0.734 s 페이드가 삼키지만 군락 이중상이 보인다.
2. Leg 8 의 요 회전(t≈1–2.5)이 거슬리면 A08 motion_prompt 에 heading 고정 문구를 더해 1회 리롤.
3. A09 좌하의 항구가 "여수가 따라온" 것으로 읽힌다면 A09 리롤(항구 없는 산지 해안).
4. 브랜드 마감 판의 카메라: 09–12 가 들어오면 `handoffFinal` 을 다시 "필름 최종 프레임"으로 돌리거나(귀환 A12→A01) 별도 마감 판을 둔다. 지금은 여수 판이 07 뒤에서 닫히고 마감은 08b 마지막 프레임 위에 선다.

## 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-08{,b}.mp4` · `.seam.png` | kling 원본 · 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-08{,b}.mp4` (+ `.first/.last.webp`) | kie post 스크럽본 |
| `landxi/assets/proto/film/legs/w0{1..8,6b,8b}{,-m}.mp4`, `w*.webp`, `manifest.json` | 마운트 산출물(10 레그, 데스크톱 crf 22 재빌드) |
| `landxi/assets/proto/film/legs/full.mp4` · `full.webp` | 풀 영상본 44.21 s / 52.9 MB · 포스터 |
| `shots/kie/v3-legs-8-8b-sheet.jpg` · `v3-leg-08{,b}-t*.jpg` · `legs-8-8b-run8.log` · `legs-8-8b-run8b.log` | 시트 · 프레임 · 로그 |
| `landxi/assets/proto/review/v3-legs-8-8b-sheet.jpg` | 리뷰 사본 |
| `shots/scrub/legs-7-8b-*.png` · `legs-7-8b-montage.jpg` · `legs-7-8b-strip.json` · `e2e-legs-8-8b.log` | 스크럽 실촬영 · e2e 로그 |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(batch 8-8b: ok 2 + 무과금 실패 2) |
| `tools/kie/legs-v3.mjs` · `tools/scrub/assemble.mjs` · `tools/film/full.mjs` · `tools/scrub/shoot-strip.mjs` | trimNeg 수정 · PLAN 08/08b + crf 22 + full 호출 · 풀 영상 · 인계 촬영 |
| `landxi/proto/scrub/{index.html,scrub.js}` · `landxi/proto/film/timeline.html` · `tests/e2e/proto-scrub.spec.mjs` | 세그먼트·카피·인계 밴드 · 풀 영상 섹션 · 기대값 |
