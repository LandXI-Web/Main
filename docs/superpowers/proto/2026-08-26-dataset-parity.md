# 데이터 관리 대조표 — 원본 `dataset.html` ↔ `landxi/proto/dataset.html`

- 원본: <https://mini531.github.io/namwon-smart-village/landxi7/dataset.html>
  (+ 프래그먼트 `dataset-upload.html` · `dataset-manage.html` · `dataset-manage-publishing.html` · `dataset-archive.html`)
- 인벤토리: `docs/superpowers/specs/2026-08-26-landxi7-function-inventory.md` §3
- 조판 마스터: **`design-canvas/v2/B5-DataMgmt.dc.html`** (1440×900, 발주 확정) · `NOTES.md` §13 (장치 지도 · 유보 3 · §13.6 구현)
  · 이전 마스터 B2-DataMgmt-{Upload,List} 는 §6 · §7 기록으로만 남는다
- 법: `design/system.md`
- 구현: `landxi/proto/dataset.{html,css,js}` · `ds-data.js` · `ds-plate.js` · `ds-thumbs.js`
- 검증: `tests/e2e/proto-dataset.spec.mjs` (17건 전부 통과, 콘솔 0) · 단위 89건 · 그림 `shots/proto-ds/b5-*.png` (`node tools/proto/shoot-ds.mjs`, 13장) · §11 구현 지도
- 발주 추가(2026-08-26): "아카이브 완료 → 그 결과를 지도의 레이어처럼 본다" → 아카이브 `표시` = 우측 판의 레이어(§5 · `NOTES.md` §13.6)

원칙 두 가지.
1. **기능은 원본과 1:1.** 표에 없는 기능은 만들지 않았고, 원본에 있는 기능은 빼지 않았다.
2. **썸네일이 목록이다(B5).** 텍스트 행 0. 4탭은 `?tab=` 로 가른 진짜 탭이고, 우측 480 슬롯을
   발행 폼 · 상세 · 지도 판이 나눠 쓴다. 좌표계가 없는 파일은 점선 무채 액자로 **자백**한다.

---

## 0. 셸

| 원본 | 동작 | 우리 구현 | 상태 |
|---|---|---|---|
| 로그인 관문 | 비로그인 진입 시 `login.html?next=` 로 되돌림 | `dataset.html` head 인라인 스크립트 — 그리기 전에 판정 | **있음** |
| 좌측 레일 A1–A11 | `include/header.html` 메뉴 10 | 대시보드와 **같은 컴포넌트를 옮겨 씀**(`dashboard.*` 는 손대지 않음), 활성 `data-menu="media"` = 데이터 관리 | **있음** |
| 푸터 include | 개인정보처리방침 · 이용약관 · 이메일주소무단수집거부 · 주소 · 고객센터 · Family Site | 원장 하단 `#foot-links` / `#foot-addr` | **있음** |
| 배경 지도(전국 뷰) | `NamwonMap.initMap`, 탭 전환해도 재초기화 없음 | B5 는 전면 지도가 없다. 지도는 우측 판(`#mapdrawer`)에 한 번만 mount 되고 탭을 옮겨도 살아 있다 | **있음(판으로)** |

## 1. 탭 4종 · `?tab=` 딥링크

| 라벨 | `?tab=` | 원본 프래그먼트 | 우리 패널 | 상태 |
|---|---|---|---|---|
| 데이터 업로드 | `upload` (기본값) | `dataset-upload.html` | `#panel-upload` | **있음** |
| 업로드 완료 | `manage` | `dataset-manage.html` | `#panel-manage` | **있음** |
| 레이어 발행중 | `publishing` | `dataset-manage-publishing.html` | `#panel-publishing` | **있음** |
| 아카이브 | `archive` | `dataset-archive.html` | `#panel-archive` | **있음** |

- `history.pushState` 로 URL 동기화 · `popstate` 로 뒤로가기 복원 — 원본과 같다.
- **탭 전환 시 필터칩·검색·하단 패널 상태가 초기화된다** — 원본 동작 그대로.
- 알 수 없는 `?tab=` 값은 `upload` 로 떨어진다.
- 원본은 프래그먼트를 `fetch()` 로 지연 로드한다. 우리는 네 패널을 한 문서에 두고 `hidden` 으로
  가른다 — **기능·상태·URL 은 같고 네트워크 왕복만 없다.**

## 2. 데이터 업로드 탭

| 기능 | 원본 | 우리 구현 | 상태 |
|---|---|---|---|
| 파일형식 필터 7 | 전체 · ECW · TIF · ZIP · SHP · XLSX/XLS · 기타 | `FMT_FILTERS` — `기타` 는 앞 다섯에 안 드는 전부 | **있음** |
| 검색 | 파일명 검색 | `#q` — 파일명·데이터명·등록자·유형 | **있음** |
| 업로드 폼 | 드래그 & 클릭 · 최대 1 TB · 허용 형식 5 · 업로드 버튼 | `#drop` (dragover/drop/click/Enter) + `#file` + 브래킷 CTA | **있음** |
| 폼 검증 | 허용 형식 · 용량 한도 | 파일 없음 / 허용하지 않는 형식 / 1 TB 초과 3종, `#up-err` | **있음** |
| 진행 카드 상태 4종 | 업로드중 · 일시정지 · 중단됨 · 대기중 + 진행률 | `UP_ST`, 6건 (`UPLOADS`) | **있음** |
| 카드별 액션 | 일시정지 / 재개 / 이어 올리기 / 업로드 취소 / 세부 정보 | `UP_ACTIONS` — 상태별로 원본과 같은 조합. 자리는 타일이 아니라 **우 패널**(`#side-acts`, 발주) | **있음** |
| 목록 길이 제어 | `대기중 n건 더 · 전체 보기` | **발주(2026-08-27)로 제거** — 6건 전부 타일로 서고 그리드가 스크롤한다 | **뺌(발주)** |
| 내 디스크 사용량 | 1,965.0 / 2,048.0 GB · 96% · 잔여 83.0 GB | `DISK` — 같은 값 | **있음** |
| 디스크 증량 신청 **모달** | 32/64/128/256/512/1024 프리셋 · 직접 입력 · 신청 사유 · 취소 / 저장 | `#m-quota` — 용량·사유 필수 검증 | **있음** |

## 3. 업로드 완료 탭

| 기능 | 원본 | 우리 구현 | 상태 |
|---|---|---|---|
| 파일형식 필터 · 검색 | 상단 공유 | 같은 `#ds-filters` / `#q` | **있음** |
| 목록 | 크기 · 업로드 일시 · 등록자 · 아카이빙 n회 | `DONE_UP` 8건 | **있음** |
| `지도 레이어 발행` 실행 | 행 버튼 | 타일 선택 → 우 패널 `#side-acts .act[data-dn]` → 패널 안 발행 폼 | **있음** |
| 발행 폼 | 발행 유형 / 기준 일자 / 데이터명 / 출처 / 설명 | `#pubform` — 기준 일자·데이터명 필수 검증 | **있음** |
| 공유 권한 **표** | 기관명 · 권한명 | `#pf-perm` 2행 × `PERMS` 3단(권한 없음/뷰어/편집) | **있음** |
| 발행 결과 | 발행중 목록으로 이동 | 제출 시 발행중 1건 생성 + `?tab=publishing` 으로 전환 | **있음** |

## 4. 레이어 발행중 탭

| 기능 | 원본 | 우리 구현 | 상태 |
|---|---|---|---|
| 파일형식 필터 · 검색 | 상단 공유 | 같음 | **있음** |
| 4단계 진행 | 파일 확인 → 공간정보 분석 → 지도 데이터 변환 → 레이어 발행 | `PUB_STEPS` — 단계 눈금 + 라벨 | **있음** |
| 진행중 / 실패 상태 | 상태 표시 | `PUB_ST` · 7건(진행 5 · 실패 2) | **있음** |
| 실패 사유 문구 | 원본 문구 | `좌표체계 정보를 확인할 수 없습니다…` 외 1건. **실패는 지우지 않는다** | **있음** |
| 발행 취소 | 행 버튼 | 타일 선택 → 우 패널 `.act[data-pb][data-act="cancel"]` | **있음** |

## 5. 아카이브 탭

| 기능 | 원본 | 우리 구현 | 상태 |
|---|---|---|---|
| 유형 필터 4 | 전체 · 정사영상 · 이미지셋 · 공간정보 | `KIND_FILTERS` | **있음** |
| 검색 | | 같음 | **있음** |
| 데이터 카드 | 유형 · 데이터명 · 원본 파일명 · 크기 · 기준일 · 등록자 · 등록일시 | `ARCHIVE` 5건 (원본 시드 4 + 판 레이어 시연용 실측 GeoJSON `여수 해양쓰레기 조사 2026` 1) | **있음** |
| 표시/숨김 | 원본 아카이브 카드에는 없음(카드 클릭 = `flyToData` 뷰 이동뿐). 발주 추가 요구 | `data-hidden` → 타일 `opacity:.34` + 선반 `숨김 — 삭제 아님`. **표시 = 판의 레이어**: 정사영상은 실제 타일, 공간정보는 실제 GeoJSON, `fitBounds` 줌 투 익스텐트. 숨김은 레이어 목록에 감쇠로 남고 판에서 꺼진다. 기하가 없으면 `실측 범위 없음` | **있음(+판)** |
| 공유 설정 **모달** | 기관명 / 권한명 표 | `#m-share` — 3단 세그먼트, 저장 시 상태 반영 | **있음** |
| 공간 편집 | 편집용 벡터를 판에 올리고 `view.fit` | 타일·상세 양쪽 — 판을 열고 그 범위로 간다. 기하 없으면 자백 | **있음** |
| 삭제 | 목록에서 제거 | `.act[data-act="del"]` — 판의 레이어도 내린다 | **있음** |
| 데이터셋 상세 | 데이터명 / 출처 / 설명 | `#detail` 드로어(우측 480 슬롯) + 실측 범위 행 | **있음** |
| 밴드 · 속성 표 | 속성명 / 속성정보 | `#detail-bands` | **있음** |

## 6. 판(지도) — (B2 기록. B5 의 판은 `NOTES.md` §13.6 을 따른다)

| 마스터 | 우리 구현 |
|---|---|
| 실측 범위 3 = 코너 브래킷(실선) + 좌표·GSD 캡션 | `EXTENTS` — 좌표·GSD 는 **`imagery.js` 실측값**이지 손으로 적은 값이 아니다 |
| 좌표계 없는 파일 = 점선 무채 액자 + 이유 한 줄 | `ex--pending` — `NW_ortho_202602_field_01.tif · 범위 미확정` |
| 범례 `측정 = 실선 / 미확정 = 파선` | `#legend` (업로드 탭) |
| 선택 데이터셋 = 락온 브래킷 + 파일명 캡션 | LX 블루 — **화면당 1회, 데이터에만**(§4) |
| 선택 데이터셋의 정사영상 판 | 그 데이터셋의 **실제 타일**(`assets/tiles/namwon_2504` 등)을 판에 올린다. 없으면 아무것도 올리지 않고 `좌표계가 없어 판에 세우지 않습니다` 로 자백 |
| 미리보기 없는 자산 | **점선 무채 액자 `미리보기 없음`** — 그럴듯한 그림으로 채우지 않는다 |

## 7. 살아있음(§5)

- **유휴 운동 1개** — 업로드중 카드의 진행률. 실제로 도는 타이머이고, 값은 `시연` 이라고 화면이 밝힌다(§7 콘티 원칙).
- **호버 180 ms 물리 반응** — 레일 아이콘 `translateY(-2px)`, 목록 행 `translateX(4px)`, 칩·탭 `translateY(-2px)`. 색만 바뀌는 곳은 없다.
- **앰버 스탬프 1개** — 판 머리 `#fresh .live` (6.4 s 주기). 액센트 블루는 선택 락온에만.
- **유리 1개** — 하단 `지도 레이어 발행` 폼(`rgba(36,39,48,.85)`). 그 외 패널은 헤어라인 액자.
- **화면 전환 = 같은 지도의 카메라 이동** — 탭·선택이 바뀌면 `fitBounds(1250ms)`.

---

## 8. 마스터와 남은 차이 — B5 는 `NOTES.md` §13.6 참조. 아래는 B2 시절 기록

| # | 차이 | 왜 |
|---|---|---|
| D1 | **레일 76 → 72 px** | 대시보드 E10 확정값(`NOTES.md` §4.3). 레일은 공용 컴포넌트라 대시보드 쪽 값을 따랐다. 원장·판이 통째로 4 px 왼쪽으로 온다 |
| D2 | 마스트헤드 `보관 도엽 **12**` → `**11**` | `imagery.js` 를 세어서 적는다. 마스터의 12 는 근거를 찾을 수 없다 |
| D3 | 진행 목록 꼬리표에 `· 시연` 추가 | 콘티 원칙(§7) — 원본 목업 시드임을 화면이 밝혀야 한다. **기능이 아니라 라벨** |
| D4 | 아카이브 썸네일이 마스터의 `ev-*.jpg` 와 다른 그림 | 마스터는 디자인용 다운샘플, 우리는 `assets/proto/crops/*` 실자산 크롭 |
| D5 | List 마스터는 **아카이브 + 업로드 완료 + 발행중 + 발행 폼을 한 아트보드에 합쳐** 보여 준다 | 우리는 원본과 같이 **네 개의 진짜 탭**으로 가른다(`?tab=`). 한 화면에 합치면 원본에 없는 화면이 된다 |
| D6 | 판의 브래킷 위치가 마스터와 다름 | 마스터는 예시 배치, 우리는 **실제 좌표에 투영**한다. 캡션 위/아래는 마스터를 따라 배치 |
| D7 | 아카이브 판에 정사영상 위·아래 여백(검은 띠) | 도엽이 정사각에 가깝고 판은 세로로 길다. 실제 footprint 를 늘려 채우지 않는다 |
| D8 | 전국 판의 줌이 마스터보다 살짝 넓다 | 실자산이 모인 서남부(`KOREA_SW`)를 세로 기준으로 맞춘 결과. 지명 라벨은 V-World 위성 타일에 없다 |

## 9. 원본에 없어서 만들지 않은 것

- **페이지네이션.** 원본 데이터 관리 프래그먼트 4종에는 페이저가 없다(인벤토리 §3 · 부록 `?page=` 목록에도 없음).
  목록 길이 제어는 원본·마스터가 쓰는 **`전체 보기` ↔ `접기`** 뿐이고, 우리도 그것만 넣었다.
  (페이저가 있는 화면은 `ai-project.html` · `ai-card.html` 이며 이 페이지가 아니다.)
- 정렬 컨트롤 · 일괄 선택 · 대량 다운로드 · 업로드 이력 타임라인 · 용량 추세 차트 — 전부 원본에 없다.
- 지어낸 운영 서사(대기 일수 · 담당자 활동 기록 · 처리 SLA)는 넣지 않았다.
  목록의 등록자 이름은 **원본 목업 시드 그대로**이며(`NOTES.md` §7: "이 페이지는 자산 목록이라
  원본 시드가 곧 파리티 대상"), 화면은 이 목록 전체를 `시연` 으로 표시한다.

## 10. 재현

```bash
node tools/serve.mjs                                    # 4173
npx playwright test tests/e2e/proto-dataset.spec.mjs    # 17건
```

그림은 `shots/proto-ds/b5-*.png` — `node tools/proto/shoot-ds.mjs` (서버 4173 필요). 목록은 §11.5.

---

## 11. B5 구현 — 무엇이 어디에 앉았나 (2026-08-27 재편 · 발주 반영)

마스터 `design-canvas/v2/B5-DataMgmt.dc.html` 12.1 (렌더 `design-canvas/v2/renders/B5-DataMgmt.png`) ↔ `landxi/proto/dataset.*`.

발주(2026-08-27, 원문): *"데이터 관리 화면은 대시보드와 동일하게 위에 공지사항은 그대로 놔두고 아래로 내려서 한다. 대시보드와 마찬가지로 내 디스크 사용량 업로드 완료 발행중 등 이건 카드 형태로 상단에 표출 그 아래에 진행 상황을 보여준다. 업로드 완료 된건 선택하면 자동으로 오른쪽에 지도와 기본 정보가 표출된다. 오른쪽 카드 형태로 레이어 발행중은 업로드 처럼 진행 경과를 카드위에 시각적으로 보여주고 일단 미니 카드 아래 표시 숨김 공유 공간편집 발행 등 이런건 없고 오른쪽 카드 펼쳐서 정리하는걸로 한다. 현재 카드는 좀 작게?? 되어 있는데 사용자가 4x4 해서 키우든 이런 4배수 개념으로 카드 이미지도 적절이 커스텀 하게 한다."* + *"대기중 2건 더 · 전체 보기 이건 필요 없다. 대기중 다 표출시킨다."*

| 발주 문장 | 마스터 장치 | 구현 위치 | 검증 |
|---|---|---|---|
| 대시보드와 동일하게 공지는 그대로, 아래로 내려서 | 레일 72 · 마스트헤드 64(공지 strip + 기준일) · 제목 + 파랑 룰 · 헤어라인 | `#mast`(`db-data.js` NOTICE · T1 공유) · `#b1 .rule::after` | e2e `레일 · 마스트헤드` |
| 디스크 · 업로드 · 완료 · 발행중은 카드로 상단에 | KPI 카드 5 = 내 디스크 96 %(warn + `디스크 증량 신청 ›`) · 데이터 업로드 6 · 업로드 완료 8 · 레이어 발행중 7(실패 2 warn) · 아카이브 5. **카드 = 단계 선택**(`?tab=`), 선택 = 틴트 + 파랑 밑줄 | `renderKpi()` `button.k[role=tab]` · `setTab()` pushState/popstate | e2e `KPI 카드 5` |
| 그 아래에 진행 상황 | 단계 뷰 = 좌 타일 그리드(3fr) / 헤어라인 / 우 패널(2fr), 같은 높이 | `#stage` grid · `#grid`(스크롤) · `#side` | e2e `?tab=` ×4 |
| 완료 선택 → 자동으로 오른쪽에 지도 + 기본 정보 | 판(V-World + 도엽 타일 + 실측 브래킷 + 캡션 `장소 · 날짜 · GSD · 측정`) + 두 열 dl 10행 + `지도 레이어 발행 ›` | `renderSide()` manage 분기 · `applyMapMode('sel')` · `imFor()`(d4 · d5 만 도엽) | e2e `완료 타일 선택` |
| 발행중은 진행 경과를 카드 위에 시각적으로 | 우상단 4눈금(채움 = 완료 단계) + 리빌 % · 실패 = warn 브래킷 + 눈금 1 warn + `실패 n/4 · 짧은 사유` | `pbTile()` `ticks()` `revealPair()` · `PUB_PCT` | e2e `발행중` |
| 미니 카드 아래 표시/숨김/공유/공간 편집/발행 없음 → 오른쪽 카드를 펼쳐서 | 타일 = 그림 + 캡션 1줄. **선반 0, 버튼 0.** 액션은 우 패널 하단 헤어라인 버튼(업로드 5 · 완료 1 · 발행중 2/3 · 아카이브 5) | `#side-acts`(스크롤 몸통 밖 — 항상 보인다) · `UP_ACTIONS` `FAIL_ACTIONS` | e2e 각 단계 + `#grid .tile button` = 0 |
| 카드는 4배수 개념으로 키우게 | 툴바 `타일 S · M · L · XL` = 6 / 4 / 3 / 2 열, 그림은 1.63:1 로 열에 맞춰 커진다 | `SIZES` · `body[data-size]` `--cols` · `localStorage lx_ds_size` | e2e `타일 크기` |
| 대기중 n건 더 · 전체 보기 없음 — 다 표출 | 업로드 6건 전부 타일. 접기 0 | `renderUpload()` — `UP_FOLD` 미사용, 그리드가 스크롤 | e2e `건 더|전체 보기|접기` 0 |

### 11.1 우 패널 — 단계별로 무엇을 펼치나

| 단계 | 판 / 액자 | 기본 정보 | 액션(헤어라인 버튼) |
|---|---|---|---|
| 선택 없음 | 점선 액자 + 단계 건수(58px) | 업로드: 디스크 · 잔여 · 허용 형식 · 한도 / 아카이브: 판에 선 레이어 목록 | 0 |
| 데이터 업로드 | 그림 리빌 %(살아있는 건 액센트) | 파일명 · 형식 · 크기 · 상태 · 진행률 (+세부 정보: 대기 순번 · 자료) | 일시정지 / 재개 / 이어 올리기 · 취소 · 세부 정보 — 상태별 `UP_ACTIONS` |
| 업로드 완료 | 도엽이면 **지도**(V-World + 실제 타일 + 브래킷), 아니면 액자(XLSX 표 · ZIP 트리 · SHP 실루엣 · 점선 자백) | 이름 · 형식 · 크기 · 업로드 일시 · 촬영일 · GSD · 좌표계 · 아카이빙 · 등록자 · 범위 | `지도 레이어 발행 ›` → 패널 안 폼(5필드 + 공유 권한 표 + 취소/발행). 폼이 열리면 판은 140 으로 낮아진다 |
| 레이어 발행중 | 그림 리빌 % + 눈금 4(액자 좌상단) · 실패는 warn 캡션 | 파일명 · 형식 · 크기 · 업로드 · 상태 · 단계 · (실패 사유 원문) · (좌표계) | 진행: 발행 취소 · 세부 정보 / 실패: **좌표계 지정** · 발행 취소 · 세부 정보 |
| 아카이브 | **판** — 표시된 자산이 레이어로 선다(§11.2). 범위 없으면 `실측 범위 없음` | 이름 · 원본 파일 · 유형 · 형식 · 크기 · 기준일 · 등록 일시 · GSD · 좌표계 · 표시 · 범위 (+상세: 데이터명 · 출처 · 설명 · 밴드/속성 표) + 레이어 목록 | 숨김/표시 · 공유 · 공간 편집 · 삭제 · 상세 |

### 11.2 아카이브 `표시` → 판의 레이어 (발주 추가 · 유지)

```
타일 선택 ─▶ renderSide() 아카이브 분기 ─▶ 표시 상태면 S.layers 에 추가 ─▶ applyMapMode('arch')   (dataset.js)
    ├ 판은 한 번만 mount(ensureMap) — 단계를 옮겨도 살아 있다(완료 = 'sel' 도엽 1장, 아카이브 = 레이어들)
    ├ syncLayers() → geomOf(a)                                                            (ds-plate.js)
    │     raster(imagery.js 도엽)   → addRaster: 실제 타일 피라미드
    │     vector(results/*.geojson) → addVector: 청록 fill + line 1px
    │     기하 없음                  → 판에 올리지 않고 레이어 행 · 판 캡션 `실측 범위 없음`
    ├ frame(bounds, 1250ms) = 줌 투 익스텐트 · Brackets.set() = 헤어라인 코너 브래킷 + 캡션
    └ 패널 액션 [숨김] = setHidden(visibility none) + 감쇠 — 목록에 남는다 / [삭제] = removeLayer
```

### 11.3 검수에서 고친 것 (2026-08-27)

| # | 증상 | 조치 |
|---|---|---|
| G1 | 마스터: 액션 행이 기본 정보 마지막 행(아카이빙 · 범위)을 덮음 | 판 260 → 220, 정보 y 680 → 640, 액션 y 826 → 812 |
| G2 | 마스터·구현: XLSX 타일 표 4열이 172px 을 넘쳐 `사매면경작` 이 붙음 | 3열(pnu · cls · area) — 구현은 S·M 열에서 `emd` 를 접고 L·XL 은 4열 |
| G3 | 구현: 우 패널 액션 행이 스크롤 몸통 안에 있어 1440×900 에서 접힘 | `#side-acts` 를 `#side-body` 밖으로 — 정보는 스크롤, 액션은 항상 보인다. 판 max 220(발행 폼 열리면 140) |
| G4 | 구현: 마스트헤드 `전체 보기 ›` 가 8px 내려앉음 | 그리드 시절 `.more{margin-top:14px}` 잔재 제거(전체 보기 자체가 없어졌다) |

### 11.4 마스터와 남은 차이 (의도)

- 완료 기본 정보에 `등록자` 1행이 더 있다 — 원본 목록의 열(등록자)을 빼지 않기 위해. 마스터는 지면상 9행(12.2: 성과 2 + 4행).
- 페이저 `1 / n · ‹ ›` — 원본 데이터 화면에 없다(§9). 2차 발주("아래 페이지 수 표기")로 추가. 쪽당 4 · 6 · 8 · 16, 클라이언트 쪽에서 자른다(§11.6).
- 발행 폼이 열리면 필드 5 + 공유 권한 표 + 취소/발행이 패널 안에서 스크롤한다(1440×900). 1920 에서는 한 화면.
- 좌표·GSD·범위는 `imagery.js` 도엽과 같은 자산(d4 · d5 · a1 · a2)에만 적는다. 그 외는 `—` / `실측 범위 없음`.
- 아카이브 5건(마스터 5) — 판 레이어 시연용 실측 GeoJSON 자산 1건 포함. 원본 시드는 그대로.

### 11.5 그림 (`node tools/proto/shoot-ds.mjs`) — 2차 갱신

`b5-01` 업로드 선택 없음 = 진행 현황판 · `02` 업로드 진행중 선택 · `03` 완료 쪽당 8 · `04` 쪽당 16(4×4) · `05`/`06` 쪽당 4 = `1 / 2` → 2쪽 · `07` 완료 도엽 선택 = 실제 위치 + 청록 결과 + 성과 2 · `08` XLSX 데이터 테이블 · `09` SHP 실루엣 + 실값 · `10` 발행 폼 · `11` 발행 실패 · `12`/`13` 아카이브 a1(사용 현황 · 성과 · 발행 이력 · 메모) · `14` 아카이브 a5 · `15` 1920.

### 11.6 2차 피드백 반영 (2026-08-27) — 마스터 12.2 · `B5-DataMgmt-Upload`

클라이언트(원문): *"1. 업로드는 카드를 선택하면 오른쪽 정보 창이 뜨고 선택하지 않으면 그냥 일반 카드별 진행현황 전체로 표기한다. 타일은 SMLXL이 아니라 4 6 8 16 이렇게 하고 아래 페이지 수 표기한다. 2. 업로드 완료된건 카드 클릭하면 실제 위치 정보와 성과가 오른쪽 정보 창에서 볼 수 있게 한다. 위치 정보가 없는건 데이터 테이블 속성을 알수있게... 3. 아카이브 아래 오른쪽 정보창 아래 레이어 표시 이력 아래 뭐 남는 공간이 있는거 같다. 메모를 남기는 공간을 만든던지. 좀 유용한 컨텐츠가 있었으면... 지금 레이어 4 · 표시 4 · 숨김 0 / 범위 / 표시·숨김 이건 별로 의미 없는듯?"*

| 피드백 | 구현 | 위치 | 검증 |
|---|---|---|---|
| 업로드 · 선택 없음 = 카드별 진행 현황 전체 | 우 패널 제목 `진행 현황` + 현황판: 한 줄 = 한 건(이름 · 상태어 · 막대 % · 크기 · 잔여). 잔여 = 크기 × (1 − %) 를 계산. 유휴 운동(1.6 s)이 막대·%·잔여를 같이 옮긴다. 아래 디스크 · 잔여 · 허용 형식 · 한도 | `boardHtml()` `remain()` `tick()` · `.pbh .pb` · `bytesOf()/fmtBytes()`(ds-data) | e2e `?tab=upload`(6줄 · 상태어 · 잔여 30.1 GB) · `업로드 상태 기계`(막대가 움직인다 · 취소 → 현황판) · `드롭존`(+1줄) |
| 선택하면 그 건의 정보 창 | 기존 상세(그림 리빌 % + dl + 액션 5). 현황판은 사라진다 | `renderSide()` upload 분기 | e2e `.pb` 0 |
| 타일은 4 · 6 · 8 · 16 | 툴바 `쪽당 4 6 8 16` = 열 2 · 3 · 4 · 4, 행 2 · 2 · 2 · 4(16 = 4×4). 타일 높이 상한 = 그리드 높이(`100cqh`) ÷ 행 — 어느 쪽이든 스크롤 없이 한 화면. `localStorage lx_ds_pp`(기본 8). 이전 `lx_ds_size` 폐기 | `PER_PAGE` · `setPp()` · `body[data-pp]` `--cols --rows` · `#grid{container-type:size}` | e2e `쪽당 4 · 6 · 8 · 16`(열 수 · 16 은 타일 높이 < 0.8× · 새로고침) |
| 아래 페이지 수 | 그리드 아래 페이저 `1 / 2 · ‹ ›`. 한 쪽이면 화살표 없음. 필터·검색·단계 이동·쪽당 변경 = 1쪽. 업로드는 드롭존이 매 쪽 첫 타일(쪽당 − 1 건). **원본에 페이저 없음(§9) → 발주 추가** | `pageOf()` `#pager` | e2e `1 / 2` → `2 / 2`(d5–d8) · 검색 → `1 / 1` · 업로드 드롭존 + 3 |
| 완료 클릭 = 실제 위치 + 성과 | 도엽(d4 · d5): 판 = V-World + 실제 타일 + 브래킷, 도엽을 가운데 두고 **3배 범위**로 프레임 → 주변의 **청록 결과 폴리곤**(results.js GeoJSON)이 같이 선다. 캡션 `… 측정 · 성과 2`. 판 아래 `성과 n · AI 결과`: 결과명 · `범위 내 n`(GeoJSON 을 실제로 세어 적는다) · 큰 수 + 단위 · 분석일 — 농지이용 **2,098 필지** · 비닐하우스 **9,664 동 / 1,674 필지**(services.js 와 같은 수). 기본 정보는 그 아래(스크롤) | `RESULT_OF` `resultRow()`(ds-data) · `resultsHtml()` `fillInBounds()` `syncResults()` `grow()` · 판 220 → 190 | e2e `완료 도엽 선택`(성과 2 · 2,098필지/9,664동 · 범위 내 2/0 · `ly-res-namwon-farmland-2025`) |
| 위치 없으면 데이터 테이블 속성 | 액자(XLSX 첫 행 · SHP 실루엣 · ZIP 트리 · 자백) + `데이터 테이블 · n열 · m행`: 속성명 / 유형 / 예시. XLSX·SHP 열은 results.js `fields`, ZIP 은 파일 트리, TIF/ECW(도엽 아님)는 밴드 3. 결과 GeoJSON 이 있는 SHP(d7)는 예시를 **첫 행 실값**으로 바꿔 적는다(`비닐하우스_단동` · `1,543`). 나머지 예시값은 `시연` | `SCHEMA` `SCHEMA_OF`(ds-data) · `schemaHtml()` `fillSchemaFromGeo()` | e2e d6(4열 · 좌표계 없음) · d3(5열 · 2,098행) · d7(실값) · d8(트리 3) |
| 아카이브 레이어 표시 이력 = 의미 없음 | `레이어 n · 표시 · 숨김 / 범위 / 표시·숨김` 블록 삭제. dl 의 `표시` · `범위` 행도 뺐다(타일과 판 브래킷이 이미 말한다). 등록 일시 · 등록자 · GSD · 좌표계는 `상세` 로 | `layersHtml()` 삭제 · `.ly` 0 | e2e `#side .ly` 0 · 문자열 0 · dt 6 |
| 유용한 컨텐츠 | 순서: **사용 현황**(이 자산을 쓴 프로젝트 · 데이터셋 · 서비스 — `ai-project-data.js` 의 같은 파일명 라벨 풀/데이터셋, `services.js`) → **성과**(RESULT_OF · a1 · a2 · a5) → 기본 정보 3행 → **발행 이력**(v1 = 등록 일시, 재발행 = 아카이빙 회수, 시드 날짜만) → **메모**. 사용 현황·발행 이력 헤더에 `시연` | `USAGE` `PUBLISH_LOG`(ds-data) · `usageHtml()` `publishHtml()` | e2e a5(1 · 1 · 1) · a1(2 · 2 · 3) · a4(0 · 2) |
| 메모를 남기는 공간 | `메모 · 이 브라우저에만 저장` textarea 500자 + `n / 500` + `저장 YYYY.MM.DD HH:MM` / `저장 없음` + `localStorage · 서버 저장 아님`. 자산별 `lx_ds_memo_<id>` = `{t, at}`. 입력 300 ms 뒤 저장. **발주 요청 편의 기능이지 서버 기능이 아니다** — 원본에 없고(§9), 서버 저장은 만들지 않았다 | `MEMO_KEY` `MEMO_MAX` · `memoHtml()` `memoGet/Put()` | e2e `아카이브 메모`(자산별 · 글자 수 · 새로고침) |

남은 차이(의도): 1440×900 에서 완료·아카이브 패널은 판 190 + 첫 블록까지 보이고 나머지는 패널 안 스크롤(1920 은 한 화면). 마스터 12.2 는 완료 = 성과 2 + dl 4행, `B5-DataMgmt-Upload` = 업로드 현황판. e2e 18 · 단위 89.
