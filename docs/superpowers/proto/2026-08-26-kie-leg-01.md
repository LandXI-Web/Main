# Leg 1 · 모형 지구본 — 생성 기록 (스틸 단계)

- 스펙: `docs/superpowers/proto/2026-08-26-film-shotlist-v2.md` leg 1 (모형 지구본 · 위성)
- 프롬프트 팩: `docs/superpowers/proto/prompts/ch1-leg-01-globe.md`
- 러너: `tools/kie/leg-01-globe.mjs` (클라이언트 `tools/kie/kie.mjs`)
- 비용 게이트(클라이언트, 2026-08-26): **스틸만 · 최대 2회 · 누적 28 크레딧**. 영상(kling)은 스틸 승인 후 별도 단계.

## 결론 (한 줄)

1차 스틸은 **재질·세계관 전부 통과, 구도에서 탈락**(한반도가 원반 중앙이 아니고 형태도 식별 불가, 지구본이 프레임의 ~73%). 2차 프롬프트는 작성·배선 완료했으나 **비용 캡에 0.5 크레딧 차이로 막혀 실행하지 않았다.** 승인 필요.

## 크레딧

| 시점 | 잔액 | 증감 |
|---|---|---|
| 세션 시작 | 1016.0 | — |
| still-1 직전 | 1016.0 | — |
| still-1 직후 | 1001.5 | **−14.5** |
| still-2 | (미실행) | 게이트 차단, 과금 0 |
| 현재 | **1001.5** | 누적 −14.5 |

- seedream 5-pro image-to-image 실측 단가 = **14.5 크레딧/장** (팩·지시서의 추정 14보다 0.5 높다).
- 그래서 2회 = **29.0 > 캡 28**. `tools/kie/leg-01-globe.mjs` 의 `gate()` 가 createTask 이전에 차단했다 → **과금 없음**.
- 원장: `shots/kie/leg01-credits.json`.

> **승인 요청**: 2차를 돌리려면 캡을 28 → **29.5** 로만 올리면 된다(추가 지출 14.5).

## 모델

| 단계 | 모델 | 근거 |
|---|---|---|
| 스틸 | `seedream/5-pro-image-to-image` | 남원 테스트 leg 에서 검증된 경로. 참조 2장 = `image_urls` 배열. **negative_prompt 필드 없음** → 팩 네거티브를 프롬프트 안 `Avoid:` 절로 접어 넣었다. |
| 영상 | *(미실행)* | 아래 조사 결과 참고 |

### kling 모델 조사 (영상 단계용, 미실행)

kie.ai 문서에서 확인한 것:

- `kling/v2-1-pro` — **kie.ai 에 확실히 존재**. `duration` `'5'|'10'`, `negative_prompt`(500자), `cfg_scale` 0–1, **`tail_image_url`(끝 프레임) 지원**. 현재 `kie.mjs` 의 `MODELS.video` 기본값. 실측 ≈50 크레딧/5 s.
- `kling-3.0/video` — kie.ai 문서에 존재. `mode` std/pro/4K (pro=1920×1080), `duration` 3–15 s, 끝 프레임은 single-shot 모드에서만. **negative_prompt 없음**, 단가 미확인.
- Kling 2.5 Turbo — 공급자(kling.ai) 공식 문서 기준 `kling-v2-5-turbo`, 5/10 s, 720P/1080P, First/Last Frame 지원. **kie.ai 쪽 정식 모델 ID 는 문서에서 확정하지 못했다**(`docs.kie.ai/market/kling/v2-5-turbo*` 404, `kie.ai/market` 403). 3rd-party 문서에 `kling/v2-5-turbo-image-to-video-pro` 표기가 보이나 **미검증**.

→ **권고**: 영상 단계 시작 시 무과금인 `createTask` 검증 오류로 2.5-turbo ID 존재 여부를 먼저 확인하고, 없으면 `kling/v2-1-pro`(끝 프레임 지원 + negative_prompt 지원)로 간다. Leg 2 가 Leg 1 의 끝 프레임을 물려받는 씸 체인이라 **`tail_image_url` 지원이 2.1 Pro 를 고르는 실질적 이유**다.

## 참조 이미지 — 원본을 그대로 쓰지 않은 이유

지시된 구도 참조 `landxi/assets/proto/film/legs/src/anchors/orbit-0.png` 는 **검은 우주 + 별 + 사각 크롭 경계**의 포토리얼 렌더다. 이건 스틸 체크리스트가 명시적으로 금지한 세 항목(검은 우주·별·가장자리)과 정확히 겹친다. i2i 레퍼런스로 그대로 넣으면 그 셋을 그대로 끌고 들어온다.

그래서 **구도 정보만 남기고 팔레트를 죽인 판**을 만들어 썼다:

- `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-comp.jpg` (1차용, 지구본 = 프레임 높이 55%)
  - orbit-0 에서 지구 원반만 크롭 → 밝기↑·채도↓로 검은 우주를 크림으로 → **원형 알파 페더링**으로 사각 경계 제거 → 크림 배경(#EFE7D8) 위 55% 크기로 배치.
- `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-comp2.jpg` (2차용, 50%로 더 축소 + 여백 확대)
- `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-style.jpg` (2차용 재질/그레이드 잠금 — 1차 스틸에서 바다 채도만 0.68로 내린 판)

재질 참조는 지시대로 `shots/kie/namwon-greenhouse-test-03.jpg` (1차). 2차는 `ref-style` 로 대체했다 — 이미 그 그레이드를 물려받은 데다, **1차에서 통과한 재질을 그대로 잠그는 쪽이 안전**하고 레퍼런스가 셋이면 구도 지시가 묽어지기 때문.

## 1차 스틸 — 실행

- taskId `3653d1913b6a6bfe03cc3631cc005c69` · 모델 `seedream/5-pro-image-to-image` · 2736×1520 PNG · 벽시계 153 s
- 참조: `ref-comp.jpg`(구도) + `namwon-greenhouse-test-03.jpg`(재질)
- 산출: `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.still-1.png`

### 사용한 프롬프트 (전문)

프롬프트 팩 `## 1) 스틸` 본문 그대로 + 참조 역할 지정 + 포토리얼리즘 + `Avoid:` 절.

```
A handmade miniature orrery-style model globe seen from space, floating in soft cream-colored haze, no table, no stand, no base, no edges — the world continues beyond the frame.
Continents are painted plaster with fine moss texture; oceans are glossy cobalt-blue resin; polar caps are matte white plaster.
A single thin brass orbit ring encircles the globe with one tiny brass satellite model on it.
The globe fills about 55% of the frame height, slightly below center; East Asia faces the camera, the Korean peninsula near the center with a few small cotton clouds over its southern coast.
One soft studio top-light from upper-left, gentle reflections on the resin ocean, no hard shadows, shallow depth of field on the far edge.
Muted palette: cream, moss green, sand, one cobalt-blue accent. Matte cream sky, no stars, no black space.
Photographed like a product still, 35mm, f/4, 16:9.

Use the first reference image ONLY for camera framing: how large the sphere is in the frame, where it sits, and which side of the world faces the camera. Ignore its colours and its materials entirely.
Use the second reference image ONLY for material and light: painted plaster, real moss, poured resin, milled brass, sifted sand, soft near-shadowless studio light, low-saturation cream-and-moss colour grade.

Photographic realism — a real handmade object photographed on a seamless cream backdrop. NOT a 3D render, NOT CGI, NOT illustration, NOT clay, no digital glow, no plastic sheen, no toy-town cuteness.

Avoid: board, table, plinth, base plate, stand, frame edge, studio backdrop seam, black space, stars, blue sky, purple, neon, text, letters, logo, watermark, people, vehicles, cartoon, CGI look, glossy plastic, lens flare.
```

### 스틸 검수 체크 — 항목별 판정

검수 시트: `shots/kie/leg01-stills.jpg` (1행 후보 스틸 / 2행 한반도 부근 2.4배 확대 / 3행 목표 프레이밍)

| # | 체크 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 한반도가 정면 중앙 부근에 있고 식별되는가 | **탈락** | 원반 중앙은 인도네시아·적도 부근. 동아시아는 **좌상단 사분면**에 있다. 2.4배 확대에서 **일본 열도 호는 또렷하게 조형되었으나 한반도 자리에는 뚜렷한 반도가 없고 중국 해안에 뭉개져 있다.** |
| 2 | 보드/받침/가장자리/검은 우주/별이 없는가 | **통과** | 크림 헤이즈만. 받침·경계·별·검은 우주 전부 없음. 지구본이 공중에 떠 있다. |
| 3 | 바다가 코발트 하나뿐이고 보라·네온이 없는가 | **조건부 통과** | 보라·네온 없음. 다만 바다 채도가 높아 화면 절반을 밝은 파랑이 차지 — "액센트 하나"보다 "파란 지구본"으로 읽힌다. 2차에서 슬레이트 쪽으로 낮추도록 지시함. |
| 4 | 재질이 "손으로 만든 모형"으로 읽히는가 | **통과 (강)** | 석고 릴리프 대륙, 이끼 질감, 실제 솜뭉치 구름, 밀링한 황동 링과 **작은 황동 위성 모형**까지. 플라스틱·CGI 느낌 없음. 이 leg 의 가장 큰 수확. |
| 5 | 텍스트·로고 없음 | **통과** | 없음. |
| + | (팩 명시) 지구본이 프레임 높이의 55% | **탈락** | 실측 **≈73%**. 영상이 55%→75% 로 달리인 하는 설계인데 시작이 이미 73%면 **카메라 무브가 사실상 죽는다.** |

**종합: 탈락.** 5개 정식 항목 중 3통과 / 1조건부 / 1탈락 + 스케일 탈락. 재질은 그대로 가져가되 구도를 다시 잡아야 한다.

## 2차 스틸 — 작성 완료, 미실행

`tools/kie/leg-01-globe.mjs still 2` 로 바로 실행 가능. 1차에서 통과한 문장은 손대지 않고 **탈락한 두 축(회전·스케일)과 경계 항목(바다 채도)만** 보강했다. 1차 프롬프트 전문에 아래 네 문단이 추가된다.

```
Rotate the model globe so the Korean peninsula sits exactly at the centre of the visible disc, facing the camera head-on. The peninsula is sculpted as a clear thumb-shaped landmass roughly one eighth of the disc width, with the Japanese island arc curving away to its right, the Yellow Sea and the Chinese coast to its left, and Siberia above it. Australia, Indonesia and the equator are NOT visible — the southern hemisphere is below the horizon of the visible disc. A few small cotton-wool clouds sit over the peninsula's southern coast.

Framing: the sphere occupies only the middle 55% of the frame height and sits slightly below centre. Leave a wide empty band of plain cream haze above the globe, at least a quarter of the frame height, and empty cream to the left and to the right. The whole sphere is inside the frame with generous margin — do not crop it, do not let it fill the frame.

The resin ocean is a deep, muted, desaturated cobalt, dark and restrained, closer to slate blue than to bright sky blue. It is the only saturated colour in the picture; everything else is cream, sand, moss green and matte white.

The thin brass orbit ring crosses the upper third of the globe, with the tiny brass satellite model near the top right of the ring.
```

참조 역할 문단도 교체된다(1번 = `ref-comp2` 프레이밍 전용, 2번 = `ref-style` 재질/그레이드 잠금).

설계 의도 셋:

1. **"호주·인도네시아·적도는 보이지 않는다"** — 남반구를 잘라내면 원반 중앙이 강제로 북위 30–40°대로 올라온다. 1차가 적도 중심이었던 걸 회전으로 못박는 장치.
2. **여백을 수치로** — "55%"만으로는 안 먹혔다(73% 나옴). "위쪽에 프레임 높이 1/4 이상 빈 크림 띠"라는 **여백 기준**으로 바꿔 지시.
3. **링을 위쪽 1/3로** — 영상에서 링·위성이 위로 빠져나가야 하는데 1차는 링이 아래 1/3에 있었다. 시작 위치를 올려두면 이탈 거리가 짧아진다.

## Leg 2 가 물려받을 것 (현재까지 확정된 것)

- **월드 룰(1차로 검증됨)**: 석고 릴리프 대륙 + 이끼 질감 + 솜뭉치 구름 + 황동 소품 + 크림 헤이즈 배경. 이 조합은 seedream 에서 재현성 있게 나온다. 재질 참조 체인은 `namwon-greenhouse-test-03.jpg` → `ch1-leg-01-globe.ref-style.jpg` 로 이어간다.
- **씸 이미지**: Leg 1 영상의 마지막 프레임(`ch1-leg-01-globe.last.png`). **아직 없다** — 영상 미실행.
- **Leg 2 시작 조건(설계상)**: 한반도 정면, 지구본 ≈75%, 남쪽 해안에 솜구름, 링·위성은 프레임 밖.
- **주의(Leg 2 로 넘길 교훈)**: seedream 은 **스케일 지시를 %로 주면 무시한다.** 여백 기준으로 지시할 것. 그리고 **한반도를 자동으로 조형해주지 않는다** — 반도 형태·이웃 지형(일본 호, 황해)을 매번 명시적으로 서술해야 한다.

## 산출물

| 경로 | 내용 |
|---|---|
| `tools/kie/leg-01-globe.mjs` | 러너(비용 게이트 · 프롬프트 1·2차 · 검수 시트). video 분기 없음 |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.still-1.png` | 1차 스틸 (2736×1520) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-comp.jpg` | 1차 구도 참조 (55%) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-comp2.jpg` | 2차 구도 참조 (50% + 여백↑) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-style.jpg` | 2차 재질/그레이드 잠금 |
| `shots/kie/leg01-stills.jpg` | 검수 시트 3행 |
| `shots/kie/leg01-credits.json` | 크레딧 원장 |

---

# 라운드 2 — ORRERY 스타일 (클라이언트 승인 방향)

클라이언트 지시(2026-08-26): 지구본이 **유튜브 ORRERY 지구본과 같은 테마·재질·조명**으로 보여야 한다. 단 배경은 어두운 곳이 아니라 **흰색/크림**. 팩의 프레이밍 규칙은 유지. 캡 = 2회 × 14.5 = 29 (게이트 30).

## 결론 (한 줄)

**still-2 통과.** ORRERY 재질·조명을 그대로 가져왔고, 라운드 1의 최대 난제였던 **한반도가 이번엔 또렷하게 조형되어 원반 정중앙에 있다**. 유일한 미달이던 스케일(72.3%)은 **재생성 없이 크림 캔버스 확장으로 54.9%에 맞췄다.** 두 번째 시도는 쓰지 않았다 — 14.5 크레딧이 남아 있다.

## 크레딧

| 시점 | 잔액 | 증감 |
|---|---|---|
| 세션 시작 | 1016.0 | — |
| still-1 (라운드 1) | 1001.5 | −14.5 |
| **라운드 2 기준선** | **1001.5** | 캡 30 |
| still-2 직전 | 1001.5 | — |
| still-2 직후 | **987.0** | **−14.5** |
| 현재 | **987.0** | 라운드 2 사용 14.5 / 30, **잔여 15.5 (1회 가능)** |

게이트를 라운드 단위(`roundStart`)로 바꿨다. `node tools/kie/leg-01-globe.mjs newround` 로 기준선을 다시 잡는다.

## 스타일 참조 — 어느 프레임을, 왜

`landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-orrery.jpg` ← `shots/yt/QUI6-seg/f_008.png` 크롭 (1920×1080 중 x375,y105,w1155,h945 → 1400px)

프레임 선정: `f_005`·`f_007` 은 **반투명 자막("Everywhere you haven't been fits on one table.")이 지구본 위를 지나가고**, `f_009` 이후는 지구본이 프레임에 잘린다. `f_008` 이 지구본이 가장 크고 자막이 안 걸린 유일한 프레임이다.

크롭으로 잘라낸 것 — **좌상단 웹캠 PiP(리뷰어 얼굴), 좌하단 ORRERY 로고 카드, 우측 마우스 커서**. 셋 다 남겨두면 체크리스트가 금지한 텍스트·로고·인물이 레퍼런스로 새어든다.

`build/scroll-craft/media/orrery.webp` 도 있으나(지시서의 `build/yt/scrollcraft/` 경로에는 없음) f_008 크롭이 해상도·선명도에서 낫다.

프롬프트에서 **명시적으로 배제**한 ORRERY 고유 요소: 어두운 배경, 주황·빨강 발광 핀(목적지 마커), 검은 지도 핀·바늘, 황동 받침대.

## still-2 — 실행

- taskId `81490826a283b89749db775a6ec96c7c` · `seedream/5-pro-image-to-image` · 2736×1520 · 벽시계 153 s
- 참조: `ref-comp2.jpg`(프레이밍 전용) + `ref-orrery.jpg`(재질·조각·조명 전용)
- 산출: `ch1-leg-01-globe.still-2.png` (원본) / `ch1-leg-01-globe.still-2.framed.png` (**납품본**)

### 사용한 프롬프트 (전문)

```
A handmade miniature model globe floating in a bright matte cream-white field — no table, no stand, no base, no board, no edges; the world simply continues beyond the frame.

The globe is built exactly like the model in the second reference image: continents sculpted in relief from painted plaster and sifted sand, real moss packed into the mountain ranges and forests, oceans of deep muted cobalt-blue poured resin, polar caps of matte white plaster. Fine handmade texture everywhere — visible grain, dust and tiny imperfections.

A single thin milled brass meridian ring encircles the globe and crosses the upper third of the frame, with one tiny brass satellite model mounted on the ring near the top.

The globe is rotated so the Korean peninsula faces the camera dead-on at the centre of the visible disc: the peninsula is sculpted as a clear thumb-shaped landmass hanging south from the mainland, the Yellow Sea on its left, the curving Japanese island arc on its right, and a few small cotton-wool clouds resting over its southern coast.

Framing: the globe is small in the frame, with a large margin of empty white space all around it — generous empty cream above, below, to the left and to the right. The whole sphere sits inside the frame, slightly below centre, and is never cropped.

Lighting: one warm directional key from the upper left with a big soft white bounce filling the shadows — the same warm sculptural light as the reference — but lifted so the surrounding field reads as bright matte cream, not darkness.

Macro photographic realism, medium-format sharpness, shallow depth of field falling off at the limb of the globe. A real physical object photographed in a studio.

Use the first reference image ONLY for framing: how small the sphere is inside the frame and how much empty cream surrounds it. Ignore its colours and materials entirely.
Use the second reference image ONLY for material, sculpting and light: the plaster-and-sand relief continents, the moss, the resin ocean, the milled brass ring, the macro realism. Ignore its dark background, its glowing pins and its stand completely.

NOT a 3D render, NOT CGI, NOT illustration, NOT clay, no digital glow, no plastic sheen, no toy-town cuteness.

Avoid: dark background, black space, night, stars, board, table, plinth, base plate, stand, frame edge, glowing orange or red pin lights, illuminated city dots, black map pins, needles, blue sky, purple, neon, text, letters, logo, watermark, people, vehicles, cartoon, CGI look, glossy plastic, lens flare.
```

### 검수 — 항목별 판정

검수 시트 `shots/kie/leg01-stills.jpg`: 1행 납품본 / 2행 한반도 3.3배 확대 / 3행 ORRERY 스타일 참조

| # | 체크 항목 | 판정 | 근거 |
|---|---|---|---|
| 1 | 한반도 정면 중앙 + 식별 | **통과** | 3.3배 확대에서 **엄지 모양 반도가 본토에서 남쪽으로 뻗은 형태로 또렷하게 조각**되어 있다. 왼쪽에 황해, 오른쪽에 **휘어진 일본 열도 호**, 남동쪽에 솜구름. 실측 원반 중심 = (0.500W, 0.530H) — 사실상 정중앙. 라운드 1 최대 실패 항목이 해결됐다. |
| 2 | 보드/받침/가장자리/검은 우주/별 없음 | **통과** | 크림 필드에 떠 있다. ORRERY 의 받침대·어두운 배경·발광 핀 전부 유입 안 됨. |
| 3 | 코발트 하나, 보라·네온 없음 | **통과** | 깊고 차분한 코발트 레진 바다. 나머지는 크림·모래·이끼 초록·무광 흰색. 라운드 1의 과채도 해소. |
| 4 | 손으로 만든 모형으로 읽히는가 | **통과 (강)** | 석고·체친 모래 릴리프에 이끼를 박아 넣은 ORRERY 조각 질감, 솜뭉치 구름, 밀링 황동 링. |
| 5 | 텍스트·로고 없음 | **통과** | 없음. |
| 6 | **ORRERY 테마·재질·조명** (신규) | **통과** | 3행 참조와 나란히 놓으면 같은 계열로 읽힌다. 좌상단 웜 키 + 소프트 바운스, 매크로 얕은 심도. |
| 7 | **배경 흰색/크림** (신규) | **통과** | 대비 6배로 밀어도 배경이 완전히 균일하다. |
| 8 | 황동 링 + 작은 위성, 위쪽 | **통과** | 링이 상단 1/3을 가로지르고 태양전지판 달린 황동 위성이 링 위쪽에 있다. 위로 빠져나갈 거리가 짧다. |
| 9 | 지구본 ≈55% + 상하 여백 | **통과 (후처리)** | 생성 원본은 **72.3%**. 아래 리프레이밍으로 **54.9%** 로 맞췄다. |

**종합: 통과.**

### 스케일 보정 — 재생성이 아니라 캔버스 확장

생성 원본이 72.3%(라운드 1의 73%와 같은 실패)라 영상의 55%→75% 달리인 여지가 없었다. 두 가지 선택지 중:

- (a) 남은 1회로 재생성 → **어렵게 얻은 한반도 조형을 다시 주사위에 건다**
- (b) 캔버스 확장으로 리프레이밍 → 크레딧 0, 지리 손실 0

배경이 균일한 크림(#EAE1D0)이라 (b)가 무손실에 가깝다. 원본을 0.7607배로 줄여 2736×1520 크림 캔버스에 60px 페더링으로 다시 앉혔다:

| 항목 | 생성 원본 | 납품본 |
|---|---|---|
| 지구본 높이 | 72.3% | **54.9%** |
| 원반 중심 | (0.509W, 0.506H) | **(0.500W, 0.530H)** — 살짝 아래 |
| 여백 상/하 | 14.6% / 13.0% | **25.7% / 19.4%** |
| 여백 좌/우 | 30.7% / 28.7% | **34.6% / 34.4%** |

이음매 검사: 대비 6배·밝기 −0.62 로 밀어도 배경에 경계나 얼룩이 나타나지 않는다.

> 지구본 픽셀 지름은 1125 → 856px 로 줄었다. 1920×1080 로 내려 kling head 로 넣으면 약 600px 이라 5초 영상 시작 프레임으로 충분하다. 네이티브 55% 생성을 원하면 **잔여 14.5 크레딧으로 still-3 를 돌릴 수 있다**(`node tools/kie/leg-01-globe.mjs still 3`).

## Leg 2 가 물려받을 것 (갱신)

- **월드 룰(확정)**: ORRERY 조각 릴리프(석고+체친 모래+이끼) + 깊은 코발트 레진 바다 + 밀링 황동 + 솜뭉치 구름 + **크림 필드 배경**. 스타일 참조 체인 = `ref-orrery.jpg` (f_008 크롭) → 이후 leg 전부 이걸 2번 참조로 쓴다.
- **참조 2장 구조가 먹힌다**: 1번 = 프레이밍 전용(크림 위 작은 원반), 2번 = 재질 전용. 역할을 문장으로 못박으면 seedream 이 지킨다.
- **씸 이미지**: Leg 1 영상 마지막 프레임 — **아직 없음**(영상 미실행).
- **Leg 2 시작 조건**: 한반도 정면, 지구본 ≈75%, 남쪽 해안 솜구름, 링·위성 프레임 밖.
- **교훈(누적)**:
  1. seedream 은 **스케일을 %로 주면 무시한다**. "여백이 크다"로 줘도 72%가 나왔다 → **스케일은 후처리 캔버스 확장으로 잡는 게 확실하다.**
  2. 한반도는 **반도 형태 + 황해 + 일본 열도 호를 매번 명시**해야 조형된다.
  3. 유튜브 프레임을 참조로 쓸 땐 **웹캠 PiP·로고 카드·자막·마우스 커서를 반드시 크롭**해서 없앤다.

## 산출물 (갱신)

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.still-2.framed.png` | **납품본** (2736×1520, 지구본 54.9%) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.still-2.png` | 생성 원본 (72.3%, 프로비넌스용) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-orrery.jpg` | ORRERY 스타일 참조 (f_008 크롭) |
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.ref-comp2.jpg` | 프레이밍 참조 |
| `shots/kie/leg01-stills.jpg` | 검수 시트 3행 |

---

# 순백 배경 (#FFFFFF) — 후처리

클라이언트 지시(2026-08-26): 라운드 2 납품본은 검수를 전부 통과했으나 배경이
**크림(#ECE4D6)** 이다. 크림 기미 없는 **완전한 흰색 #FFFFFF** 로 바꿀 것.

## 결론 (한 줄)

**재생성 없이 후처리로 끝났다. 크레딧 0.** 배경은 프레임 전역이 정확히
`255,255,255`(비교 픽셀 152만 개 중 순백 비율 **1.000000**), 지구본·링·위성·솜구름
픽셀은 **양자화 오차 미만(최대 편차 0.04/255)** 으로 그대로다. 예비로 잡아둔
재생성 1회(14.5)는 **쓰지 않았다** — 잔액 987.0 유지.

## 왜 임계값 컷아웃이 아니라 알파 매팅인가

배경만 흰색으로 미는 흔한 방법 셋은 이 그림에서 전부 실패한다.

| 방법 | 이 스틸에서 깨지는 지점 |
|---|---|
| 전역 레벨/흰점 보정 | 배경이 균일하지 않다. 좌상단 (234,225,208) ↔ 우하단 (239,232,220) 로 **최대 12레벨 기울기**. 상수 흰점으로 밀면 기울기가 회색 얼룩으로 남고, 지구본 픽셀까지 같이 밝아진다(= 승인된 그레이드 훼손). |
| 마스크 컷아웃 후 흰 배경 합성 | 상단 극관 림이 **디포커스로 20~40px 에 걸쳐 흐리다.** 하드 컷은 그 자리를 잘라 회색 림을 남기고, 페더를 넓히면 크림이 흰색으로 번지는 **할로**가 생긴다. |
| 밝기 임계값 마스크 | 극관(흰 석고)과 배경(크림)의 **대비가 20레벨뿐**이라 지구본 윗부분이 마스크에서 통째로 빠진다. |

그래서 매팅 합성식을 그대로 되돌리는 **더하기 보정**을 썼다.

```
관측  obs = a·F + (1−a)·B        a = 피사체 커버리지, B = 크림 배경
목표  out = a·F + (1−a)·W        W = (255,255,255)
따라서 out = obs + (1−a)·(W − B)
```

`(1−a)` 는 그 픽셀에 원래 섞여 있던 **배경의 양**이다. 디포커스로 배경이 절반
섞인 림 픽셀은 정확히 절반만 밝아진다. 경계에서 값이 튀지 않으므로 **할로도
회색 림도 원리적으로 생기지 않는다.** `a=1` 이면 `out = obs` — 지구본은 자동으로
무손실이다.

세 가지 세부:

1. **B 는 공간 필드**다. 피사체를 뺀 배경 픽셀만으로 정규화 컨볼루션(σ=110)을
   돌려 기울기를 그대로 따라가는 부드러운 배경을 추정한다. 상수가 아니다.
2. **알파를 두 갈래로** 만든다. 지구본은 **원 피팅**(구는 원이다 — 경계점 최소제곱,
   흐린 상단 30% 는 피팅에서 제외, 이상치 3회 트림) 후 림 흐림 폭만큼 smoothstep
   램프. 링·위성은 배경 편차 기반(황동은 채도가 높아 잘 분리된다).
3. `a≈0` 인 곳은 잔차까지 죽여 **정확히 255** 로 만든다. 필름 그레인(파랑 채널
   ±20레벨 크로마 노이즈)이 남으면 순백이 아니다. **비네트·헤이즈는 더하지 않았다.**

## 실측 — 원 피팅 & 리프레이밍

| 항목 | 값 |
|---|---|
| 피팅된 원반 | 중심 (1378.5, 778.1), 반지름 534.7 → 지름 1069 px = 원본 높이의 **70.4%** |
| 캔버스 | **1920×1080**, 순백 |
| 축소 배율 | 0.5555 (다운스케일만, 업스케일 없음) |
| 지구본 높이 | **55.0%** (594 px) |
| 원반 중심 | **(0.500W, 0.530H)** |
| 여백 상/하 | **25.5% / 19.5%** |
| 여백 좌/우 | **34.5% / 34.5%** |

라운드 2 크림 납품본(54.9%, 25.7/19.4)과 프레이밍이 사실상 같다 — 배경만 바뀌었다.

## 검수 — 2배 확대로 본 것

검수 시트 `shots/kie/leg01-white.jpg`
(1행 before/after · 2행 after 2배 엣지 크롭 5종 · 3행 before 같은 자리 2배 크롭)

크롭 5종은 위험한 자리만 골랐다: **상단 극관 림**(가장 흐린 경계), **좌측 림 + 황동 링**,
**우측 림 + 링이 실루엣을 빠져나가는 지점**, **하단 림**, **솜구름**.

| # | 체크 | 판정 | 근거 |
|---|---|---|---|
| 1 | 배경이 정확히 #FFFFFF | **통과** | 원반 밖 120px 바깥 1,527,322 px 전부 `255,255,255`. min=max=255. |
| 2 | 크림 틴트 잔류 | **통과** | 원반 +54px 부터 프레임 끝까지 R−B 차이 **0**. 틴트 이음매 없음. |
| 3 | 할로 / 회색 림 | **통과** | 원반 바깥 6px 간격 고리별로 재면 비-흰색 비율이 0.288 → 0.102 → 0.037 → 0.0006 → **0** 으로 단조 감소한다. 특정 반경에서 도로 올라가는 구간이 없다 = **테두리 고리가 없다.** 남은 비-흰색은 전부 황동 링(min 121,80,38)과 흐린 림 본체다. |
| 4 | 대비 10배 부스트 검사 | **통과** | 255 기준 잔차를 10배로 밀어도 배경이 완전히 흰색이고, 컷아웃 자국(피팅 반경의 원)이 드러나지 않는다. |
| 5 | 지구본 픽셀 불변 | **통과** | `alpha>0.999` 인 899,290 px 에서 최대 편차 **0.043/255** — 8비트 반올림 후 완전 동일. |
| 6 | 솜구름·링·위성 손실 | **통과** | 2배 확대에서 구름 솜털 가닥, 링의 디포커스 구간, 태양전지판 달린 위성 전부 남아 있다. |
| 7 | 비네트·헤이즈 미추가 | **통과** | 더하기 항은 `(1−a)(255−B)` 하나뿐. 반경 의존 항이 없다. |

**종합: 통과. 2단계(재생성) 불필요.**

## 남는 성질 (알아둘 것)

지구본은 **크림 배경 앞에서 촬영된 물체**다. 배경만 순백으로 바꿨으므로 물체가
받은 **웜한 크림 바운스는 그대로 남아 있다** — 상단 극관이 순백 배경 대비 아주
살짝 따뜻하게 읽힌다. 이건 결함이 아니라 "흰 배경에 놓인 실물 모형"의 정상적인
모습이고, 지시대로 지구본을 건드리지 않은 결과다. 완전히 중립적인 극관을
원하면 그때는 재생성(14.5)이 필요하다.

Leg 2 이후로 넘길 것: **배경색은 생성 단계에서 잡지 말고 후처리로 잡는 게 싸고
정확하다.** 스케일과 같은 결론이다(교훈 1). 생성은 재질·조형·회전에만 크레딧을 쓴다.

## 산출물 (추가)

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/ch1-leg-01-globe.still-2.white.png` | **순백 납품본** (1920×1080, 배경 #FFFFFF, 지구본 55%) |
| `tools/kie/leg-01-white.py` | 후처리 러너 (매팅 배경 교체 + 원 피팅 리프레이밍 + 검증 + 검수 시트). `yolo` 환경 |
| `shots/kie/leg01-white.jpg` | 순백 검수 시트 (before/after + 2배 엣지 크롭) |

> 실행: `~/anaconda3/envs/yolo/python.exe tools/kie/leg-01-white.py`
> (샌드박스에서 numpy 의 BLAS/LAPACK 경로가 프로세스째 죽어서 `dot`/`lstsq` 없이
> 스칼라 산술로만 3×3 정규방정식을 푼다 — 러너 주석 참고.)

## 크레딧 (갱신)

| 시점 | 잔액 | 증감 |
|---|---|---|
| still-2 직후 | 987.0 | — |
| 순백 후처리 | **987.0** | **0** (API 호출 없음) |

이 작업의 캡은 14.5(재생성 1회)였고 **한 푼도 쓰지 않았다.**
