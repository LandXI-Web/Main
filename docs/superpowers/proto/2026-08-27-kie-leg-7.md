# v3 Leg 7 — 회선 끊김 후 taskId 회수 + 스크럽 마운트 기록 (kling/v2-1-pro, 2026-08-27, 배치 7)

- 클라이언트 결정(원문): **"D7 GO"** — Leg 7(A07→A08, rev.3e 앵커) 정확히 1회, 캡 60.
- 앵커/프롬프트: `tools/kie/anchors-v3.json` A07 `motion_prompt` **토씨 그대로**, `negative` 870 → 500자(`trimNeg`, 생략만).
- 러너: `tools/kie/legs-v3.mjs` — `run 7` 은 createTask 직후 폴링 중 **회선 끊김**(`legs-7-run.log`: `generating (10s)` 뒤 `ERROR: fetch failed`). 클라이언트가 taskId `993b669995fce2a04579d5b86677a6d1` 를 전달 → `fetch 7 <taskId>` 로 **recordInfo 만 폴링해 결과를 회수**(새 createTask 0, 과금 0). 이 경로는 커밋 1087aa7 의 `fetch` 서브커맨드.
- 원장: `shots/kie/legs-v3-credits.json` (`batch:"7"` 1건, `recovered:true`) · 로그 `shots/kie/legs-7-run.log`(끊김) · `legs-7-fetch.log`(회수) · 시트 `shots/kie/v3-leg-7-sheet.jpg` (1행 × t=0,1,2,3,4,4.85 + A08) · 사본 `landxi/assets/proto/review/v3-leg-7-sheet.jpg`.
- 규칙 준수: 과금 호출 **정확히 1회**(run), 재생성 0, 추가 스틸 0, 캡 60 중 **50 사용**.

## 결론 (한 줄)

**Leg 7 은 씸이 붙고(06b→07 0.133 % · 07 끝 ↔ A08 일치) 카메라도 한 방향으로만 내려오지만, 프롬프트가 요구한 "모래톱과 스티로폼 더미가 프레임 절반"까지는 내려오지 않는다** — 5초 동안 방파제는 화면폭의 ~1/6 에서 ~1/4 로 커질 뿐이고(끝 고도 ≈ 2–3 km 감), 흰 스티로폼 더미는 이 배율에서 읽히지 않는다. 그리고 앵커 차이가 만든 **방파제 모핑**(A07 짧은 갈고리 → A08 긴 직선)이 t≈0.3–1.5 에 일어난다. 스크럽에서는 "항구가 자라나며 방파제가 뻗는다"로 읽혀 견딜 만하고, 플레이스홀더 07 을 대체해 06b→07 컷(13 %)이 사라진 값이 더 크다.

## 크레딧 · 회수 타임라인

| 시점 | 잔액 | 증감 | 비고 |
|---|---|---|---|
| run 7 createTask 직전 | 9,746.5 | — | `legs-7-run.log` |
| run 7 폴링 끊김(≈10 s) | (미측정) | −50(과금됨) | taskId 는 kie 쪽에만 남음 |
| fetch 7 회수 (2026-08-27T04:30:33Z) | **9,696.5** | 0 | recordInfo `success`, `creditsConsumed 50`, 다운로드 12.8 MB, wall 2.1 s |

잔액 차 9,746.5 → 9,696.5 = 50 = `credits_reported`. 원장 줄은 `fetch` 가 `credits_charged_at_fetch:0 / credits_charged_at_run:50` 으로 적은 것을 `credits_charged:50`(run 시점 잔액 기준) 으로 손봐 배치 합계(`spent_by_rev["rev3-legs7"] = 50`, 캡 잔여 10)가 실제와 맞게 했다. run 이 pending 줄을 남기지 못한 이유: run 은 1087aa7 이전 코드로 돌았다(pending 줄 기록은 그 커밋에서 추가).

## 파라미터

`model kling/v2-1-pro` · `duration "5"` · `cfg_scale 0.5` · head A07.png · tail A08.png · 출력 1932×1072 / 24 fps / 122 프레임 / 5.083 s.
스크럽 인코딩: `scale=-2:1080, libx264 high, preset slow, crf 20, -g 8 -keyint_min 8 -sc_threshold 0 -movflags +faststart -an` → `src/v3-leg-07.mp4` (5.1 MB). 씸 프레임 인코딩본 `-sseof -0.15` → `gen/v3-leg-07.seam.png`.

## Leg 7 — A07 → A08 (여수 해안 · 해양쓰레기)

- taskId `993b669995fce2a04579d5b86677a6d1` · 과금 50 · 생성 485 ms(kie 보고 `gen_ms`, 회수 시점 값) · 원본 12.8 MB
- 프롬프트(verbatim): A07 `motion_prompt` — "The breakwater arm and the moored fishing-boat models stay in frame throughout and only grow larger, the cobalt resin water stays still and wakeless; the warm key holds from upper camera left. The camera descends in one smooth continuous glide along the coastline, dropping steadily while it tilts down from about sixty-five degrees toward forty, until the grey sand strand and the white polystyrene drifts at the foot of the breakwater fill the near half of the frame. The altitude only decreases, never rises. Nothing enters or leaves the frame. One single continuous take, no cuts, no camera shake, no zoom snap. Slow, cinematic, controlled."

| 규칙 | 판정 | 근거(타임스탬프) |
|---|---|---|
| 연속 이동 하나 | **통과** | 0→4.85 s 완만한 전진+틸트다운 하나. t=0 지구본 곡률 지평선(우상단 검은 공간) → t≈2 지평선이 위로 빠지고 → t≥3 평평한 저공 사각. 컷·점프 없음. |
| 느림 | **통과(과하게)** | 너무 느리다. 항구 배율이 5초에 ~1.5× — 프롬프트의 "프레임 절반이 모래톱"에는 한참 못 미친다. 스크럽에는 유리(프레임 간 변화 작음). |
| 피사체 잔류 | **부분** | 방파제·어선·마을은 끝까지 프레임 안. 그러나 **방파제가 t≈0.3–1.5 에 짧은 갈고리(A07)에서 긴 직선 팔(A08)로 모핑**한다 — t=0.5 갈고리 끝이 곧게 펴지기 시작, t=1.0 길이 1.5×, t=1.5 직선 완성, t=2 이후 안정. 마을도 같은 구간에 건물이 두 배로 는다. 앵커 두 장의 방파제 형상 차이를 kling 이 그 사이 1초에 처리한 것. |
| 아무것도 들어오지 않음 | **통과(경미)** | 프롭·판·배경 없음. 어선이 t≈1–2 에 몇 척 늘고(마을과 같은 이유), 바다에 작은 흰 점(부표?)이 t≥3 에 방파제 끝 근처에 생긴다. |
| 환각 | 모핑 1건 | 위 방파제·마을. **흰 스티로폼 더미는 이 배율에서 보이지 않는다** — A08 앵커 자체가 "몇 개 흰 점" 수준이라 기대치도 낮았다. |
| 씸 → A08 | **양호** | t=4.85 ↔ A08 구도·방파제·섬·마을 일치(같은 프레임으로 읽힌다). |

**종합: 통과(모핑 1건, 하강 부족).** 재생성해도 앵커가 같으면 모핑은 반복된다 — 없애려면 A07 방파제를 A08 과 같은 직선형으로 리롤해야 한다(D7 번복). 하강 부족은 스크럽 목적에는 오히려 안정적이어서 수용.

## 씸 실측 (assemble.mjs, 인코딩본 64×36 gray)

| 씸 | 이전 | 지금 | luma |
|---|---|---|---|
| 06→06b | 0.144 % | 0.144 % | 49.7→49.6 |
| **06b→07** | **13.26 %**(미니어처 → 위성 정사 컷) | **0.133 %** | 49.8→49.76 |

전 씸 0.13–0.20 %. 렌더러 전환 씸이 0 이 됐다 — 필름 전체가 kling 단일 소스(`manifest.source`).

## 스크럽 마운트 (`tools/scrub/assemble.mjs --only 07`)

- PLAN 07: `mp4 src/v3-leg-07.mp4` (재인코딩 없이 그대로) · `gen` 에서 모바일본 · look `diorama` · wp `여수` · 카메라 authored **17 km/피치 10 → 2.4 km/피치 40**, 좌표는 국동항 방파제(127.7305, 34.5630)에 고정(영상이 옆으로 거의 움직이지 않는다). 끝 고도 2.4 km 는 마지막 프레임에서 방파제(수백 m)가 화면폭 ~1/4 인 것에서 역산. 이전 플레이스홀더 끝(127.7215, 34.5690 · 11.2 km/피치 2)은 폐기.
- `handoffFinal`: `legIndex` 7(마지막 레그, 계산값 그대로) · **center 를 필름 끝 카메라 그대로**(이전 [127.736, 34.566] 오프셋은 z13 수축 프레임이 V-World 모자이크 공백을 피하려던 것 — z15.8 → 수축 끝 z15.2 의 프레임은 lon ±0.02° 라 공백이 전부 밖). `dzFor(15.82)`: room 0.80 ≥ 0.35 → dz 0.621 그대로, floor 15 불변(e2e 계약).
- 필름 41.02 → **40.66 s**, 트랙 8.942 → **8.864 vh**, 페이스 0.218 편차 0 %. 데스크톱 50.5 MB / 모바일 13.2 MB.
- `landxi/proto/scrub/index.html`: 07 `data-sc-w` 1.186 → 1.108. 카피 창은 레그 1–06b 는 vh 위치 고정(×8.942/8.864), 07 `0.887 0.955`(레그 시작 +0.11 vh ~ 인계 밴드 앞), 피날레 `0.958 1.000`.
  - 07 카피: `07 · 여수 해안 · 해양쓰레기 / 방파제 아래 흰 것들을 센다` + 브래킷 카운트업 **`해양쓰레기 항공 1,857건 · 드론 2,078건 · 8종`**(창 10–70 % = 필름 t≈0.8–2.6 s, 방파제가 뻗는 구간). 숫자는 결과 파일 실측 — `yeosu-marine-2025-aerial.geojson` 1,857 피처 · `yeosu-marine-2026-drone.geojson` 2,078 피처 · `cls` 8종. **샷 리스트의 1,860 은 반올림값이라 쓰지 않았다**(dive 리포트도 1,857). 타입은 토큰(`lx-label/sb-h2/sb-bracket lx-cap/lx-num`)만.
  - 인계 카피: `여수 국동항 · 127.7305, 34.5630 · ALT 2.4 km`.
- 타임라인 `landxi/proto/film/timeline.html`: 행 07 → `AI 생성 (kling v2-1-pro)` 5.08 s, 08–12 시작 40.67 → 61.00 s, 헤더 40.67 s.

## 테스트 · 촬영

- `npx playwright test tests/e2e/proto-scrub.spec.mjs` → **4 passed (4.9 m)**. 기대값 수정 0 — `M.legs.length` 8 · `seek(0.5)` 레그 05 · 지연 로딩 index 7 · 인계 카메라 · 마감 판(floor(zoom) 15 불변, 수축 ×0.65) 전부 그대로 통과. 주석만 갱신(트랙 8.864vh).
- `node tools/scrub/shoot-strip.mjs --legs 6b-7 --prefix legs-6b-7`(마지막 레그일 때 다음 씸 대신 여수 인계 밴드 in/mid/end 를 찍도록 수정) → `shots/scrub/legs-6b-7-{1..7}-*.png`, 몽타주 `legs-6b-7-montage.jpg`, 콘솔/페이지 오류 0.
  - seam 06b→07 mid(필름 5.08 s / 계기 17 km): 같은 프레임, 크로스페이드 안 보임(이전엔 위성 정사가 밑에서 올라왔다).
  - L07-mid(필름 2.53 s / 6.4 km): 방파제 직선 완성 뒤, 마을 확대 중.
  - handoff-07-in(4.96 km): 필름 위 인계 카피. handoff-07-mid/end(2.40 km): V-World 위성 판이 같은 좌표·피치 40 으로 켜진다. 모자이크 공백 없음.

## 다음 라운드에 반영할 것

1. 방파제 모핑은 A07↔A08 형상 차이의 대가. leg 8(A08→A09)은 A08 에서 시작하므로 영향 없음.
2. "저공 하강"을 kling 이 소극적으로 처리한다(leg 5 도 비슷) — 정말 모래톱 근접이 필요하면 A08 을 더 낮은 사각으로 리롤하고 프롬프트의 종료 조건을 구체 숫자 대신 앵커에 맡긴다.
3. 이번 회수로 `fetch` 경로가 실전 검증됐다. 이후 run 은 pending 줄을 먼저 적으므로 taskId 없이 `fetch n` 만으로 회수된다.

## 산출물

| 경로 | 내용 |
|---|---|
| `landxi/assets/proto/film/legs/gen/v3-leg-07.mp4` · `.seam.png` | kling 원본(taskId 회수) · 씸 프레임 |
| `landxi/assets/proto/film/legs/src/v3-leg-07.mp4` (+ `.first/.last.webp`) | 스크럽본 |
| `landxi/assets/proto/film/legs/w07{,-m}.mp4`, `w07{,-last}.webp`, `manifest.json` | 마운트 산출물(8 레그, 단일 소스) |
| `shots/kie/v3-leg-7-sheet.jpg` · `v3-leg-07-t*.jpg` · `legs-7-run.log` · `legs-7-fetch.log` | 시트 · 프레임 · 로그 |
| `landxi/assets/proto/review/v3-leg-7-sheet.jpg` | 리뷰 사본 |
| `shots/scrub/legs-6b-7-*.png` · `legs-6b-7-montage.jpg` · `legs-6b-7-strip.json` · `e2e-legs-6b-7.log` | 스크럽 실촬영 · e2e 로그 |
| `shots/kie/legs-v3-credits.json` | 크레딧 원장(batch 7, recovered) |
| `tools/scrub/assemble.mjs` · `tools/scrub/shoot-strip.mjs` · `landxi/proto/scrub/index.html` · `landxi/proto/film/timeline.html` | 조립기(07 AI) · 촬영(마지막 레그) · 카피 · 타임라인 |
