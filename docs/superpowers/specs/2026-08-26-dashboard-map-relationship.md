# 대시보드 ↔ 지도 관계 정리 — 현황 위젯을 지도와 자연스럽게 어울리게 하는 법

- 작성일 2026-08-26 · 브랜치 `plan1-foundation` · **설계 문서. 코드는 한 줄도 건드리지 않았다.**
- 발주(원문): *"대시보드를 다시 정리해야 한다. 지도에서 AI 추론 결과가 보이는데, 대시보드 기능을 보면
  학습데이터 현황, AI 분석 실행 대기/완료 등 이런 것들 위주다.
  **현황 대시보드를 어떻게 지도와 자연스럽게 어울리게 하느냐가 관건.** 조사 자료도 복합적으로 검토해서 정리."*
- 선행: `2026-08-26-map-dashboard-options.md`(A안 확정) · `2026-08-26-landxi7-function-inventory.md` §2 ·
  `2026-08-25-client-taste-profile.md` §4 §5 §7 · 벤치마크 8종(팔란티어·kepler·vw-lab·all4land·alive·fui·RDyt·iyRYc)
- 대상 아트보드: `design-canvas/v2/renders/B2-Dashboard.png`(현행 마스터) · `design-canvas/v2/NOTES.md` §4

---

## 0. 이 문서가 답하는 한 문장

> **판(plate)은 위젯의 배경이 아니라 원장(ledger)의 대답이다.**
> 현황 숫자가 지도 위에 "얹히는" 게 아니라, **위치를 가진 현황만** 지도에서 스스로 자기 모양으로 서고,
> 위치가 없는 현황은 지도에 올리지 않는 것이 곧 "자연스럽게 어울리는" 것이다.

### 0.1 발주 문장에 대한 사실 확인 (Playwright 재검증, 2026-08-26)

원본 <https://mini531.github.io/namwon-smart-village/landxi7/dashboard.html> 을 1440×900 `networkidle` 로
다시 열어 DOM·인라인 스크립트를 전량 덤프했다. **탭 0개 · `<table>` 0개 · 지도 요소 0개** — `options.md` §1.3 과 일치.

| 발주가 부른 이름 | 원본 대시보드의 실제 | 실제로 어디 있나 |
|---|---|---|
| **학습데이터 현황** | **단독 위젯 없음** | B12 스토리지 도넛의 `학습데이터 7.4 TB` 세그먼트 + B10 프로젝트별 데이터 사용량 Top5 |
| **AI 분석 실행 대기/완료** | **대시보드에 없음** | A5 `analysis-ai.html` 3탭. 실측: 대기 1 · 실행 1 · 실패 1 · 완료 7 = **10건** |
| 대시보드에 있는 "대기" | **카드 발행 승인 대기 2건**(B6 KPI · B13 목록)뿐 | — |

> **따라서 이 문서는 위젯을 새로 만들지 않는다.** 발주가 말한 두 가지는
> 원본 대시보드 안에서 **B10·B12(학습데이터·용량)** 와 **B9 `연결된 분석 과제 14개`(분석 실행)** 라는
> 이미 있는 숫자로 존재한다. 지도는 그 숫자를 **펼쳐 보이는 표현**이지 새 기능이 아니다.

### 0.2 원본이 스스로 자백한 데모 시드 (인라인 주석 원문)

정직 태깅(§5)의 근거는 우리 판단이 아니라 **원본 소스의 주석**이다.

| 위젯 | 원본 주석 | 판정 |
|---|---|---|
| B10 Top5 | `/* 프로젝트 현황 - 데이터 사용량 Top 5 (가로 막대). 프로젝트명=실데이터, 사용량=데모 */` | 이름 `측정` / GB 값 `추정` |
| B11 7일 방문 | `/* 사용자 이용 현황 - 최근 7일 방문 (막대, 데모) */` `vd=[38,52,47,61,55,24,19]` | **전량 `추정`** |
| B12 스토리지 | `/* … 실데이터: SSD 32.4 / HDD 12.1 TB */` (합 44.5) | 총량 `44.5 / 184 TB` = `측정` / **7분류는 `추정`** |
| B13 승인 2건 | `/* … 데모용 시드(요청자=AI 부서 담당자). 실제 구현 시 카드 발행 요청 큐에서 status='pending' 목록을 채운다. */` | 건수 2 = `측정`(원본 규약) / 요청자·일시 `추정` |

### 0.3 지도에 올릴 수 있는 실자산 (이 문서가 쓰는 전부)

| 자산 | 실측 | 출처 |
|---|---|---|
| 남원 농지이용 2025 | 2,098필지 · 315.9 ha · **읍면동 33개 분해**(운봉읍 237 · 송동면 149 · 대강면 142 · 보절면 138 …) | `results.js` |
| 남원 비닐하우스 2025 | 1,674필지 · **읍면동 29개 분해**(금지면 397 · 운봉읍 275 · 인월면 157 · 아영면 147 …) | `results.js` |
| 여수 해양쓰레기 | 항공 1,860 / 드론 2,078(8클래스) | `results.js` |
| 정사영상 | 11종 · 남원 4시점(2504·2506·2508·2510, GSD 1.08–1.69 cm) · 국산리 2 · 제주 3 · 남원 시가지 2 | `imagery.js` |
| 모델 | 10종 · 클래스 합 57 · `inferred:true` 6건 | `models.js` |
| 작업 AOI | A5 실측 10건이 **전부 남원 읍면동 단위**(주천면 · 아영면 · 금지면 · 시중앙권 · 운봉읍 · 인월면 · 도통동 · 사매면) | `page-analysis-{running,done}.js` |
| **없는 것** | 공지 12건에 **지역 필드 0개**(실측) · 발행 카드 8건의 개별 지역 **미확정** · DSM−DTM 0 | — |

---

## 1. 원본 대시보드 기능의 성격 분류

`무엇의 현황인가` 5분류 = **학습데이터 / 분석 실행 상태 / 승인·문의 업무 / 사용·용량 / 모델·버전**
`공간` = 그 값이 **어느 지역·어느 영상·어느 AOI에 묶이는가** (yes / partial / no)
`시간` = 그 값이 **어느 시점에 매여 있는가** (yes / partial / no)

| # | 위젯 | 원본 값 | 무엇의 현황 | 공간 | 무엇에 묶이나 | 시간 | 시간축 |
|---|---|---|---|---|---|---|---|
| B1 | 제목 + 부제 | `LX 관리자 대시보드` | — (화면 정체) | **no** | — | **no** | — |
| B2 | 기준일 | `2026.08.26` (`new Date()`) | 사용·용량(시스템 시각) | **partial** | 값 자체는 위치 없음. **데이터 기준시점으로 승격하면** 정사영상 4시점에 묶인다 | **yes** | 오늘 / (승격 시) 취득 시점 |
| B3 | 공지 스트립 | 고위험 탐지 건 긴급 처리 안내 · 2026.04.15 | 승인·문의 업무 | **no** | **원본 공지 12건 전부 지역 필드 없음**(실측) | **yes** | 게시일 |
| B4 | KPI ① 전체 사용자 | 21명 / 정상 19 · 대기 1 | 사용·용량(계정) | **no** | — | **no** | 스냅샷 |
| B5 | KPI ② 발행 분석 카드 | 8건 / 공개 7 · 비공개 1 | 승인·문의 업무(산출물) | **partial** | 카드는 분석 결과를 가리키나 **8건의 개별 지역이 원본에 없다** | **yes** | 발행일(목록 미노출) |
| B6 | KPI ③ 카드 발행 승인 대기 | 2건 / 검토 필요 | 승인·문의 업무 | **partial** | B13 2행의 요약. 행이 가리키는 과제를 통해서만 지역이 생긴다 | **yes** | 요청일시 |
| B7 | KPI ④ 가입 승인 대기 | 1건 / 승인 필요 | 승인·문의 업무(계정) | **no** | — | **no** | 스냅샷 |
| B8 | KPI ⑤ 미답변 문의 | 6건 / 전체 12 | 승인·문의 업무 | **no** | 문의 2건 실측에 지역 필드 없음 | **yes** | 접수일 |
| B9 | AI 기반 모델(백본) | XI-VFM v2.1 · 2026.03.12 · **연결된 분석 과제 14개** | **모델·버전 + 분석 실행 상태** | **partial → yes** | 모델 자체는 위치 없음. **`14개`가 가리키는 과제는 전부 읍면동 AOI를 가진다**(실측 10건) | **yes** | 최종 적용일 · 각 과제 실행일 |
| B10 | 차트 ① 프로젝트 현황 Top5 | 412 / 318 / 256 / 198 / 142 GB | **학습데이터 · 사용·용량** | **yes** | 5개 프로젝트 = 실제 사업 종목(도로안전 · 농지 · 비닐하우스 · 사료작물) → 실자산 지역 | **partial** | 누적값, 시점 없음 |
| B11 | 차트 ② 최근 7일 방문 | 38·52·47·61·55·24·19 | 사용·용량(접속) | **no** | — | **yes** | 7일 |
| B12 | 차트 ③ 스토리지 | 44.5 / 184 TB · 7분류 | **학습데이터 · 사용·용량** | **partial** | `정사영상 18.2`는 실제 도엽 11종에, `학습데이터 7.4`는 라벨셋에 귀속. **배분 자체는 데모** | **no** | 스냅샷 |
| B13 | 카드 발행 승인 대기 목록 | 2행(도로안전 정사영상 v2.1 / 농지 활용 분석 v2.0) | 승인·문의 업무 | **yes** | 각 행 = 특정 카드 = 특정 분석 과제 = **특정 AOI** | **yes** | 요청일시 |
| B14 | 사용자·콘텐츠 관리 타일 4 | 21명 / 12건 / 6·12 / 15건 | 승인·문의 업무 · 사용·용량 | **no** | — | **no** | 스냅샷 |
| B15 | 푸터 | 회사 정보 · Family Site | — | **no** | — | **no** | — |
| B16 | 딥링크 규약 | `?status=대기` `?open=<id>` | (규약) | **n/a** | 상태를 URL로 실어 나르는 규약 | **n/a** | — |

**집계**: 공간 `yes` 3(B10 · B13 + 승격된 B9) · `partial` 5(B2 · B5 · B6 · B9 · B12) · `no` 7(B1 · B3 · B4 · B7 · B8 · B11 · B14) + B15 · B16.

> **16개 중 지도가 진짜로 할 말이 있는 것은 넉넉히 잡아도 8개다.**
> 나머지 8개를 지도에 올리려는 순간 불투명 격자·가짜 히트맵이 생긴다 — 그것이 이전 Ops Atlas 빌드의 반려 사유였다.

---

## 2. 관계 등급 3단

| 등급 | 정의 | 판(plate)의 역할 | 개수 |
|---|---|---|---|
| **Ⅰ 지도에 직접 표현** | 그 현황 **자체가 위치를 가진 것** | 첫 프레임부터 **아무 조작 없이** 판 위에 서 있다 | **4** |
| **Ⅱ 지도와 연동** | 값은 위치가 없지만 **가리키는 대상이 위치를 가진 것** | 원장 행 호버·클릭 때만 판이 **카메라·감쇠로 응답** | **4** |
| **Ⅲ 원장 전용** | 위치가 없는 것 | **판은 반응하지 않는다.** 무반응이 곧 정직성의 증명 | **8** |

기본 규칙 셋:

1. **Ⅰ만 상시 그린다.** Ⅱ는 사용자가 원장을 만졌을 때만 판에 나타나고, 손을 떼면 사라진다.
2. **감쇠하되 삭제하지 않는다**(Palantir P1). 필터·선택은 비매칭을 흐리고 클릭 불가로 만들 뿐이다.
3. **선 종류가 정직성이다**(`yt-iyRYc9sVRsw` §5-2 D5 → `측정 = 실선 + 채움` / `추정 = 점선 고스트` / `미확정 = 무채 파선 + 이유 한 줄`).
   색으로 신뢰도를 말하지 않는다. 범례에 정의문 1줄을 반드시 노출한다(`benchmark-vwlab` §5-5).

### 2.1 Ⅰ등급 — 지도에 직접 표현 (4)

| # | 위젯 | 판 위의 정확한 처리 | 조사 장치 | 정직 태그 규칙 |
|---|---|---|---|---|
| **B9** | 연결된 분석 과제 `14개` → **작업 AOI 상태 레이어** (= 발주가 말한 "AI 분석 실행 대기/완료") | 남원 읍면동 경계를 AOI로 쓰고 **상태별 헤어라인 4종**을 판에 직접 그린다.<br>· **완료 7** = 잉크 1px **실선** + 채움 0 + 내부 우하단 `읍면동 · n건`(실측 `emd` 값)<br>· **실행 1**(주천면) = LX 블루 1px 실선 + **AOI 안쪽을 훑는 스윕선 1개**(주기 6s) + 테두리에 5스텝 진행 호(`대기·전처리·분석·후처리·완료`)<br>· **대기 1**(금지면) = 무채 **파선**, 채움 0, 정지<br>· **실패 1**(아영면) = 무채 파선 + `실패 · 사유` 한 줄. **삭제하지 않는다** | D10 락온 3비트(`bench-fui` §4 D9) · D20 이미지 안 헤어라인(`benchmark-vantor` §2.6) · P1 감쇠(`benchmark-palantir` §2 P1) | 읍면동 경계 = `측정`(실경계).<br>작업 10건은 원본 시드 → 판 우하단 `모의 실행 · 원본 시드 10건` 한 줄.<br>**원본 B9의 `14개`와 실측 목록 10건이 원본 자체에서 어긋난다 → `AOI 미지정 4`를 원장 B9 카드에 자백 한 줄로 선언하고 지도에는 그리지 않는다** |
| **B10** | 프로젝트 현황 Top5 → **사업 지역 코로플레스** (= 발주가 말한 "학습데이터 현황"의 지역 표현) | 시군구 면이 중심에서 거리순 70ms 스태거로 **원형으로 번지며** 채색(1,100ms). 실자산 4곳(남원 · 여수 · 제주 · 국산리)만 채우고 **나머지 전 시군구는 무채 `#E8EBEE`** — 어둡게 하지 않는다. 원장의 랭크드 바 5행과 **같은 순번 · 같은 굵기**의 헤어라인으로 결선 | D09 라디얼 와이프(`bench-fui` §4 D10 · `benchmark-vwlab` §5-2) · D16 랭크드 바(`yt-RDytbVDzMF4` §3 F8) | 프로젝트명 = `측정`(실선).<br>**GB 값 = `추정`** → 점선 고스트 바 + `[추정]` 태그(원본 주석 `사용량=데모`).<br>자산 없는 시군구는 `missingList`로 선언 + 무채 |
| **B12** | 스토리지 `정사영상 18.2 TB` → **정사영상 footprint** | `imagery.js` 11종의 실제 범위를 판 위에 **가장 얇은 헤어라인 액자**로 그린다(남원 4시점은 겹치므로 액자 1개 + `4 시점` 뱃지). 액자 바깥 위쪽 2단 캡션 `GSD 1.08 cm · 2025-04` | D18 액자-not-카드(`bench-nodes-creative` §2 D1) · D15 프로비넌스(`yt-RDytbVDzMF4` §3 F18) | footprint · GSD · 촬영일 = `측정`(`imagery.js` 실측).<br>**용량 배분 7분류 = `추정`** — 원본 주석상 총량 44.5 TB만 실데이터.<br>도넛 범례 옆 `[추정]` 1회 |
| **B13** | 카드 발행 승인 대기 2행 → **대상 지역 핀 2** | 판 위 핀 2개. 채운 마커가 아니라 **12px 십자 + 점선 리더선**으로 원장 행과 물리적으로 이어진다.<br>도로안전 정사영상 v2.1 → 도통동 · 농지 활용 분석 v2.0 → 시중앙권 | D10 락온 · D06 개체→속성(`benchmark-palantir` §2 P3 · §1.3) | 카드명 · 건수 = `측정`(원본 규약값).<br>**카드 ↔ 지역 연결은 우리가 주장한 것 → `추정(연결)`** 점선 리더 + 시작점에 `연결 추정` 6px 라벨.<br>요청자 · 일시는 원본 주석상 데모 → 원장 행에 `[추정]` |

### 2.2 Ⅱ등급 — 지도와 연동 (4)

| # | 위젯 | 원장을 만졌을 때 판이 하는 일 | 조사 장치 | 정직 태그 규칙 |
|---|---|---|---|---|
| **B2** | 기준일 | 원본 `new Date()`를 **데이터 기준시점으로 승격**: `● 2025-10 정사영상 기준 · 기준일 2026.08.26`.<br>클릭하면 판 하단 유리 스크러버의 창이 그 시점으로 750ms 이동하고 판의 밑그림 영상이 교체된다 | D08 신선도 + LIVE 스탬프(`benchmark-palantir` §2 P13 · `bench-fui` §4 D12) · D02 유리 스크러버(`bench-taste-kepler-planet-video` §7 장치3) | 취득 시점 = `측정`. `new Date()` 시스템 날짜는 **데이터가 아니므로 액센트를 주지 않는다**(회색 라벨) |
| **B5** | 발행 분석 카드 8건 | 클릭 → 판에서 **발행의 원천이 되는 결과 4건만 남기고 나머지 감쇠**. 상단에 `[발행 카드 ×]` 제거 가능 칩.<br>**8건 각각의 위치는 판에 표시하지 않는다** — 원본에 목록이 없다 | D05 감쇠 + 칩 바(`benchmark-palantir` §2 P1) · D04 브러시-링크(P2 · P16) | 판 우하단 `카드 8건 ↔ 지역 매핑 미확정 · 결과 4건만 표시` = **미확정 파선 + 이유 한 줄** |
| **B6** | KPI 카드 발행 승인 대기 2건 | 원본 딥링크 `?status=대기`와 **같은 동작**: 판의 비대상 개체 감쇠 + `[승인 대기 ×]` 칩 + 원장 B13 2행에 40ms 스태거 브래킷 | D05 · D04 · D10 | B13의 태그를 그대로 상속. **새 상태를 만들지 않는다** |
| **B16** | 딥링크 규약 | 규약을 **지도 상태 규약으로 확장**: `?status=` `?open=`에 더해 **현재 카메라 · 시점 창 · 필터 칩을 그대로 들고** A6 지도 서비스로 넘어가는 `[전체 지도로]`. 뒤로 오면 같은 상태가 복원된다 | D07 상태 보존 승격(`benchmark-palantir` §2 P7) | URL에 실리는 값 = 원본 파라미터 + 카메라 4수치뿐. **지어낸 상태 키를 추가하지 않는다** |

### 2.3 Ⅲ등급 — 원장 전용 (8) · 지도에 억지로 올리지 않는다

| # | 위젯 | 왜 지도에 없나 | 원장 안에서만 하는 일 | 조사 장치 |
|---|---|---|---|---|
| **B1** | 제목 + 부제 | 화면 정체성, 값이 아님 | 상단 고정. 판과 무관 | — |
| **B3** | 공지 스트립 | **실측: 원본 공지 12건 전부 지역 필드 0개.** 본문의 "고위험 탐지 건 12% 증가"에도 AOI가 없다 | 호버 시 원장 안에서만 라인 마스크 리빌. **판은 정지한다** | D20 `clip-path: inset(-5px 0)` 리빌 |
| **B4** | 전체 사용자 21명 | 계정에 좌표가 없다 | 진입 시 카운트업 1회 후 정지 | D13 카운트업(`benchmark-all4land` §3-3 ⑨) |
| **B7** | 가입 승인 대기 1건 | 위와 같음 | 원장 내 코너 브래킷만 | D10 |
| **B8** | 미답변 문의 6건 | 실측 문의 2건에 지역 필드 없음 | 원장 내 코너 브래킷만 | D10 |
| **B11** | 최근 7일 방문 | 접속에 좌표가 없다 | **스파크라인**. 형태가 B10 랭크드 바 · B12 도넛과 서로 다르다(균일 카드 그리드 금지) | D16 스탯 타일 3종(`yt-RDytbVDzMF4` §3 F8) |
| **B14** | 관리 타일 4 | 업무 큐, 좌표 없음 | 호버 시 코너 브래킷 380ms | D10 |
| **B15** | 푸터 | 정적 | 콜로폰 한 줄을 여기에 둔다 | D17 정직 태깅(`yt-iyRYc9sVRsw` §5-2) |

> **Ⅲ등급의 무반응이 이 설계의 핵심이다.**
> B4 · B7 · B8 · B14를 만졌을 때 판이 아무것도 하지 않는 것은 버그가 아니라
> **"이 숫자는 위치가 없다"는 화면의 선언**이다. 이것 하나로 취향 프로필 §5-9(모든 숫자가 해설인가)와
> §7 콘티 원칙(지어낸 운영 서사 금지)이 동시에 지켜진다.

### 2.4 등급 요약

```
Ⅰ  지도에 직접 표현 (상시 · 조작 없이)   B9   B10   B12   B13
Ⅱ  지도와 연동 (원장을 만졌을 때만)      B2   B5    B6    B16
Ⅲ  원장 전용 (판 무반응)                B1   B3    B4    B7   B8   B11   B14   B15
```
